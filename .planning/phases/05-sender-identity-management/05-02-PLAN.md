---
phase: 05-sender-identity-management
plan: 02
type: tdd
wave: 2
depends_on: [05-01]
files_modified:
  - app/Http/Controllers/Workspaces/SendersController.php
  - app/Http/Requests/Workspaces/SenderUpdateRequest.php
  - routes/web.php
  - resources/views/senders/index.blade.php
  - resources/views/senders/edit.blade.php
  - resources/views/senders/_form.blade.php
  - tests/Feature/Workspaces/SenderControllerTest.php
autonomous: true
requirements: [SENDER-03, SENDER-04, SENDER-05]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - A member can edit any sender in the current workspace and sees updated values in the list.
    - A member can delete any sender in the current workspace only after the documented confirmation, and the empty state appears when the last row is removed.
    - Invalid fields retain submitted values and show field-specific validation, while a foreign workspace sender ID returns 404 for edit/update/delete.
  artifacts:
    - Resource-style edit/update/destroy routes and a shared update Form Request exist.
    - Edit and delete controls are CSRF-protected and rendered in the approved Bootstrap UI.
    - Feature coverage proves all five Phase 5 requirements, including member authorization and cross-workspace concealment.
  key_links:
    - Every edit/update/destroy action resolves the sender via the active workspace relation before reading or mutating it.
    - The same normalized field contract is used on create and update so later Phase 7 capture can share deterministic data rules; normalized duplicate pairs are rejected in both paths.
    - Delete confirmation prevents issuing DELETE on cancellation and success redirects back to the list; the rendered delete form contains the documented confirmation handler.
---

<objective>
Complete the sender identity library with shared-member edit/delete behavior, validation/error states, and adversarial workspace-isolation coverage while preserving the Plan 01 tracer architecture.

Purpose: Turn the proven create/list path into the full Phase 5 CRUD capability required by SENDER-03, SENDER-04, and SENDER-05 without owner-only restrictions or vendor edits.
Output: A tested, package-layout sender CRUD page with relation-first 404 concealment and documented delete confirmation.
</objective>

<execution_context>
@/Users/meigire/Work/idai-jatim/sendportal/.opencode/gsd-core/workflows/execute-plan.md
@/Users/meigire/Work/idai-jatim/sendportal/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-sender-identity-management/05-CONTEXT.md
@.planning/phases/05-sender-identity-management/05-RESEARCH.md
@.planning/phases/05-sender-identity-management/05-UI-SPEC.md
@.planning/phases/05-sender-identity-management/05-VALIDATION.md
@AGENTS.md
@.planning/phases/05-sender-identity-management/05-01-PLAN.md
@app/Http/Controllers/Workspaces/SendersController.php
@app/Http/Requests/Workspaces/SenderStoreRequest.php
@resources/views/senders/index.blade.php
@resources/views/senders/_form.blade.php
@tests/Feature/Workspaces/SenderControllerTest.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — specify complete CRUD, validation, and tenant-concealment behavior</name>
  <files>tests/Feature/Workspaces/SenderControllerTest.php</files>
  <read_first>
    tests/Feature/Workspaces/SenderControllerTest.php; app/Http/Controllers/Workspaces/SendersController.php; app/Http/Requests/Workspaces/SenderStoreRequest.php; routes/web.php; .planning/phases/05-sender-identity-management/05-CONTEXT.md; .planning/phases/05-sender-identity-management/05-UI-SPEC.md
  </read_first>
  <action>Following TDD RED, extend the existing feature test with separate behavior cases for SENDER-03 edit by an ordinary member, SENDER-04 delete by an ordinary member, required label/from_name and malformed from_email rejection with preserved input/errors, normalized duplicate rejection on create/update, and D-04 foreign sender edit/update/delete requests returning 404 without mutation. Add assertions that D-02 permits a member to mutate a shared sender created by another member, D-01 does not require owner middleware, delete cancellation leaves the row intact, the rendered delete form contains an HTML/DOM assertion for the exact documented confirmation contract, and the list shows the specified empty state after the last deletion. Define and assert the exact Phase 5 CRUD route allowlist: `senders.index`, `senders.create`, `senders.store`, `senders.edit`, `senders.update`, and `senders.destroy`; the filtered sender route names must equal this set, with no additional sender route. For every sender-ID route (`senders.edit`, `senders.update`, and `senders.destroy`), send a request using a foreign-workspace ID and assert HTTP 404 plus no mutation. Also assert that the planned later-phase identifiers `campaigns.sender-selection`, `campaigns.sender-auto-capture`, `App\\Services\\Campaigns\\SelectCampaignSender`, and `App\\Services\\Campaigns\\AutoCaptureCampaignSender` are absent/not exposed in Phase 5. Mark this as the forward-looking Phase 6 integration constraint: Phase 6 may add campaign selection only through its own current-workspace-scoped seam. Verify navigation active state and exact UI copy where it is observable. Run the focused suite before changing production code; the new tests must fail for missing behavior rather than syntax/import reasons, then commit the RED gate with test(05-02).</action>
  <behavior>
    - PUT to a current-workspace sender updates all three fields and redirects with Sender updated successfully.
    - DELETE to a current-workspace sender removes it and redirects with Sender deleted successfully.
    - Missing label/from_name and invalid from_email return validation errors while valid submitted values remain available.
    - Any foreign sender ID on edit/update/delete returns 404 and leaves the foreign row unchanged.
    - A cancelled delete confirmation does not issue DELETE and leaves the row visible.
    - The sender row delete form contains the documented `confirm` message/handler in rendered HTML, proving the browser must confirm before issuing DELETE.
    - The filtered sender route names are exactly `senders.index`, `senders.create`, `senders.store`, `senders.edit`, `senders.update`, and `senders.destroy`; no campaign sender-selection or auto-capture route is exposed.
    - `campaigns.sender-selection`, `campaigns.sender-auto-capture`, `App\\Services\\Campaigns\\SelectCampaignSender`, and `App\\Services\\Campaigns\\AutoCaptureCampaignSender` are absent/not exposed in Phase 5.
    - Foreign sender IDs return 404 on every sender-ID CRUD route (`senders.edit`, `senders.update`, `senders.destroy`) without mutation. Phase 6 must preserve this current-workspace scope when it adds campaign selection.
  </behavior>
  <verify>
    <automated>vendor/bin/phpunit tests/Feature/Workspaces/SenderControllerTest.php</automated>
  </verify>
  <acceptance_criteria>
    - The test additions cover SENDER-03, SENDER-04, SENDER-05, D-01, D-02, and D-04 with observable HTTP/database assertions.
    - RED failures identify unimplemented edit/delete/validation/isolation behavior, not test setup errors.
    - The RED commit is present before the GREEN implementation commit.
  </acceptance_criteria>
  <done>The complete Phase 5 behavior contract is executable and failing for the intended gaps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement update/delete, validation states, and relation-scoped 404s</name>
  <files>app/Http/Controllers/Workspaces/SendersController.php, app/Http/Requests/Workspaces/SenderUpdateRequest.php, routes/web.php, resources/views/senders/index.blade.php, resources/views/senders/edit.blade.php, resources/views/senders/_form.blade.php</files>
  <read_first>
    app/Http/Controllers/Workspaces/SendersController.php; app/Http/Requests/Workspaces/SenderStoreRequest.php; routes/web.php; resources/views/senders/index.blade.php; resources/views/senders/create.blade.php; resources/views/senders/_form.blade.php; app/Http/Middleware/RequireWorkspace.php; .planning/phases/05-sender-identity-management/05-UI-SPEC.md; .planning/phases/05-sender-identity-management/05-RESEARCH.md
  </read_first>
  <action>Following TDD GREEN, add the exact named CRUD routes `senders.index`, `senders.create`, `senders.store`, `senders.edit`, `senders.update`, and `senders.destroy` to the existing host sender group, and ensure the sender route-name allowlist contains no other route. Resolve every sender for edit, update, and destroy through `$request->user()->currentWorkspace()->senders()->findOrFail($senderId)` (or an equivalent relation-scoped lookup), so D-04 returns 404 and never exposes a foreign record. Use a dedicated SenderUpdateRequest or a shared validated request with the same required/string/email/max rules and resolved normalization as create; reject duplicate normalized `(workspace_id, from_name, from_email)` pairs while allowing the current record to retain its own pair. Authorize any active-workspace member per D-01 and D-02, never OwnsCurrentWorkspace. Add an edit page using the exact UI-SPEC Edit Sender / Save Changes / Return to Senders copy, preserve old values and inline errors, and add an index delete form with CSRF, method spoofing, destructive styling, and the documented confirmation before DELETE. Preserve the exact success/error/empty-state copy, responsive table behavior, escaping, and no owner/creator affordances. Do not expose the later-phase identifiers `campaigns.sender-selection`, `campaigns.sender-auto-capture`, `App\\Services\\Campaigns\\SelectCampaignSender`, or `App\\Services\\Campaigns\\AutoCaptureCampaignSender`; Phase 6 must introduce campaign selection through a separate current-workspace-scoped integration and must not weaken this boundary. Iterate until all new tests pass, then commit feat(05-02).</action>
  <verify>
    <automated>vendor/bin/phpunit tests/Feature/Workspaces/SenderControllerTest.php</automated>
    <automated>php artisan route:list --path=senders</automated>
    <automated>php -l app/Http/Requests/Workspaces/SenderUpdateRequest.php &amp;&amp; php -l app/Http/Controllers/Workspaces/SendersController.php</automated>
  </verify>
  <acceptance_criteria>
    - Focused tests pass for edit, delete, validation, shared-member access, and foreign-ID 404 behavior.
    - Every mutation route is protected by auth, verified, and RequireWorkspace, uses CSRF/method spoofing where applicable, and has no owner-only middleware.
    - Foreign sender IDs return 404 for display and mutation without changing either workspace's data.
    - Create and edit normalize fields consistently, reject duplicate normalized pairs, and use only validated fields.
    - The executable test suite proves the exact six-route Phase 5 CRUD allowlist, foreign-workspace 404 behavior for every sender-ID route, and absence of the named Phase 6/7 route and service identifiers; Phase 6 integration is explicitly constrained to current-workspace sender selection.
  </acceptance_criteria>
  <done>All Phase 5 CRUD and tenancy behaviors are green through the host-owned Laravel stack.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR — audit complete CRUD UI, security, and cross-database compatibility</name>
  <files>database/migrations/2026_08_05_000000_create_senders_table.php, app/Models/Sender.php, app/Models/Workspace.php, app/Http/Controllers/Workspaces/SendersController.php, app/Http/Requests/Workspaces/SenderStoreRequest.php, app/Http/Requests/Workspaces/SenderUpdateRequest.php, routes/web.php, app/Providers/AppServiceProvider.php, resources/views/layouts/sidebar/sendersMenuItem.blade.php, resources/views/senders/index.blade.php, resources/views/senders/create.blade.php, resources/views/senders/edit.blade.php, resources/views/senders/_form.blade.php, tests/Feature/Workspaces/SenderControllerTest.php</files>
  <read_first>
    database/migrations/2026_08_05_000000_create_senders_table.php; app/Models/Sender.php; app/Models/Workspace.php; app/Http/Controllers/Workspaces/SendersController.php; app/Http/Requests/Workspaces/SenderStoreRequest.php; app/Http/Requests/Workspaces/SenderUpdateRequest.php; routes/web.php; app/Providers/AppServiceProvider.php; resources/views/layouts/sidebar/sendersMenuItem.blade.php; resources/views/senders/index.blade.php; resources/views/senders/create.blade.php; resources/views/senders/edit.blade.php; resources/views/senders/_form.blade.php; tests/Feature/Workspaces/SenderControllerTest.php; .github/workflows/ci.yml; .php-cs-fixer.dist.php
  </read_first>
  <action>Run the final TDD refactor/security pass. Keep the resolved normalized field policy deterministic for future Phase 7 capture: trim label/from_name/from_email, lowercase only from_email, preserve From Name casing, reject duplicate normalized `(workspace_id, from_name, from_email)` pairs at application and database boundaries, and document the pair semantics in the model/test if needed. Do not silently introduce provider, reply-to, default-sender, verification, campaign-selection, or auto-capture features. Confirm the migration matches the existing unsigned integer workspace key and works on both MySQL/PostgreSQL conventions. Confirm sidebar fragments preserve existing Manage Users output while showing Senders for every active-workspace member per D-03. Confirm every user-visible sender value is escaped, delete requires the documented confirmation, the HTML/DOM assertion remains alongside manual cancellation verification, and the relation predicate is present on all record operations. Apply PHP-CS-Fixer-compatible cleanup only, commit optional refactor(05-02), and leave vendor untouched.</action>
  <verify>
    <automated>vendor/bin/phpunit</automated>
    <automated>vendor/bin/php-cs-fixer fix --dry-run --diff app/Models/Sender.php app/Models/Workspace.php app/Http/Controllers/Workspaces/SendersController.php app/Http/Requests/Workspaces/SenderStoreRequest.php app/Http/Requests/Workspaces/SenderUpdateRequest.php routes/web.php app/Providers/AppServiceProvider.php tests/Feature/Workspaces/SenderControllerTest.php</automated>
    <automated>git diff --check &amp;&amp; test -z "$(git diff --name-only -- vendor/mettle/sendportal-core)"</automated>
  </verify>
  <acceptance_criteria>
    - Full PHPUnit suite and focused sender tests pass after refactor.
    - All SENDER-01 through SENDER-05 behaviors are covered by feature tests and the UI contract is present in server-rendered views; SENDER-05 includes no cross-workspace sender use and the Phase 6 boundary assertion.
    - MySQL/PostgreSQL-compatible migration types and foreign key/index behavior match existing workspace schema conventions.
    - No production file under vendor/mettle/sendportal-core is modified and no deferred Phase 6/7 behavior is included.
  </acceptance_criteria>
  <done>Phase 5 is complete: shared workspace sender CRUD is secure, validated, navigable, tested, and package-upgrade-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser → edit/delete routes | Untrusted sender ID, form fields, CSRF token, and method override cross the web boundary. |
| Authenticated member → shared workspace library | Any current-workspace member is intentionally allowed to mutate shared records, but only inside that workspace. |
| Host → vendor layout | Host-provided navigation HTML crosses into the package-rendered sidebar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-05 | Elevation of privilege | Edit/update/destroy lookup | high | mitigate | Relation-scoped findOrFail for all actions plus explicit foreign-ID 404 tests. |
| T-05-06 | Tampering | Update/delete forms | high | mitigate | Form Request validation, CSRF, method spoofing, validated fields only, and confirmation before DELETE. |
| T-05-07 | Information disclosure | Error/status behavior | high | mitigate | Foreign records are never globally fetched; return 404 rather than 403 or explanatory response. |
| T-05-08 | Tampering | Migration/model tenancy key | high | mitigate | Database foreign key/index and relation-created workspace_id prevent orphaned or request-selected ownership. |
| T-05-09 | Information disclosure | Sender labels/names/emails in Blade | medium | mitigate | Normal escaped Blade interpolation; no raw output for sender data. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists; existing Composer graph and security controls remain unchanged. |
</threat_model>

<verification>
- `vendor/bin/phpunit tests/Feature/Workspaces/SenderControllerTest.php`
- `vendor/bin/phpunit`
- `vendor/bin/php-cs-fixer fix --dry-run --diff ...` over all changed PHP files
- `php artisan route:list --path=senders`
- `git diff --check` and zero changed paths under `vendor/mettle/sendportal-core`
- Manual end-of-phase UI check: owner and ordinary member can navigate, create, edit, and confirm/cancel delete; validation, empty, populated, overflow, and responsive states match 05-UI-SPEC.md.
</verification>

<success_criteria>
Operators can manage a complete shared sender library per workspace: create, list, edit, and delete label/From Name/From Email records; all members with active workspaces see the navigation; invalid input is rejected; foreign workspace IDs consistently return 404; and the feature remains host-owned with zero vendor edits.
</success_criteria>

<output>
Create `.planning/phases/05-sender-identity-management/05-02-SUMMARY.md` when done, including RED/GREEN/REFACTOR gate commits, full-suite evidence, and manual UI verification notes.
</output>

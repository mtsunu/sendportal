---
phase: 01-constraint-resolution-and-security-control
plan: 12
type: execute
wave: 12
depends_on:
  - 01-11
files_modified:
  - tests/Composer/ComposerPolicyGuardTest.php
autonomous: true
gap_closure: true
requirements:
  - COMP-03
must_haves:
  truths:
    - "Every tracked production PHP program that the sole PHP-token-aware command-shaped program-bearing helper identifies as containing a Composer/guard/evaluator invocation, and that has no record from the bounded direct-launch extractor, emits one source-provenanced unclassified PHP audit record and therefore fails the route audit, including files under app/ and tools/."
    - "The direct PHP extractor remains deliberately limited to its existing literal proc_open, exec, system, passthru, and shell_exec APIs; unknown dispatch mechanisms are rejected by the source-level fallback instead of being evaluated."
    - "Disposable staged app/ and tools/ fixtures for variable-function, popen, and callable dispatch each produce nonempty source-provenanced evidence and nonempty route-audit failures without executing fixture code, while comments, docblocks, ComposerPolicyCommandContract-style reason strings, and arbitrary literal prose produce no records."
    - "Existing scripts/ indirect-dispatch coverage, root Composer-script provenance behavior, guarded CI/README evidence, and manifest/PHAR/lock/runtime boundaries remain unchanged."
  artifacts:
    - path: tests/Composer/ComposerPolicyGuardTest.php
      provides: "A PHP-token-aware, command-shaped, scope-independent tracked-PHP no-record invariant with no whole-source Composer regex, explicit trusted/generated-source exclusions, and staged application/tool dispatch regression coverage."
  key_links:
    - from: "tracked app/ and tools/ PHP source"
      to: "auditRoutes() PHP finalization and routeAuditFailures()"
      via: "the sole PHP-token-aware command-shaped program-bearing helper, followed by source-level unclassified PHP evidence when bounded direct-launch extraction yields no record"
      pattern: "unclassified-php"
    - from: "disposable staged PHP dispatch fixtures"
      to: "auditRoutes()"
      via: "initializeFixtureRepositoryFiles() and Git tracked-file enumeration without PHP execution"
      pattern: "routeAuditFailures"
---

<objective>
Close the verified PHP provenance blind spot in the Composer route audit.

Purpose: A tracked application or tool PHP file must not be able to represent a Composer-bearing indirect dispatch while yielding zero audit evidence.
Output: One dependency-free test/audit source with a marker-gated tracked-PHP fallback and staged app/tools regression coverage.
Decision trace: D-01 preserves the PHP 8.2–8.4 support contract; D-02 preserves the blocking Composer-native security safeguard; D-03 confines this repair to Composer enforcement evidence and preserves Laravel 11, SendPortal Core 3, the manifest, PHAR provenance, lockfile boundary, and runtime behavior.
</objective>

<execution_context>
@/Users/meigire/.codex/gsd-core/workflows/execute-plan.md
@/Users/meigire/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-constraint-resolution-and-security-control/01-CONTEXT.md
@.planning/phases/01-constraint-resolution-and-security-control/01-RESEARCH.md
@.planning/phases/01-constraint-resolution-and-security-control/01-10-SUMMARY.md
@.planning/phases/01-constraint-resolution-and-security-control/01-11-SUMMARY.md
@.planning/phases/01-constraint-resolution-and-security-control/01-REVIEW.md
@.planning/phases/01-constraint-resolution-and-security-control/01-VERIFICATION.md
@tests/Composer/ComposerPolicyGuardTest.php
@composer.json
@bin/composer-policy
@tools/composer/ComposerPolicyCommandContract.php
@tools/composer/composer-2.10.2.phar.sha256
@.github/workflows/ci.yml

<interfaces>
`auditRoutes(string $repositoryRoot): array` enumerates Git-tracked source and feeds the resulting records to `routeAuditFailures()`.

`phpProcessLaunches(string $contents): array` intentionally recognizes only literal `proc_open`, `exec`, `system`, `passthru`, and `shell_exec` calls. It is not a callable/data-flow evaluator.

`routeSourceKind()`, `routeAuditMarker()`, `markerSourceLine()`, and `shellRouteRecord()` provide the bounded provenance seam. The PHP program-bearing decision and its source line must come solely from one token-aware helper over `token_get_all()` output; PHP finalization may not retain a raw whole-source Composer regex. That helper ignores comments/docblocks and recognizes only literal command-shaped Composer/guard/evaluator invocations, such as Composer install/update/audit/validate commands, `@composer`, or PHP invoking `bin/composer-policy`; arbitrary prose and ComposerPolicyCommandContract reason strings are not invocations. Existing `tests/`, `.planning/`, binary/manifest/lock non-source handling, plus vendor and generated-output exclusions, are explicit trust exclusions; ordinary tracked PHP is not a route allowlist.

`initializeFixtureRepositoryFiles()` stages fixture source in a temporary Git root. Its fixtures are text inspected by `auditRoutes()` and must never be executed.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make one staged application PHP indirect dispatch fail closed with source provenance</name>
  <reversibility rating="reversible">This changes only the dependency-free audit harness and its disposable fixtures; it changes no public API, dependency graph, persisted data, or runtime behavior.</reversibility>
  <files>tests/Composer/ComposerPolicyGuardTest.php</files>
  <read_first>01-VERIFICATION.md `gaps`, `Key Link Verification`, and `Behavioral Spot-Checks`; 01-REVIEW.md CR-01; tests/Composer/ComposerPolicyGuardTest.php `routeSourceKind`, `routeAuditMarker`, `markerSourceLine`, `phpProcessLaunches`, `auditRoutes`, `routeAuditFailures`, and the Plan 11 scripts/indirect.php fixture loop; app/Console/Kernel.php, public/index.php, and tools/composer/ComposerPolicyCommandContract.php to preserve their comment/docblock and trusted-source behavior; 01-11-SUMMARY.md; composer.json and bin/composer-policy boundaries.</read_first>
  <behavior>
    - RED: A disposable staged `app/IndirectComposer.php` containing a variable function whose literal command is a Composer install produces no record and no failure before the repair.
    - GREEN: That fixture produces exactly one `unclassified-php` record for its own path, with an unclassified classification, a positive physical line, and segment/source evidence containing the Composer marker; `routeAuditFailures()` is nonempty.
    - Regression: Disposable staged PHP files containing Composer text only in a line comment or docblock, actual ComposerPolicyCommandContract-style reason strings, or arbitrary literal prose produce no records, including representative application entry/source forms; literal command-shaped code strings and process/evaluator invocations remain detectable.
    - The fixture PHP is never included, invoked, or run as a command; staged Git enumeration is the only fixture interaction.
  </behavior>
  <action>Start by adding the staged app fixture and its source-provenance/nonempty-failure assertions, then run the focused route-audit group to demonstrate RED. Keep the fixture an indirect variable-function `system` call so it lies outside the known bounded API list; do not add a general PHP evaluator or expand `phpProcessLaunches()` beyond the five literal APIs already owned by that function.

In `auditRoutes()`, replace the route-allowlist condition on PHP no-record finalization with a tracked-production-PHP boundary driven solely by one PHP-token-aware program-bearing helper: after the lexical extractor has produced no record for a program the helper identifies as command-bearing, emit exactly one `shellRouteRecord()` using the existing `php-program` provenance fields. Replace every PHP-path raw whole-source Composer-bearing regex/fallback used by finalization with that helper and use the helper's matching executable-token line for provenance. Its finite grammar must ignore comments/docblocks and must accept only literal command-shaped invocations: a Composer operation such as install/update/audit/validate, an `@composer` shortcut, a PHP command invoking `bin/composer-policy`, or an evaluator/process expression that carries one of those command forms. It must reject arbitrary prose and literal reason strings, including actual ComposerPolicyCommandContract-style explanatory text, even when they contain the word Composer. Apply it to ordinary tracked PHP, including `app/` and `tools/`, while retaining explicit exclusion of the policy guard, tests, planning artifacts, vendor trees, and tracked generated PHP output. Preserve the existing non-source handling for manifests, lockfiles, PHAR/digest material, and other binary/generated artifacts. Do not add a PHP evaluator, broaden `phpProcessLaunches()`, classify a direct unguarded command as supported, or alter composer-script, scripts/, CI/README, manifest, PHAR, lockfile, or runtime logic. Per D-03, the only implementation file changed is the test/audit harness.</action>
  <verify>
    <automated>php -l tests/Composer/ComposerPolicyGuardTest.php &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php --group=route-audit-fail-closed</automated>
  </verify>
  <done>A staged `app/` variable-function dispatcher is represented by one source-provenanced unclassified record and makes the route audit fail without executing its source; comments, docblocks, actual contract reason strings, and arbitrary literal prose produce no records; every existing focused route-audit regression remains green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the tracked-PHP provenance matrix across app and tools without widening the parser</name>
  <files>tests/Composer/ComposerPolicyGuardTest.php</files>
  <read_first>Task 1 result; 01-VERIFICATION.md remaining gap; tests/Composer/ComposerPolicyGuardTest.php Plan 11 indirect-PHP fixture loop, `initializeFixtureRepositoryFiles`, `trackedFiles`, `auditRoutes`, `routeAuditFailures`, production `--route-audit` assertion, and exclusions in `routeSourceKind`; 01-11-SUMMARY.md validation evidence.</read_first>
  <behavior>
    - RED: Each staged `app/` and `tools/` PHP fixture using a variable-function dispatch, `popen`, or `call_user_func` has no direct-parser record; before the completed boundary each would be eligible to disappear from audit output.
    - GREEN: All six staged command-shaped fixtures have a nonempty record subset for their own path containing source line and invocation-bearing segment evidence, and each produces a nonempty `routeAuditFailures()` result.
    - Regression: Existing `scripts/` callable, variable-function, popen, and marker-only fixtures retain their established one-record failure behavior; comment/docblock-only PHP, actual ComposerPolicyCommandContract-style reason strings, and arbitrary literal prose including artisan/public-index-shaped and trusted-contract-shaped text remain outside the detector; root Composer scripts retain their behavior and the normal production audit prints exactly three guarded CI/README records.
  </behavior>
  <action>Add a table-driven disposable fixture matrix that stages all three indirect command-shaped forms under both `app/` and `tools/`: variable-function invocation, `popen`, and `call_user_func`. Each command literal must use a finite Composer operation form such as install or update. For every one of the six cases, require records for the exact staged path, `unclassified-php` plus unclassified classification, a finite positive source line, and invocation-bearing raw segment/source provenance; require `routeAuditFailures()` to be nonempty. Keep each fixture as source text only and prove no fixture runtime side effect is possible. Add separate staged comment-only, docblock-only, actual ComposerPolicyCommandContract-style reason-string, and arbitrary-prose PHP cases shaped like the repository's artisan/public entry and trusted command-contract sources; require an empty record set for those files while preserving detection for literal command-shaped code strings.

Add regression assertions that the no-record finalizer is not a generic parser expansion: `phpProcessLaunches()` remains bounded to its known direct API set; the new helper is the only PHP program-bearing decision seam; direct literal guarded forms still use `ComposerPolicyCommandContract`; and the Plan 11 scripts/ and root Composer-script cases preserve their behavior. Assert no PHP finalization route uses a raw whole-source Composer regex after the helper is introduced. Retain explicit exclusions for tests, planning, vendor, manifests, lockfiles, PHAR/digest, and generated PHP output, but do not introduce an allowlist that excludes tracked application or tool source. Assert that production `--route-audit` retains exactly three guarded CI/README records and the existing no-root-lockfile/no-vendor boundary. Per D-01 through D-03, do not modify Composer constraints, policy, guard/PHAR provenance, scripts, CI, Laravel/Core integration, or runtime tests.</action>
  <verify>
    <automated>php -l tests/Composer/ComposerPolicyGuardTest.php &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php --group=route-audit-fail-closed &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php --route-audit &amp;&amp; test ! -e composer.lock &amp;&amp; test ! -d vendor</automated>
  </verify>
  <done>All six staged app/tools command-shaped variable-function, popen, and callable-dispatch fixtures fail closed with nonempty, source-provenanced unclassified evidence; comment/docblock-only PHP, actual contract reason strings, and arbitrary prose produce no records; production audit retains exactly three guarded CI/README records; all protected dependency/runtime boundaries remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tracked PHP source → lexical route audit | Untrusted tracked application/tool source can represent a Composer mutation through APIs outside the bounded direct extractor; the sole token-aware helper must distinguish command-shaped invocations from comments, docblocks, contract reason strings, and prose. |
| disposable Git fixture → audit harness | Fixture text must be inspected as tracked source and must not execute PHP, Composer, or a script hook. |
| audit record → pre-install CI gate | Missing or misprovenanced evidence could permit a direct Composer mutation before the guard is invoked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12-01 | Tampering | `auditRoutes()` tracked PHP finalization | high | mitigate | Task 1 replaces the path allowlist and raw PHP finalization regex with one PHP-token-aware command-shaped no-record invariant for ordinary tracked production PHP, retaining explicit trusted/generated exclusions. |
| T-01-12-02 | Elevation | indirect variable-function, callable, and popen dispatch | high | mitigate | Task 2 stages each form under both app/ and tools/ and requires source-provenanced unclassified evidence plus nonempty audit failures without execution. |
| T-01-12-03 | Denial of service | bounded source parsing | low | accept | Preserve existing lexical bounds and direct API allowlist; do not evaluate PHP or implement data-flow interpretation. |
| T-01-12-04 | Tampering | Composer manifest, guard, PHAR, lock, and runtime contract | high | mitigate | Limit modifications to `tests/Composer/ComposerPolicyGuardTest.php`; automated checks retain root lock/vendor absence and existing guarded production evidence. |
| T-01-12-SC | Tampering | package installation | low | accept | This plan installs no dependency and changes no package-manager manifest or lockfile. |
</threat_model>

<verification>
Run the focused route-audit group after each RED/GREEN cycle, then run the full dependency-free guard suite and production `--route-audit`. Confirm root `composer.lock` and `vendor/` remain absent. The fixture assertions must establish record path, line, command-shaped source evidence, unclassified classification, and a nonempty failure list for every app/tools indirect form without fixture execution; they must also establish that comment-only/docblock-only PHP, actual ComposerPolicyCommandContract-style reason strings, and arbitrary prose produce no records, that no raw whole-source PHP Composer regex remains in the finalization seam, and that the production audit retains exactly three guarded CI/README records.
</verification>

<success_criteria>
- A PHP program identified only by the token-aware helper as a command-shaped Composer/guard/evaluator invocation outside the five direct APIs cannot produce zero records merely because it resides in app/ or tools/.
- Six disposable staged app/tools cases covering variable-function, popen, and callable dispatch each produce source-provenanced unclassified evidence and audit failures.
- Comment-only and docblock-only PHP marker text, actual ComposerPolicyCommandContract-style reason strings, and arbitrary prose produce no records; raw whole-source PHP Composer regex finalization is absent; tests/planning/vendor/generated source exclusions remain explicit; the parser remains bounded; scripts/ and Composer-script behavior remains intact.
- The full dependency-free suite and production route audit pass, and the plan leaves manifest, PHAR, lockfile, and runtime boundaries untouched.
</success_criteria>

<output>
Create `.planning/phases/01-constraint-resolution-and-security-control/01-12-SUMMARY.md` when done.
</output>

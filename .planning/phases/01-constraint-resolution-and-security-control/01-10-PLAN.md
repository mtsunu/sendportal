---
phase: 01-constraint-resolution-and-security-control
plan: 10
type: execute
wave: 10
depends_on:
  - 01-09
files_modified:
  - tests/Composer/ComposerPolicyGuardTest.php
autonomous: true
gap_closure: true
requirements:
  - COMP-03
must_haves:
  truths:
    - "Every supported-provenance route candidate containing a Composer, repository-guard, or evaluator marker yields one supported, unsupported, or source-provenanced unclassified record; a marker-bearing candidate cannot silently produce an empty audit result."
    - "Every marker-bearing tracked source outside the approved workflow, shell/Make, Docker, and PHP provenance kinds yields exactly one source-provenanced unclassified record; planning and test material remain excluded from production evidence."
    - "Direct Composer mutations in every reproduced shell, evaluator, PHP CLI, PHP program, workflow-YAML, and Docker equivalence class fail the dependency-free route audit, while guarded literal equivalents are supported through ComposerPolicyCommandContract."
    - "The real tracked CI and README evidence remains exactly the existing guarded production evidence, and CI continues to run the dependency-free route gate before dependency installation."
    - "The closure performs no Composer install, network call, lockfile generation, manifest/policy change, PHAR/provenance change, Laravel/Core change, or application behavior change."
  artifacts:
    - path: "tests/Composer/ComposerPolicyGuardTest.php"
      provides: "A finite normalized route grammar, source-level no-record invariant, provenance-aware audit records, and staged Git regression matrix."
  key_links:
    - from: "workflow YAML, shell scripts, Make recipes, Dockerfiles, and PHP launch calls"
      to: "auditRoutes() and routeAuditFailures()"
      via: "bounded provenance extraction, normalized candidate classification, and one source-level unclassified fallback per marker-bearing candidate"
      pattern: "source-level Composer/evaluator-bearing no-record invariant"
    - from: "guarded staged literals"
      to: "ComposerPolicyCommandContract::decide()"
      via: "the existing shared invocation decision used by direct shell, evaluator, Docker, and PHP route classifiers"
      pattern: "ComposerPolicyCommandContract"
    - from: ".github/workflows/ci.yml"
      to: "tests/Composer/ComposerPolicyGuardTest.php"
      via: "existing full dependency-free suite and --route-audit commands before bin/composer-policy install"
      pattern: "Verify Composer policy routes"
    - from: "unknown tracked source kind"
      to: "auditRoutes()"
      via: "explicit provenance dispatch finalization before any supported-route classification"
      pattern: "unclassified-unknown-source"
---

<objective>
Close the Phase 01 route-audit completeness blocker with one finite, normalized source grammar and a source-level marker-bearing no-record invariant.

Purpose: Ensure supported execution provenance cannot conceal a direct Composer mutation merely by moving it into a shell control form, wrapper, evaluator, YAML spelling, Docker instruction, PHP CLI option, or PHP dynamic launch expression.
Output: A single dependency-free test/audit source with production-quality staged Git fixtures that covers every reproducible class from the route-completeness report, while preserving the existing protected dependency and runtime boundary.
</objective>

<execution_context>
@/Users/meigire/.codex/gsd-core/workflows/execute-plan.md
@/Users/meigire/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-constraint-resolution-and-security-control/01-CONTEXT.md
@.planning/phases/01-constraint-resolution-and-security-control/01-RESEARCH.md
@.planning/phases/01-constraint-resolution-and-security-control/01-07-SUMMARY.md
@.planning/phases/01-constraint-resolution-and-security-control/01-08-SUMMARY.md
@.planning/phases/01-constraint-resolution-and-security-control/01-09-SUMMARY.md
@.planning/phases/01-constraint-resolution-and-security-control/01-VERIFICATION.md
@.planning/phases/01-constraint-resolution-and-security-control/01-REVIEW.md
@.planning/debug/route-audit-completeness.md
@tests/Composer/ComposerPolicyGuardTest.php
@tools/composer/ComposerPolicyCommandContract.php
@bin/composer-policy
@.github/workflows/ci.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one marker-bearing source candidate through normalized provenance to deterministic evidence</name>
  <reversibility rating="costly">The shared finite grammar and source-record shape become the enforcement seam for all tracked dependency routes, so changing their meanings later requires coordinated fixture and audit updates.</reversibility>
  <files>tests/Composer/ComposerPolicyGuardTest.php</files>
  <read_first>.planning/debug/route-audit-completeness.md sections “Required invariant”, “Normalized grammar to support”, and “Precise exclusions”; 01-VERIFICATION.md `gaps`; 01-REVIEW.md CR-01, CR-02, and WR-01; tests/Composer/ComposerPolicyGuardTest.php `workflowCommandScalars`, `normalizedLogicalLines`, `parseInvocation`, `inlinePhpProgram`, `classifyInlinePhpRouteSegment`, `classifyShellRouteSegment`, `auditRoutes`, and existing disposable fixture helpers; tools/composer/ComposerPolicyCommandContract.php.</read_first>
  <behavior>
    - RED 1: In a disposable staged Git workflow, a literal `run: &name` scalar containing wrapped `php -n -r` direct Composer produces one source-provenanced unsupported or unclassified record and a nonempty `routeAuditFailures()` result; the literal guarded equivalent produces supported evidence through the shared command contract.
    - RED 2: The exact alias spelling `run: *name` is handled only when it references a bounded literal run-scalar anchor in the same workflow source; its audit record retains the alias line plus anchor/raw-source provenance.
    - RED 3: A marker-bearing workflow scalar, shell segment, Docker instruction, or PHP launch candidate that cannot be reduced by the accepted grammar yields exactly one deterministic unclassified record instead of disappearing from the record set.
    - RED 4: A disposable staged marker-bearing file outside the approved workflow, shell/Make, Docker, and PHP provenance kinds produces exactly one `unclassified-unknown-source` record with its physical path, line, and raw source, and `routeAuditFailures()` is nonempty.
    - RED 5: Staged planning/test files produce no production audit evidence, while the checked-in workflow and README retain their existing supported records, the existing CI route test remains before `php bin/composer-policy install`, and protected dependency/runtime artifacts remain unchanged.
  </behavior>
  <action>Write the staged RED assertions before modifying the parser. Establish a small explicit route-candidate data shape carrying source kind, physical path, physical line, logical/raw source, decoded scalar when applicable, and deterministic chain/trail context. Add explicit source-kind dispatch at the start of `auditRoutes()`: exclude `.planning/` and `tests/` material from production evidence exactly as today; dispatch only approved workflow, shell/Make, Docker, and PHP provenance kinds to their bounded extractors; and for every other tracked source kind with a Composer executable, repository guard, or evaluator marker, create exactly one `unclassified-unknown-source` record before any supported-route classification. The record must retain the source path, physical line, source kind, raw source, and rejection rationale, and an unknown source may never become supported or be silently skipped. Add a disposable staged unknown-source fixture proving this path and a staged planning/test control proving the production-evidence exclusion.

Add a single source-level finalization helper used by every approved provenance extractor: whenever a candidate contains a Composer executable, repository guard, or evaluator marker, it must return exactly one or more audit records; if the bounded classifier returns none, append one `unclassified` record naming why its grammar did not accept the candidate. Apply this invariant to normal shell segments, inline PHP, tracked PHP launch calls, workflow command scalars, and the Docker instruction candidates introduced by this plan; it must be based on source text/provenance, never on execution.

Implement the first thin vertical path through workflow provenance: accept only a literal workflow `run:` scalar after an optional static anchor declaration and a literal alias reference to that same run scalar. Preserve the physical alias line as the record location and preserve both alias raw text and anchor raw/decoded text as evidence. Feed the recovered command text through the existing normalized shell and shared contract path. A missing, duplicate, dynamic, multi-node, or non-literal anchor/alias carrying a marker becomes one source-provenanced unclassified candidate. This is a bounded spelling rule, not a general YAML parser or YAML anchor resolver.

In the same path, normalize the accepted shell wrappers before inline-PHP detection: leading assignments; `env`, `command`, `sudo`, and `timeout`; then the finite PHP CLI options `-n`, `-d value`, attached `-dvalue`, `-c value`, and `-z value` for `php` or `phpX.Y`. Locate one `-r`/`--run` literal program boundary after that normalization, preserving the outer wrapper trail. Do not execute fixture contents, evaluate values, broaden the Composer command contract, or add dependencies. Per D-01, D-02, and D-03, keep PHP support, native policy, Laravel 11, SendPortal Core, and all dependency artifacts unchanged.</action>
  <verify>
    <automated>php -l tests/Composer/ComposerPolicyGuardTest.php &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php --group=route-audit-fail-closed &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php --route-audit &amp;&amp; php -r '$ci=file_get_contents(".github/workflows/ci.yml"); exit($ci !== false &amp;&amp; preg_match("~php tests/Composer/ComposerPolicyGuardTest\\.php[\\s\\S]*php tests/Composer/ComposerPolicyGuardTest\\.php --route-audit[\\s\\S]*php bin/composer-policy install~", $ci) === 1 ? 0 : 1);' &amp;&amp; test ! -e composer.lock &amp;&amp; test ! -d vendor &amp;&amp; git diff --exit-code -- composer.json bin/composer-policy tools/composer/ComposerPolicyCommandContract.php tools/composer/composer-2.10.2.phar tools/composer/composer-2.10.2.phar.sha256 .github/workflows/ci.yml README.md tests/Composer/ComposerPolicyLivePackagistTest.php</automated>
  </verify>
  <done>A staged anchored workflow path reaches the same shared command decision as an ordinary route, an unknown marker-bearing tracked source emits exactly one source-provenanced rejection, planning/test inputs remain absent from production evidence, and the existing production guarded evidence and pre-install CI ordering are preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the finite grammar matrix across shell, evaluator, PHP, YAML, and Docker audit boundaries</name>
  <files>tests/Composer/ComposerPolicyGuardTest.php</files>
  <read_first>Task 1 implementation and RED fixtures; .planning/debug/route-audit-completeness.md “Normalized grammar to support”, “Required regression fixture matrix”, and “Precise exclusions”; 01-VERIFICATION.md and 01-REVIEW.md; existing parser limits and all route-audit fixture loops in tests/Composer/ComposerPolicyGuardTest.php; ComposerPolicyCommandContract.php; .github/workflows/ci.yml.</read_first>
  <behavior>
    - RED 1: Disposable staged fixtures for all reproduced shell list/control/compound forms, wrapper prefixes, indirect forms, literal evaluators, and malformed or over-bound variants yield nonempty records and nonempty failures for direct, dynamic, or rejected marker-bearing routes; guarded literal routes remain supported through the shared contract.
    - RED 2: `php`, `php8.4`, `-n`, separate/attached `-d`, `-c`, `-z`, `-r`, `--run`, and accepted outer wrappers classify one literal inline program; malformed, extra, unsupported, dynamic, or over-bound argument forms produce source-provenanced unclassified failures.
    - RED 3: In both inline PHP and tracked PHP files, the five supported process-launch APIs route literal strings/arrays through the shell grammar. Composer/evaluator-bearing programs with variable, concatenated, indirect, callable, malformed, unmodeled, or nonliteral launch expressions produce nonempty unclassified evidence even when the individual expression lacks a literal Composer token.
    - RED 4: Workflow inline/block/quoted/anchored/alias scalars and Docker `RUN`, shell-form `CMD`/`ENTRYPOINT`, and JSON-array `CMD`/`ENTRYPOINT` preserve source path, line, source kind, and raw/decoded provenance; direct forms fail and guarded literals remain supported.
    - RED 5: An unknown marker-bearing tracked source is rejected once by the source-kind dispatch before parser-specific classification, while staged planning/test material remains absent from production evidence.
    - RED 6: The production route audit still reports exactly the real guarded CI/README evidence; its failure gate remains before dependency installation, and all protected-artifact checks pass.
  </behavior>
  <action>Expand the Task 1 grammar as one bounded closure, using disposable staged Git roots for every equivalence class so the audit never executes workflow, shell, Docker, or PHP fixture content. Keep the grammar finite and document it in nearby PHPDoc/comments and fixture labels: quote/escape-aware lists joined by `;`, `&amp;&amp;`, `||`, `|`, and `&amp;`; control words `if`/`then`/`elif`/`else`/`fi`, `!`, `do`/`done`, bounded `for`/`while`/`until`/`case` bodies; parenthesized subshells; exact brace groups; POSIX `name() {}` and Bash `function name {}` forms. Support the explicit wrapper list of assignments, `env`, `command`, `sudo`, `timeout`, `exec`, `time`, `nice`, and `stdbuf`, each with a documented option boundary. Every marker-bearing unrecognized compound, unbalanced form, expansion, alias, substitution, indirection, or wrapper layout must become one source-level unclassified record rather than being silently ignored. Retain Task 1's source-kind dispatch as the only route for marker-bearing unknown tracked sources: do not pass them into the approved parser kinds or classify them as supported; continue excluding only planning/test material from production evidence.

Retain only the existing literal `bash`/`sh`/`zsh -c` and single-argument `eval` evaluator recursion, with existing depth, payload-count, token, segment, logical-line, and compound bounds. Normalize wrappers before evaluator and PHP recognition. Treat alternate options, dynamic or concatenated payloads, extra arguments, backticks, `$()`, executable variables, `${SHELL:-sh}`, `xargs`, `find -exec`, `source`/`.` process substitutions, alias expansion, and other expansion-dependent forms as unclassified when their bounded source subtree is marker-bearing; do not emulate a shell.

Make inline PHP and tracked PHP share the same program-level invariant. Tokenize only; recognize the current five launch APIs with one literal first argument or existing literal array form, recurse literal commands through the normalized shell/contract classifier, and append one `unclassified-php` record for each dynamic/unclassifiable launch whenever the enclosing program is Composer/evaluator-bearing. Add the final program-level fallback when such a program produces no child record. Correct the launch-bound regression to use `MAX_ROUTE_INLINE_PHP_LAUNCHES`. Do not evaluate variables, constants, concatenation, includes, callbacks, reflection, callable dispatch, arbitrary process APIs, PHP execution, or arbitrary CLI options.

Add finite Docker provenance extraction for `RUN`, shell-form `CMD`/`ENTRYPOINT`, and JSON-array `CMD`/`ENTRYPOINT`: accept only the listed literal instruction shapes, pass their command words into the same classifier, and preserve Docker path, physical line, instruction source kind, raw instruction, and decoded command. An unsupported marker-bearing Docker instruction becomes one unclassified source record; do not implement Dockerfile expansion, stage semantics, shell selection, mounts, heredocs, or general JSON/YAML parsing. Keep exact source provenance through all recursions and assert direct negative forms have both nonempty record sets and failures. Keep the production audit’s known supported CI/README evidence exact; do not add staged files to the real evidence set.

Finish with the focused route group, full dependency-free suite, production audit, CI ordering assertion, exact D-01/D-02/D-03 manifest-policy assertion, root lockfile/vendor absence checks, and a protected-artifact diff gate. Do not alter `composer.json`, the guard, contract, Composer PHAR/provenance, CI, README, live Packagist test, Laravel/Core integration, application source, or any lockfile; this closure has no network requirement.</action>
  <verify>
    <automated>php -l tests/Composer/ComposerPolicyGuardTest.php &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php --group=route-audit-fail-closed &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php --route-audit &amp;&amp; php -r '$m=json_decode(file_get_contents("composer.json"),true,512,JSON_THROW_ON_ERROR); $p=$m["config"]["policy"]??[]; $a=$p["advisories"]??[]; $ids=array_keys($a["ignore-id"]??[]); sort($ids); $expected=["PKSA-3r5d-mb8f-1qw9","PKSA-m5cs-t1y6-qpcs","PKSA-mdq4-51ck-6kdq"]; sort($expected); exit(($m["require"]["php"]??null)==="^8.2" &amp;&amp; ($m["require"]["laravel/framework"]??null)==="^11.0" &amp;&amp; ($m["require"]["mettle/sendportal-core"]??null)==="^3.0" &amp;&amp; !isset($m["require-dev"]["roave/security-advisories"], $m["config"]["platform"]) &amp;&amp; ($p["ignore-unreachable"]??null)===false &amp;&amp; ($a["block"]??null)===true &amp;&amp; ($a["audit"]??null)==="fail" &amp;&amp; $ids===$expected ? 0 : 1);' &amp;&amp; php -r '$ci=file_get_contents(".github/workflows/ci.yml"); exit($ci !== false &amp;&amp; preg_match("~php tests/Composer/ComposerPolicyGuardTest\\.php[\\s\\S]*php tests/Composer/ComposerPolicyGuardTest\\.php --route-audit[\\s\\S]*php bin/composer-policy install~", $ci) === 1 ? 0 : 1);' &amp;&amp; test ! -e composer.lock &amp;&amp; test ! -d vendor &amp;&amp; git diff --exit-code -- composer.json bin/composer-policy tools/composer/ComposerPolicyCommandContract.php tools/composer/composer-2.10.2.phar tools/composer/composer-2.10.2.phar.sha256 .github/workflows/ci.yml README.md tests/Composer/ComposerPolicyLivePackagistTest.php</automated>
  </verify>
  <done>Every reproduced marker-bearing audit class has a staged direct/dynamic failure fixture and a guarded literal contract fixture where applicable; every accepted provenance node preserves source evidence; finite grammar exclusions fail explicitly; and the production audit preserves its real supported evidence without widening Phase 01 scope.</done>
</task>

</tasks>

## Source Coverage Audit

| Source | ID | Required outcome or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Operators resolve on PHP 8.4 without Composer safeguards being bypassed. | 01-10 | COVERED | The no-record invariant closes a demonstrated route-audit safeguard bypass without modifying the resolver result. |
| REQ | COMP-01 | Clean PHP 8.4 install remains free of platform/security bypasses. | 01-10 | COVERED | Protected-artifact and no-network checks preserve the already-proven install path. |
| REQ | COMP-02 | Published PHP 8.2–8.4 metadata remains accurate. | 01-10 | COVERED | D-01 manifest assertion and protected-artifact gate preserve the contract. |
| REQ | COMP-03 | The Roave replacement retains platform and dependency-security controls. | 01-10 | COVERED | Every supported-provenance marker-bearing route now records guard, direct, or explicit rejection evidence. |
| RESEARCH | Architecture responsibility map | Keep security policy in Composer, locked audit in Phase 2, and runtime integration in Phase 3. | 01-10 | COVERED | Parser-only change leaves manifest, lockfile, and host/Core runtime untouched. |
| RESEARCH | Do-not-hand-roll constraints | Do not invent dependency policy, PHP compatibility, or resolver behavior. | 01-10 | COVERED | No dependency or runtime work; the bounded test recognizer delegates allowed commands to the existing contract. |
| RESEARCH | Phase-boundary constraints | No lockfile, platform emulation, Laravel/Core major change, or unrelated refactor. | 01-10 | COVERED | Enforced by no-lockfile/vendor and protected-artifact verification. |
| CONTEXT | D-01 | Preserve PHP 8.2–8.4 with PHP 8.4 target evidence. | 01-10 | COVERED | Task verification preserves the exact manifest contract. |
| CONTEXT | D-02 | Preserve the native blocking/audit policy and exact time-bounded exceptions. | 01-10 | COVERED | Guarded forms use the shared contract and verification retains policy assertions. |
| CONTEXT | D-03 | Preserve Laravel 11/Core 3 and avoid unrelated application work. | 01-10 | COVERED | Only the dependency-free audit test source changes. |
| VERIFICATION | Gap 1 | PHP CLI options and wrappers must reach inline-PHP classification or explicit failure evidence. | 01-10 Task 1–2 | COVERED | Normalized wrapper/PHP option grammar plus staged direct and guarded fixtures. |
| VERIFICATION | Gap 2 | Composer-bearing dynamic inline programs must fail with evidence. | 01-10 Task 2 | COVERED | Program-level dynamic-launch and zero-child-record fallbacks cover inline and tracked PHP. |
| REVIEW | CR-01 | Wrapper/option PHP routes must not bypass. | 01-10 Task 1–2 | COVERED | Tracer proves a normalized wrapped `php -r` path; matrix expands options and wrappers. |
| REVIEW | CR-02 | Composer-bearing variables feeding process launches must not disappear. | 01-10 Task 2 | COVERED | Enclosing program marker propagates to dynamic/unclassifiable launch evidence. |
| REVIEW | WR-01 | Inline PHP launch bound uses its own constant. | 01-10 Task 2 | COVERED | Regression fixture derives from `MAX_ROUTE_INLINE_PHP_LAUNCHES`. |
| DEBUG | all reproduced audit classes | Shell/control/wrapper/indirection, evaluator/expansion exclusions, PHP CLI/program dynamics, YAML anchors/aliases, and Docker provenance. | 01-10 Task 2 | COVERED | One finite staged equivalence matrix covers each listed class with explicit exclusions. |
| DEBUG | unknown tracked source | A marker-bearing file outside approved provenance must not bypass classification. | 01-10 Task 1–2 | COVERED | Source-kind dispatch emits exactly one `unclassified-unknown-source` fixture record before parser classification. |
| CONTEXT | Deferred ideas | None. | — | EXCLUDED | CONTEXT.md has no deferred implementation item. |
| VERIFICATION | Temporary advisory expiry | Phase 2 lockfile review. | — | EXCLUDED | Explicitly assigned to Phase 2; this plan does not create or change a lockfile. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| tracked workflow, shell, Make, Docker, and PHP source → route-audit candidate | Source-controlled executable text may hide a direct Composer mutation behind a syntactic spelling or dynamic expression. |
| normalized candidate → shared Composer command contract | A guarded literal must be classified by the same allowlist as ordinary dependency routes. |
| bounded parser limits/exclusions → audit output | Unsupported or over-bound marker-bearing text must create a source record rather than disappear or execute. |
| CI workflow → dependency installation | The dependency-free audit must fail CI before the guarded install has an opportunity to resolve packages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-44 | Tampering / elevation of privilege | source-level route candidate classification | critical | mitigate | Enforce exactly one-or-more record result for every Composer/guard/evaluator-bearing candidate from approved provenance, with a deterministic unclassified fallback. |
| T-01-45 | Tampering / elevation of privilege | shell controls, wrappers, evaluators, and indirection | critical | mitigate | Normalize only documented literal forms and reject expansion-dependent, dynamic, malformed, or over-bound forms with outer source provenance. |
| T-01-46 | Tampering / elevation of privilege | PHP CLI and process-launch parser | critical | mitigate | Normalize finite PHP CLI options/wrappers; route literal launches through the shared contract; propagate program markers to dynamic and zero-child fallbacks. |
| T-01-47 | Tampering | YAML and Docker route provenance | high | mitigate | Support only literal `run` anchor/alias spelling and listed Docker instructions; preserve physical source evidence and reject all other marker-bearing forms. |
| T-01-48 | Denial of service | recursive parsing | high | mitigate | Retain limits for line length, tokens, segments, evaluator depth/payloads, compound bodies, and inline launches; turn bound breaches into one deterministic record. |
| T-01-49 | Repudiation | audit evidence | medium | mitigate | Require source kind, path, physical line, raw/decoded payload, logical source, and recursion trail in staged and production records. |
| T-01-50 | Tampering | Phase 01 dependency/runtime boundary | high | mitigate | Retain manifest policy assertion, root artifact absence checks, protected-artifact diff gate, and no-network verification. |
| T-01-51 | Tampering / elevation of privilege | unknown tracked source provenance | critical | mitigate | Exclude only planning/test material from production evidence; route every other marker-bearing unknown source to exactly one `unclassified-unknown-source` record with physical provenance. |
</threat_model>

<verification>
Run the focused route-audit group, complete dependency-free suite, and production `--route-audit`. The staged matrix must assert nonempty records and failures for every direct, dynamic, malformed, excluded, or over-bound marker-bearing candidate, and shared-contract supported evidence for guarded literals. Assert that the disposable unknown-source fixture produces exactly one source-provenanced `unclassified-unknown-source` record and a nonempty failure set, while planning/test controls remain absent from production evidence. Confirm physical provenance fields for YAML aliases and Docker instructions, CI ordering before installation, exact production supported evidence, D-01/D-02/D-03 policy values, protected artifacts, and root lockfile/vendor absence.
</verification>

<success_criteria>
- Every marker-bearing candidate from supported workflow, shell, Make, Docker, and PHP provenance has a finite classification path or one explicit source-level rejection record; no candidate can pass by returning zero audit records.
- Every other marker-bearing tracked source has exactly one source-provenanced `unclassified-unknown-source` record, while `.planning/` and `tests/` inputs remain explicitly excluded from production evidence.
- The staged regression matrix covers all reproducible equivalence classes named in `route-audit-completeness.md` in one bounded implementation batch, with no runtime execution and no general shell/YAML/Docker/PHP/data-flow parser claim.
- Guarded literal variants are recognized through `ComposerPolicyCommandContract`, while direct or excluded variants make `routeAuditFailures()` nonempty.
- CI’s existing dependency-free gate remains ahead of installation, the production audit retains only real guarded evidence, and protected Phase 01 dependency/runtime artifacts remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/01-constraint-resolution-and-security-control/01-10-SUMMARY.md` when done.
</output>

---
phase: 01-constraint-resolution-and-security-control
plan: 07
type: execute
wave: 7
depends_on:
  - 01-06
files_modified:
  - bin/composer-policy
  - tools/composer/ComposerPolicyCommandContract.php
  - tests/Composer/ComposerPolicyGuardTest.php
  - tests/Composer/ComposerPolicyLivePackagistTest.php
autonomous: true
gap_closure: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
must_haves:
  truths:
    - "Caller policy environment variables and machine-global Composer configuration cannot disable, broaden, or replace the exact D-02 advisory policy; guarded children use a private guard-created Composer home and carry credentials only through COMPOSER_AUTH."
    - "The guard accepts only canonical validate, audit, install, and update commands, rejects aliases and every unreviewed or project/configuration-mutating command before the PHAR starts, and reasserts the exact repository policy immediately before install or update."
    - "The tracked-route audit recognizes the verified workflow-YAML, bounded shell-list/wrapper/control, Composer alias, and production PHP process-launch forms; any Composer-bearing supported execution surface that it cannot classify fails closed rather than disappearing as zero records."
    - "Delegated Composer stdout and stderr stream to their matching parent channels before process exit, retain bounded memory, and return the exact child status; only preflight probes use bounded concurrent capture."
    - "The dependency-free test harness concurrently drains both child pipes, so alternating stdout/stderr output larger than pipe capacity completes without deadlock and preserves each channel."
    - "After all repairs, independent empty-home/cache PHP 8.4 checkouts complete a live Packagist-backed resolver dry-run and script-enabled install plus configured audit, while the main checkout retains no composer.lock or vendor tree and the D-01 through D-03 manifest contract remains exact."
  artifacts:
    - path: "bin/composer-policy"
      provides: "Environment-isolated, command-allowlisted Composer guard with exact-policy preflight and streaming-preserving delegation."
    - path: "tools/composer/ComposerPolicyCommandContract.php"
      provides: "Single dependency-free canonical command contract consumed by the guard and route audit."
    - path: "tests/Composer/ComposerPolicyGuardTest.php"
      provides: "Black-box regressions for hostile effective policy, denied commands, route forms, separate streaming channels, and deadlock-safe subprocess capture."
    - path: "tests/Composer/ComposerPolicyLivePackagistTest.php"
      provides: "Repeatable real-PHP-8.4, no-cache Packagist re-verification gate that leaves dependency artifacts only in temporary checkouts."
  key_links:
    - from: "caller environment and COMPOSER_HOME"
      to: "Composer 2.10.2 policy engine"
      via: "complete pinned-release policy override rejection plus a private temporary COMPOSER_HOME passed to every child while COMPOSER_AUTH is explicitly preserved"
      pattern: "Hostile global policy never appears in the guarded child's effective configuration."
    - from: "guard argv and tracked Composer routes"
      to: "tools/composer/ComposerPolicyCommandContract.php"
      via: "the same canonical command/alias decision is used before PHAR execution and during route classification"
      pattern: "Only validate, audit, install, and update are accepted; aliases and all other commands fail before delegation."
    - from: "bin/composer-policy delegated child"
      to: "calling process stdout/stderr"
      via: "direct matching descriptors for delegation, distinct from bounded concurrent preflight capture"
      pattern: "Channel identity, live delivery, bounded memory, and child exit status are preserved."
    - from: "tests/Composer/ComposerPolicyLivePackagistTest.php"
      to: "live Packagist metadata and committed composer.json policy"
      via: "two independent PHP 8.4 temporary checkouts with isolated empty homes/caches, global no-cache mode, guarded commands, direct metadata markers, and configured audit"
      pattern: "Fresh compatibility evidence cannot be replaced by cache, offline, historical, direct-Composer, or repository-lockfile evidence."
---

<objective>
Close the four remaining COMP-03 verification gaps without pulling Phase 2 lockfile ownership or Phase 3 CI-matrix work into Phase 1.

Purpose: Make the repository Composer guard enforce the committed policy against hostile caller state, restrict its mutation surface, fail closed across the repository's supported executable route forms, and preserve Composer's normal process I/O contract.
Output: A shared command contract, hardened guard, dependency-free adversarial regression suite, and mandatory fresh PHP 8.4 Packagist re-verification gate.
Decision trace: D-01 retains PHP 8.2–8.4 with real PHP 8.4 as the live proof target; D-02 retains blocking native policy, `ignore-unreachable: false`, and only the three owner-approved time-bounded advisory IDs; D-03 confines changes to Composer enforcement/tests and preserves Laravel 11, SendPortal Core 3, and the Phase 1 no-lockfile boundary.
</objective>

<execution_context>
@/Users/meigire/.codex/gsd-core/workflows/execute-plan.md
@/Users/meigire/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-constraint-resolution-and-security-control/01-CONTEXT.md
@.planning/phases/01-constraint-resolution-and-security-control/01-RESEARCH.md
@.planning/phases/01-constraint-resolution-and-security-control/01-PATTERNS.md
@.planning/phases/01-constraint-resolution-and-security-control/01-06-SUMMARY.md
@.planning/phases/01-constraint-resolution-and-security-control/01-REVIEW.md
@.planning/phases/01-constraint-resolution-and-security-control/01-VERIFICATION.md
@composer.json
@bin/composer-policy
@tests/Composer/ComposerPolicyGuardTest.php
@tools/composer/composer-2.10.2.phar.sha256
@.github/workflows/ci.yml
@README.md

<interfaces>
Current guard seams to split rather than overload:
- `rejectOverrides(list&lt;string&gt; $arguments): void` runs before PHAR verification and must remain the earliest caller-input rejection boundary.
- `runComposer(list&lt;string&gt; $command, string $repositoryRoot): array{int,string}` currently merges and buffers channels; replace it with a bounded concurrent probe-capture seam and a direct streaming delegation seam.
- `repositoryDistributionPaths(): array{root:string,phar:string,record:string}` remains the canonical checkout/distribution boundary.

Current audit seams to extend with bounded, explicit coverage:
- `normalizedLogicalLines()` and `commandChainSegments()` currently handle line continuations and a limited shell list.
- `parseInvocation()` currently returns only canonical mutation verbs and skips every PHP file.
- `auditRoutes()` is the production entry point behind `php tests/Composer/ComposerPolicyGuardTest.php --route-audit`.

New shared interface:
- `tools/composer/ComposerPolicyCommandContract.php` exposes the canonical allowed commands, known mutation aliases, global-option parsing, and command decision used by both the guard and audit. It is dependency-free and loaded by fixed repository-relative path.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Enforce one exact effective-policy path from hostile caller to allowed resolver</name>
  <files>tests/Composer/ComposerPolicyGuardTest.php, tools/composer/ComposerPolicyCommandContract.php, bin/composer-policy</files>
  <read_first>01-VERIFICATION.md first and second gaps plus effective-policy and guarded-policy-mutation spot-checks; 01-REVIEW.md CR-01 through CR-03; bin/composer-policy in full; tests/Composer/ComposerPolicyGuardTest.php synthetic distribution, environment, override, and marker helpers; composer.json config.policy and constraints; pinned Composer 2.10.2 PolicyConfig.php environment reads identified by the review.</read_first>
  <behavior>
    - RED 1: Each nonempty pinned-release security variable—COMPOSER_POLICY_ADVISORIES_BLOCK, COMPOSER_POLICY_MALWARE_BLOCK, COMPOSER_POLICY_ABANDONED_BLOCK, COMPOSER_SECURITY_BLOCKING_ABANDONED, and COMPOSER_AUDIT_ABANDONED—fails with the fixed override diagnostic before the trusted-distribution marker exists; the existing policy/platform override matrix remains green.
    - RED 2: A caller COMPOSER_HOME whose config contains extra ignore, ignore-id, ignore-severity, platform, repository, and insecure transport settings is not used. The synthetic child records a different empty guard-owned home, none of the hostile config reaches it, and a COMPOSER_AUTH sentinel is preserved without copying auth.json.
    - RED 3: Canonical validate, audit, install, and update pass the command contract, including the current `--no-cache -vvv update --dry-run` shape. Exact probes `config policy.advisories.block false`, `global config policy.advisories.ignore-severity high`, `create-project vendor/package`, `self-update`, `require vendor/package`, `remove vendor/package`, aliases i/u/upgrade, an unknown command, and command-local file/global/project selectors fail before version or policy probes.
    - RED 4: In disposable copied repositories, install/update fail before the PHAR runs when the manifest changes block to false, adds an advisory ID, adds a broad/severity ignore, adds config.platform, restores Roave, or changes PHP/Laravel/Core bounds; the unchanged manifest passes and delegates.
  </behavior>
  <action>Write the four black-box groups first and run the unchanged suite; RED is valid only when marker/status assertions expose the current bypass, not when fixture setup or PHP syntax fails.

Create dependency-free `tools/composer/ComposerPolicyCommandContract.php` with strict types and one explicit contract: canonical commands are `validate`, `audit`, `install`, and `update`; known Composer mutation aliases include `i`, `u`, and `upgrade` but are classified as forbidden; every other command is forbidden. Parse only the global options the repository actually uses, including no-cache, verbosity, ANSI/quiet/profile, no-interaction, no-plugins, and no-scripts forms, with exact arity. Continue to reject working-directory selectors and additionally reject file/global/project-selection forms before command resolution. Do not promise or implement a generic Composer CLI parser. The guard and audit must both call this same contract so an alias or unreviewed guarded command cannot be accepted by one surface and rejected by the other.

Extend the guard's earliest environment check from the exact Composer 2.10.2 policy-source inspection, including COMPOSER_POLICY_ADVISORIES_BLOCK and the other pinned policy/audit overrides named in RED 1. Before PHAR verification, create a unique private mode-0700 temporary Composer home controlled by the guard, register reliable recursive cleanup, and pass an explicit child environment to version, policy-help, and delegated processes. Replace caller COMPOSER_HOME with that directory; preserve only explicit credential transport through the caller's COMPOSER_AUTH value and do not copy global config.json or auth.json. Keep normal isolated cache variables usable by the live test and preserve all existing canonical-root, PHAR digest/version/capability, platform-override, and alternate-directory controls.

Before install or update delegation, parse the canonical repository composer.json and require D-01 through D-03 exactly: PHP `^8.2`, Laravel `^11.0`, Core `^3.0`, no Roave or config.platform/legacy audit section, `policy.ignore-unreachable: false`, advisory block true, audit fail, exactly the three approved D-02 IDs, each complete owner/risk/expiry rationale, and no broad/package/severity ignore or extra policy key. This reassertion runs immediately before the resolver operation after command parsing and before the PHAR starts that operation. Validate/audit remain read-only allowed commands; configuration/global/project-creation/self-update and every unreviewed command remain unavailable through the guard. Per D-03, do not edit composer.json or dependency bounds.</action>
  <verify>
    <automated>php -l tools/composer/ComposerPolicyCommandContract.php &amp;&amp; php -l bin/composer-policy &amp;&amp; php -l tests/Composer/ComposerPolicyGuardTest.php &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php --group=effective-policy-command-contract</automated>
  </verify>
  <acceptance_criteria>The test group proves all pinned security-policy overrides stop before Composer, hostile machine-global settings cannot enter any child, COMPOSER_AUTH remains available through the reviewed channel, only four canonical commands are accepted, every mutation/escape case stops before probes, and install/update cannot start unless the repository manifest still expresses exactly D-01 through D-03. The unchanged guarded resolver path reaches the trusted PHAR at the canonical checkout root.</acceptance_criteria>
  <done>The effective runtime policy and guarded command surface are narrower than caller-controlled state: no environment, global home, alias, subcommand, or project/config mutation can weaken or rewrite the policy before a supported resolver runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail closed for verified workflow, shell, alias, wrapper, and PHP route forms</name>
  <files>tests/Composer/ComposerPolicyGuardTest.php, tools/composer/ComposerPolicyCommandContract.php</files>
  <read_first>01-VERIFICATION.md third gap and route-parser adversarial spot-check; 01-REVIEW.md CR-04; current normalizedLogicalLines, commandChainSegments, commandTokens, parseInvocation, classifyRoute, and auditRoutes functions; the Task 1 command contract; current CI and README guarded invocations.</read_first>
  <behavior>
    - RED 1: Disposable workflow fixtures detect and reject direct mutations in folded and literal run blocks, `- run:` list-item syntax, quoted inline run scalars, and canonical inline run syntax; the existing guarded CI scalar remains supported.
    - RED 2: The same bounded shell parser rejects direct mutations inside `if ...; then ...; fi`, a parenthesized subshell, and `timeout 30 ...`; existing list operators, quote/escape controls, env/command/sudo/PHP wrappers, and sibling-segment isolation remain green.
    - RED 3: Direct Composer aliases i, u, and upgrade are mutation records and fail. Guarded aliases or a guarded command outside Task 1's four-command contract fail as unsupported instead of being approved because they use the wrapper.
    - RED 4: Production PHP is no longer categorically skipped. Disposable tracked PHP fixtures using literal proc_open arrays or exec/system/passthru/shell_exec strings for direct install/update fail, a literal guarded process array is supported, and dynamic Composer-bearing process-launch expressions fail closed as unclassified.
    - RED 5: Any Composer-bearing command in a supported workflow/shell/PHP execution surface that exceeds parser bounds or cannot be confidently classified produces an explicit failure record; it can never yield an empty record set.
  </behavior>
  <action>Add the disposable Git fixtures first and require the current production parser to fail RED by missing the exact record, never by scanning fixture source from the real repository.

Replace line-local YAML handling with a bounded GitHub-workflow command-scalar reader for tracked `.github/workflows/*.yml` and `.yaml` files. It must handle inline `run:`, sequence-item `- run:`, quoted inline values, and indented literal/folded block scalar variants with their chomping indicators. Preserve source line and scalar style in each record. This is deliberately a workflow run-scalar parser, not a claim of general YAML compliance. If a supported workflow contains Composer-like executable text in a run value that this bounded reader cannot assemble, emit an unclassified failure.

Extend the existing bounded shell tokenizer only for the reproduced grammar: command lists, parenthesized subshell grouping, `if`/`then`/`elif`/`else`/`fi` control boundaries, and the timeout wrapper with its numeric duration/options. Reuse structured token parsing for current env, command, sudo, assignment, YAML-run, and PHP prefixes. Recognize Composer's install/update aliases as mutation verbs, then use the shared command contract to reject aliases and any guarded command outside the allowlist. Do not implement expansions, functions, arbitrary redirections, or a general shell AST; when Composer-like executable tokens occur in a supported command scalar outside this grammar, fail closed with the path, line, scalar, and parse reason.

Inspect tracked production PHP rather than skipping the language. Use PHP's built-in tokenizer to locate literal calls to proc_open, exec, system, passthru, and shell_exec and literal Composer command arguments; classify direct/guarded forms through the same invocation and command contract. The exact guard implementation may be recognized through its existing structural audit and fixed path, but all other production PHP process surfaces are inspected. A Composer-bearing dynamic expression at one of these launch APIs is unclassified and failing. Do not claim arbitrary PHP data-flow analysis.

Keep deterministic per-invocation records and the existing production-evidence requirement. Exact fixtures must cover the six verifier reproductions—folded YAML, list-item run, if, subshell, timeout, and aliases—plus literal/direct and dynamic PHP launch cases. Finish by proving the real CI and README records remain guarded and that no disposable fixture path or command is present in production `git ls-files` evidence.</action>
  <verify>
    <automated>php -l tests/Composer/ComposerPolicyGuardTest.php &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php --group=route-audit-fail-closed &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php --route-audit</automated>
  </verify>
  <acceptance_criteria>Every reproduced fail-open form yields an exact invocation or explicit unclassified failure from the production parser. Direct aliases and guarded disallowed commands fail against the shared command contract. Supported production PHP process-launch surfaces are inspected rather than skipped. The implementation remains bounded to documented workflow scalars, the reproduced shell grammar, and named PHP launch APIs; unsupported Composer-bearing execution text fails closed without a broad parser claim.</acceptance_criteria>
  <done>The route audit cannot silently convert any verifier-reproduced workflow, shell, alias, wrapper, or PHP execution form into zero records, while current guarded CI and operator routes remain accepted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve streaming channels, make the harness deadlock-safe, and re-prove PHP 8.4 live</name>
  <precondition>`/Users/meigire/Library/Application Support/Herd/bin/php84` is executable and reports PHP 8.4.x; the checked-in Composer PHAR digest matches its strict record; Packagist egress is available or can be retried through the normal approval path.</precondition>
  <files>bin/composer-policy, tests/Composer/ComposerPolicyGuardTest.php, tests/Composer/ComposerPolicyLivePackagistTest.php</files>
  <read_first>01-VERIFICATION.md fourth gap, fresh-Packagist key link, and escalation gate; 01-REVIEW.md CR-05 and WR-01; current guard runComposer/delegation path; current test runCommand helper; 01-06-SUMMARY.md live evidence; composer.json and PHAR provenance record.</read_first>
  <behavior>
    - RED 1: A synthetic delegated child emits distinct stdout/stderr sentinels, pauses until the parent test observes stdout, then exits with status 37. The guarded caller observes stdout before child exit, receives each sentinel only on its original channel, and receives status 37.
    - RED 2: A synthetic delegated child alternates at least 1 MiB on stdout and 1 MiB on stderr. The wrapper and test helper complete within the bounded harness timeout, preserve exact per-channel byte counts/digests, and do not merge or truncate output.
    - RED 3: Version and policy-help probes that emit on both channels are drained concurrently under a 256 KiB-per-channel cap; cap overflow or probe timeout fails with a fixed preflight diagnostic before delegation.
    - RED 4: A standalone runCommand fixture writes more than platform pipe capacity to stderr before closing stdout, then reverses the order; the test harness returns both exact channels without hanging.
  </behavior>
  <action>Write the channel, live-stream, large-output, preflight-bound, and helper-deadlock cases first. Change the dependency-free test result shape to retain `status`, `stdout`, and `stderr` separately; update existing assertions deliberately rather than concatenating channels.

Split the guard process path. For version and policy-capability preflights, use nonblocking stdout/stderr pipes drained concurrently with stream_select, a deterministic timeout, and a 256 KiB cap on each channel; close/terminate cleanly and fail before delegation on timeout or overflow. For the delegated Composer command, attach child stdin/stdout/stderr directly to the parent's matching descriptors so output streams live without wrapper buffering, channel identity is unchanged, interactive/noninteractive behavior is not altered, and the exact child status is returned. Keep the canonical repository cwd and Task 1's explicit isolated environment on both paths. Do not use a merged descriptor or accumulate delegated output in memory.

Make the test harness itself deadlock-safe with concurrent nonblocking pipe draining, explicit timeout/termination, and a limit safely above the 1 MiB-per-channel regression. The streaming-before-exit case must use a release-file handshake: the child writes/flushed stdout, waits, the parent observes it through the guard, then creates the release file. The large-output assertions compare per-channel lengths and hashes and require exact status 37.

Create dependency-free `tests/Composer/ComposerPolicyLivePackagistTest.php`. It must require PHP 8.4, verify the four-line PHAR provenance/digest, exact D-01 through D-03 manifest policy, guarded CI/README strings, and absence of main-checkout composer.lock/vendor. Build two independent temporary application copies with distinct initially empty COMPOSER_HOME, COMPOSER_CACHE_DIR, and XDG_CACHE_HOME directories. Scrub all Composer policy/platform/cache/network/repository overrides including Task 1's pinned policy variables, preserve optional COMPOSER_AUTH only through the guard, use global `--no-cache`, and invoke only `PHP_BINARY bin/composer-policy`. One copy runs strict validation and verbose update dry-run; the other runs strict validation, a script-enabled verbose install, and configured audit --locked. Require successful direct repo.packagist.org metadata-download markers, no cache/offline fallback, tagged stable Laravel 11 and Core 3, separate stdout/stderr logs, and cleanup of both copies. A sandbox egress failure must trigger the normal escalation/retry path; it cannot be replaced by Plan 06 evidence or local cache. Do not edit CI, composer.json, the PHAR/provenance record, README, or create a repository lockfile.</action>
  <verify>
    <automated>php -l bin/composer-policy &amp;&amp; php -l tests/Composer/ComposerPolicyGuardTest.php &amp;&amp; php -l tests/Composer/ComposerPolicyLivePackagistTest.php &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php --group=process-io &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php --route-audit &amp;&amp; /Users/meigire/Library/Application\ Support/Herd/bin/php84 tests/Composer/ComposerPolicyLivePackagistTest.php</automated>
  </verify>
  <acceptance_criteria>Preflight capture is concurrent and bounded, while delegation is unbuffered and channel-preserving. The live handshake proves output arrives before exit; the 1 MiB alternating case and the helper-only deadlock cases complete with exact channel hashes and exit status. After these fixes, the standalone PHP 8.4 integration gate obtains new direct Packagist metadata, completes resolver/install/audit paths through the isolated and allowlisted guard, preserves stable Laravel/Core and the exact policy, and leaves the main checkout without dependency artifacts.</acceptance_criteria>
  <done>The guard behaves like the Composer process it delegates to, the security suite cannot deadlock while proving that contract, and fresh PHP 8.4 Packagist evidence confirms the hardened path still satisfies COMP-01 through COMP-03.</done>
</task>

</tasks>

## Artifacts this phase produces

- `bin/composer-policy` — isolated effective-policy boundary, explicit command allowlist, exact-policy resolver preflight, bounded concurrent probe capture, and direct separate-channel delegation.
- `tools/composer/ComposerPolicyCommandContract.php` — one canonical allowlist/alias/global-option contract shared by guard enforcement and route auditing.
- `tests/Composer/ComposerPolicyGuardTest.php` — dependency-free RED/GREEN black-box coverage for all four verifier gaps, including hostile homes, mutation commands, exact route fixtures, streaming, channel identity, and deadlock-safe capture.
- `tests/Composer/ComposerPolicyLivePackagistTest.php` — required repeatable fresh PHP 8.4 live-Packagist resolve/install/audit gate.
- `01-07-SUMMARY.md` — exact RED/GREEN evidence, route records, channel/large-output results, and current Packagist-backed PHP 8.4 versions and audit outcome.
- No repository `composer.lock`, `vendor/`, CI matrix/image change, application-source change, Laravel/Core bound change, or advisory-exception expiry decision.

## Source Coverage Audit

| Source | ID | Required outcome or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Operators resolve a secure, accurately declared PHP 8.2–8.4 graph on real PHP 8.4 without bypassing Composer safeguards. | 01-07 | COVERED | Runtime controls close the remaining bypasses; Task 3 repeats live PHP 8.4 evidence. |
| REQ | COMP-01 | Clean PHP 8.4 standard resolution/install without platform ignore or emulation. | 01-07 | COVERED | Task 3 mandates independent empty-cache live Packagist resolve and install through the guard. |
| REQ | COMP-02 | Published PHP constraint accurately covers 8.2, 8.3, and 8.4. | 01-07 | COVERED | Tasks 1 and 3 reassert `^8.2` and reject config.platform or bound mutation. |
| REQ | COMP-03 | Roave conflict is replaced by a non-weakened effective security safeguard. | 01-07 | COVERED | All four verifier gaps are direct effective-control blockers closed by Tasks 1–3. |
| RESEARCH | — | Composer 2.10+ native policy, real PHP 8.4, isolated home, stable Laravel/Core, no bypass, and no Phase 1 lockfile. | 01-07 | COVERED | The pinned PHAR remains fixed; isolation, exact policy, and live evidence are mandatory gates. |
| CONTEXT | D-01 | Preserve PHP 8.2–8.4 with PHP 8.4 as primary target. | 01-07 | COVERED | No manifest edit; PHP 8.4 live re-verification is required. |
| CONTEXT | D-02 | Retain blocking native policy, fail-closed sources, and only three documented temporary advisory IDs. | 01-07 | COVERED | Effective configuration isolation and exact-policy preflight enforce the decision at runtime. |
| CONTEXT | D-03 | Preserve Laravel 11/Core 3 and avoid unrelated work. | 01-07 | COVERED | Only guard/contracts/tests change; lockfile and PHP 8.4 CI matrix remain later-phase work. |
| VERIFICATION | Gap 1 | Policy environment/global configuration isolation. | 01-07 Task 1 | COVERED | Exact pinned variables, hostile homes, explicit COMPOSER_AUTH, and manifest preflight are black-box tested. |
| VERIFICATION | Gap 2 | Command allowlist and policy/project mutation prevention. | 01-07 Task 1 | COVERED | Four-command contract and marker-before-PHAR denials cover every reproduced mutation/escape. |
| VERIFICATION | Gap 3 | Route audit fail-open YAML/shell/alias/wrapper/PHP forms. | 01-07 Task 2 | COVERED | Bounded supported grammars plus unclassified failure cover every verifier reproduction without a generic parser claim. |
| VERIFICATION | Gap 4 | Separate streaming stdout/stderr delegation and deadlock-safe harness. | 01-07 Task 3 | COVERED | Direct descriptors, bounded concurrent probes, live handshake, large alternating output, and helper regressions cover the contract. |
| VERIFICATION | Deferred | Temporary advisory exceptions expire/review at Phase 2. | — | EXCLUDED | Explicitly deferred to Phase 2; this plan preserves but does not re-approve or remove them. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller environment/global Composer home → guard | Untrusted local state can override policy or contribute broad global configuration unless isolated. |
| caller argv → command contract and manifest preflight | Untrusted commands/options can mutate policy, escape the project, or invoke an unreviewed Composer surface. |
| tracked workflow/shell/PHP text → route audit | Valid executable forms can disappear if the bounded parsers fail open or skip a language. |
| guard preflight/delegation → parent process channels | Probe output can deadlock; delegated output can be merged, buffered, truncated, delayed, or assigned the wrong status. |
| test child processes → dependency-free harness | Two full pipes can hang the security proof and turn a regression into an indefinite run. |
| guarded PHP 8.4 process → Packagist | Mutable external dependency/advisory data and network/cache state determine whether the current graph and policy still work. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-27 | Tampering / elevation of privilege | Composer policy environment and global home | critical | mitigate | Reject every pinned 2.10.2 security-policy override, replace COMPOSER_HOME with a private guard-owned empty directory, preserve credentials only through COMPOSER_AUTH, and run hostile-config black-box probes. |
| T-01-28 | Tampering | guarded Composer command/project surface | critical | mitigate | Share a four-command canonical allowlist, reject aliases/unreviewed commands/selectors before PHAR execution, and reassert the exact manifest immediately before install/update. |
| T-01-29 | Tampering / repudiation | route inventory | critical | mitigate | Parse the exact reproduced workflow/shell/PHP surfaces with bounded grammars and fail closed on Composer-bearing supported text that cannot be classified. |
| T-01-30 | Tampering / denial of service | Composer process I/O | high | mitigate | Bound and concurrently drain probes; directly connect delegated stdin/stdout/stderr to matching parent descriptors and return the exact child status. |
| T-01-31 | Denial of service | subprocess test helper | high | mitigate | Concurrent nonblocking pipe drain with timeout and output caps; verify alternating output above pipe capacity with channel hashes. |
| T-01-32 | Tampering / denial of service | live compatibility evidence | high | mitigate | Require two isolated no-cache PHP 8.4 checkouts, direct Packagist metadata markers, stable Laravel/Core, configured audit, no fallback, and no main-checkout dependency artifacts. |
</threat_model>

<verification>
Run PHP lint on all four changed/created PHP files. Run each focused RED/GREEN group, then the complete dependency-free guard suite and production route audit. Require the effective-policy/command markers, exact workflow/shell/PHP fixture records, live-before-exit channel handshake, 1 MiB per-channel hashes, and helper deadlock probes to pass. Finally run the standalone live integration test under explicit Herd PHP 8.4; it must complete fresh guarded Packagist resolution and script-enabled install/audit from separate empty homes/caches and leave the repository free of composer.lock/vendor.
</verification>

<success_criteria>
- COMP-03: Caller environment, global configuration, unreviewed commands, policy/project mutations, tracked route syntax, and process-I/O behavior can no longer weaken or bypass the exact native Composer safeguard.
- COMP-01: Fresh independent PHP 8.4 resolver/install evidence succeeds through the final hardened guard without platform emulation, ignore flags, direct Composer, cache/offline fallback, or repository lockfile creation.
- COMP-02: The manifest still declares `php: ^8.2`, keeps Laravel `^11.0` and Core `^3.0`, and has no config.platform.
- Every verifier reproduction has a deterministic black-box regression; the route audit remains explicitly bounded and fails closed outside its supported grammar.
- Phase 2 retains lockfile/advisory-expiry ownership and Phase 3 retains PHP 8.4 CI/runtime-matrix ownership.
</success_criteria>

<output>
Create `.planning/phases/01-constraint-resolution-and-security-control/01-07-SUMMARY.md` when done.
</output>

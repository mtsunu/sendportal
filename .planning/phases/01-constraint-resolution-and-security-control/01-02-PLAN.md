---
phase: 01-constraint-resolution-and-security-control
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - composer.json
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
must_haves:
  truths:
    - "The published root PHP constraint is `^8.2`, accurately declaring PHP 8.2, 8.3, and 8.4 support under D-01."
    - "Roave is absent only when Composer 2.10+ native advisory blocking and audit-fail policy replace it under D-02."
    - "Flagged assumption A-01: the approved checkpoint path yields a stable Laravel 11 / existing SendPortal Core graph rather than a development branch or an unapproved scope change."
    - "A clean temporary PHP 8.4 installation uses `composer install --prefer-dist --no-interaction` without platform-ignore or policy-bypass controls and is followed by a blocking locked audit in that temporary graph."
  artifacts:
    - path: "composer.json"
      provides: "Accurate PHP support declaration and Composer-native advisory policy without the incompatible Roave metapackage."
    - path: ".planning/phases/01-constraint-resolution-and-security-control/01-02-SUMMARY.md"
      provides: "Exact candidate versions, isolated PHP 8.4 install/audit evidence, and a Phase 2 lockfile handoff."
  key_links:
    - from: "composer.json config.policy.advisories"
      to: "Composer 2.10+ resolver and `composer audit --locked`"
      via: "block=true and audit=fail"
      pattern: "Native policy rejects advisory-affected candidates and the exact temporary graph is audited"
    - from: "composer.json require.php"
      to: "real PHP 8.4 clean install"
      via: "root constraint `^8.2` with no config.platform emulation"
      pattern: "The declared compatibility contract and target runtime agree"
  prohibitions:
    - status: flagged-unverified
      rule: "Do not use Composer policy/platform bypasses, a development branch, a Core fork, a Laravel major upgrade, or unrelated refactoring to obtain the Phase 1 result."
---

<objective>
Apply only the approved manifest policy/constraint change and prove its temporary PHP 8.4 resolution path without creating the repository lockfile.

Purpose: Deliver the accurate, secure Phase 1 Composer contract that Phase 2 can freeze into a reviewed lockfile.
Output: Updated `composer.json` plus isolated PHP 8.4 install and locked-audit evidence; repository `composer.lock` remains absent.
</objective>

<execution_context>
@/Users/meigire/.codex/gsd-core/workflows/execute-plan.md
@/Users/meigire/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-constraint-resolution-and-security-control/01-CONTEXT.md
@.planning/phases/01-constraint-resolution-and-security-control/01-RESEARCH.md
@.planning/phases/01-constraint-resolution-and-security-control/01-PATTERNS.md
@.planning/phases/01-constraint-resolution-and-security-control/01-VALIDATION.md
@.planning/phases/01-constraint-resolution-and-security-control/01-01-SUMMARY.md
@composer.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the approved minimal Composer support and security-policy contract</name>
  <reversibility rating="costly">D-01’s PHP 8.2–8.4 constraint is published Composer metadata used by operators and later PHP CI coverage, so narrowing or reopening it requires coordinated compatibility changes.</reversibility>
  <precondition>01-01-SUMMARY.md records a human-approved path that satisfies D-01, D-02, and D-03 without selecting a development branch or a bypass.</precondition>
  <read_first>composer.json; .planning/phases/01-constraint-resolution-and-security-control/01-01-SUMMARY.md; 01-CONTEXT.md decisions D-01 through D-03; 01-PATTERNS.md section “composer.json”; 01-RESEARCH.md sections “Pattern 1” and “Open Questions”.</read_first>
  <files>composer.json</files>
  <action>Implement only the checkpoint-approved manifest change. Per D-01, replace the redundant root `require.php` expression with `^8.2`, retaining the published PHP 8.2–8.4 contract. Per D-02, remove `roave/security-advisories` from `require-dev` and add `config.policy.advisories` under the existing `config` object with `block: true` and `audit: fail`; preserve `optimize-autoloader`, `preferred-install`, `sort-packages`, and `allow-plugins`. Per D-03, retain `laravel/framework: ^11.0`, `mettle/sendportal-core: ^3.0`, all unrelated bounds, scripts, and host source files. Do not add `config.platform`, legacy `config.audit` advisory keys, advisory-ignore settings, policy/platform overrides, or a dev-branch framework constraint. Change `minimum-stability` only if the checkpoint includes evidence and rationale for that exact change; otherwise leave it intact and let Task 2 reject any development-branch selection.</action>
  <verify>
    <automated>composer validate --strict --no-check-publish &amp;&amp; php -r '$m=json_decode(file_get_contents("composer.json"), true, 512, JSON_THROW_ON_ERROR); exit(($m["require"]["php"] ?? null) === "^8.2" &amp;&amp; !isset($m["require-dev"]["roave/security-advisories"]) &amp;&amp; (($m["config"]["policy"]["advisories"]["block"] ?? null) === true) &amp;&amp; (($m["config"]["policy"]["advisories"]["audit"] ?? null) === "fail") ? 0 : 1);' &amp;&amp; test ! -e composer.lock</automated>
  </verify>
  <acceptance_criteria>`composer.json` is valid; it declares `php: ^8.2`, keeps Laravel 11 and SendPortal Core constraints unchanged, contains native advisory blocking/audit-fail policy, contains no Roave entry, and repository lockfile creation remains deferred.</acceptance_criteria>
  <done>The repository manifest honestly declares the PHP support contract and has a reviewable Composer-native security-control replacement.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the approved graph installs and audits in an isolated real-PHP-8.4 copy</name>
  <read_first>composer.json after Task 1; 01-01-SUMMARY.md checkpoint outcome; 01-RESEARCH.md sections “Pattern 2”, “Validation Architecture”, and “Security Domain”; 01-VALIDATION.md sections “Per-Task Verification Map” and “Wave 0 Requirements”.</read_first>
  <precondition>The approved manifest exists, PHP is 8.4.x, Composer is 2.10.x-or-newer 2.x, and the checkpoint did not authorize a pause or a scope expansion requiring replanning.</precondition>
  <files>composer.json</files>
  <action>Before any Composer command, fail the task if `COMPOSER_POLICY`, `COMPOSER_NO_BLOCKING`, `COMPOSER_NO_SECURITY_BLOCKING`, `COMPOSER_IGNORE_PLATFORM_REQ`, or `COMPOSER_IGNORE_PLATFORM_REQS` is exported. Create a disposable `mktemp -d` copy excluding `.git`, `vendor`, and `composer.lock`; do not generate a lockfile in the repository. In that copy only, set `minimum-stability` to `stable` and keep `prefer-stable: true` before resolving; the repository value remains untouched unless the checkpoint explicitly approved a different, documented change. Discover the available tagged Laravel 11 versions with `composer show laravel/framework --all --format=json`, filter them to `^v?11.[0-9]+.[0-9]+$`, and retain the list as evidence. Then run the exact standard install command `composer install --prefer-dist --no-interaction` on real PHP 8.4, followed by the blocking `composer audit --locked` against the temporary lockfile. Parse that lockfile’s `laravel/framework` entry and require both a matching tagged stable 11.x version and membership in the discovered tagged-version list; reject `11.x-dev` even if the install succeeds. Record the resolved Laravel/Core versions, the exact accepted Laravel tag, PHP/Composer versions, audit result, and command exit statuses in the summary. Per D-01 and D-02, use no `config.platform`, `--ignore-platform-req`, `--ignore-platform-reqs`, `--no-blocking`, `--no-security-blocking`, `COMPOSER_POLICY=0`, advisory-ignore mechanism, or non-blocking audit mode. Per D-03, stop and report rather than change package bounds if the approved path cannot resolve the existing Laravel 11/Core composition.</action>
  <verify>
    <automated>zsh -ceu 'repo="$PWD"; test ! -e "$repo/composer.lock"; if env | grep -Eq "^(COMPOSER_POLICY|COMPOSER_NO_BLOCKING|COMPOSER_NO_SECURITY_BLOCKING|COMPOSER_IGNORE_PLATFORM_REQ|COMPOSER_IGNORE_PLATFORM_REQS)="; then exit 1; fi; php -r "exit(version_compare(PHP_VERSION, \"8.4.0\", \">=\") &amp;&amp; version_compare(PHP_VERSION, \"8.5.0\", \"&lt;\") ? 0 : 1);"; composer --version | grep -E "Composer version 2\\.(10|[1-9][0-9])\\."; composer validate --strict --no-check-publish; tmp="$(mktemp -d)"; trap "rm -rf -- $tmp" EXIT; rsync -a --exclude=.git --exclude=vendor --exclude=composer.lock "$repo/" "$tmp/"; cd "$tmp"; php -r "\$m=json_decode(file_get_contents(\"composer.json\"), true, 512, JSON_THROW_ON_ERROR); \$m[\"minimum-stability\"]=\"stable\"; \$m[\"prefer-stable\"]=true; file_put_contents(\"composer.json\", json_encode(\$m, JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES).PHP_EOL);"; composer validate --strict --no-check-publish; composer show laravel/framework --all --format=json &gt; laravel-framework-versions.json; composer install --prefer-dist --no-interaction &gt; install.log 2&gt;&amp;1; composer audit --locked; php -r "\$catalog=json_decode(file_get_contents(\"laravel-framework-versions.json\"), true, 512, JSON_THROW_ON_ERROR); \$tags=array_map(static fn(\$x)=&gt;ltrim(\$x, \"v\"), array_values(array_filter(\$catalog[\"versions\"] ?? [], static fn(\$x)=&gt;preg_match(\"/^v?11\\.[0-9]+\\.[0-9]+$/\", \$x) === 1))); \$lock=json_decode(file_get_contents(\"composer.lock\"), true, 512, JSON_THROW_ON_ERROR); \$packages=array_merge(\$lock[\"packages\"] ?? [], \$lock[\"packages-dev\"] ?? []); foreach (\$packages as \$package) { if ((\$package[\"name\"] ?? null) === \"laravel/framework\") { \$version=ltrim(\$package[\"version\"], \"v\"); exit(preg_match(\"/^11\\.[0-9]+\\.[0-9]+$/\", \$version) === 1 &amp;&amp; in_array(\$version, \$tags, true) ? 0 : 1); } } exit(1);"; test ! -e "$repo/composer.lock"'</automated>
  </verify>
  <acceptance_criteria>The recorded isolated install completes with the exact standard command, `composer audit --locked` exits successfully in the temporary graph, the installed framework is a tagged stable Laravel 11 release rather than `11.x-dev`, no bypass setting/flag is present, and the repository still lacks `composer.lock`.</acceptance_criteria>
  <done>A secure, real-PHP-8.4 candidate is proven ready for Phase 2’s intentional lockfile creation and review, or execution stops with evidence that the checkpoint outcome cannot satisfy the contract.</done>
</task>

</tasks>

## Artifacts this phase produces

- `composer.json` with the approved PHP declaration and Composer-native advisory policy.
- `01-02-SUMMARY.md` with isolated PHP 8.4 install/audit evidence and exact-version handoff to Phase 2.
- No committed `composer.lock`; CI, runtime validation, and application integration changes remain Phase 3 work.

## Source Coverage Audit

| Source | ID | Required outcome or constraint | Plan | Status | Notes |
|--------|----|--------------------------------|------|--------|-------|
| GOAL | — | Secure, accurately declared PHP 8.2–8.4 graph resolves on real PHP 8.4 without Composer bypasses. | 01-01, 01-02 | COVERED | Tracer proves the blocker; the approved manifest and isolated install prove the route. |
| REQ | COMP-01 | Standard clean PHP 8.4 install has no platform-ignore or emulation route. | 01-02 | COVERED | Disposable real-runtime install is explicit and leaves the repository lockfile untouched. |
| REQ | COMP-02 | Root manifest accurately declares PHP 8.2, 8.3, and 8.4 support. | 01-02 | COVERED | Task 1 requires and verifies `require.php: ^8.2`. |
| REQ | COMP-03 | Roave conflict is replaced without weakening platform checks or the security safeguard. | 01-01, 01-02 | COVERED | Blocking checkpoint precedes native policy replacement and isolated audit. |
| RESEARCH | — | Composer 2.10 native policy, stable-only candidate evidence, no dev branch, isolated solver/install, and Phase 2 lockfile boundary. | 01-01, 01-02 | COVERED | Each research constraint is an action, precondition, acceptance criterion, or phase handoff. |
| CONTEXT | D-01 | Preserve PHP 8.2–8.4, prioritizing PHP 8.4; record the costly metadata contract. | 01-01, 01-02 | COVERED | Cited in tracer, precondition/action, and costly reversibility marker. |
| CONTEXT | D-02 | Replace Roave only with native policy and a blocking locked audit; never use bypasses. | 01-01, 01-02 | COVERED | Cited in checkpoint, manifest action, and isolated audit. |
| CONTEXT | D-03 | Make only compatible manifest changes; preserve Laravel 11 and SendPortal Core. | 01-01, 01-02 | COVERED | Cited in tracer, checkpoint, and manifest/action stop conditions. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository `composer.json` → Composer 2.10+ policy engine | The manifest determines package candidacy and audit behavior for an external dependency ecosystem. |
| isolated installation → Packagist packages/advisories | A real target-runtime resolution consumes mutable package/advisory metadata and may create an ephemeral lockfile. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | Composer command/config policy | high | mitigate | Commit explicit `config.policy.advisories.block=true` and `audit=fail`; reject policy/platform override flags and environment controls. |
| T-01-05 | Tampering | PHP compatibility declaration | high | mitigate | Preserve D-01’s `^8.2` contract and test the candidate on actual PHP 8.4 without `config.platform` emulation. |
| T-01-06 | Tampering | Laravel candidate selection | high | mitigate | Require a tagged stable Laravel 11 installed version and stop on a development branch, Core fork, major upgrade, or unrelated bound change. |
| T-01-07 | Elevation of privilege | removal of Roave safeguard | high | mitigate | Remove the metapackage only in the same manifest edit as the native policy, then run blocking `composer audit --locked` in the isolated graph and hand the exact graph to Phase 2. |
</threat_model>

<verification>
After each manifest change run `composer validate --strict --no-check-publish`. For the approved result, reproduce the disposable clean PHP 8.4 install with the exact standard command, run `composer audit --locked` inside that temporary graph, and confirm the repository contains no generated lockfile.
</verification>

<success_criteria>
- COMP-01: The approved candidate completes a clean standard install on actual PHP 8.4 without platform emulation or ignore flags.
- COMP-02: `composer.json` declares `^8.2`, which includes PHP 8.2, 8.3, and 8.4.
- COMP-03: Roave no longer blocks the graph, native policy is explicit, platform checks remain intact, and the temporary exact graph passes a blocking locked audit.
</success_criteria>

<output>
Create `.planning/phases/01-constraint-resolution-and-security-control/01-02-SUMMARY.md` when done.
</output>

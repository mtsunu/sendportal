---
phase: 01-constraint-resolution-and-security-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - COMP-01
  - COMP-03
must_haves:
  truths:
    - "D-02's resolved internal-only residual-risk exception is limited to PKSA-m5cs-t1y6-qpcs, PKSA-3r5d-mb8f-1qw9, and PKSA-mdq4-51ck-6kdq, each with the owner-accepted-risk and expiry reason."
    - "A real PHP 8.4 / Composer 2.10+ solver result is captured without changing repository dependency files or using policy, platform, global-configuration, or development-branch controls."
    - "The temporary candidate and repository manifest omit config.platform, legacy config.audit advisory settings, package-wide ignores, and severity-wide ignores."
  artifacts:
    - path: ".planning/phases/01-constraint-resolution-and-security-control/01-01-SUMMARY.md"
      provides: "Recorded PHP 8.4 clean-environment solver evidence for the exact D-02 exception."
  key_links:
    - from: "temporary composer.json policy candidate"
      to: "Composer 2.10+ PHP 8.4 solver"
      via: "fresh COMPOSER_HOME and scrubbed COMPOSER/policy/platform override environment"
      pattern: "The exact three-ID policy is evaluated without repository lockfile writes or inherited global Composer configuration."
---

<objective>
Establish clean-environment PHP 8.4 solver evidence for the resolved, narrowly scoped Composer policy before the repository manifest is changed.

Purpose: Preserve D-01 through D-03 while proving the actual Composer/Packagist condition without relying on global Composer state.
Output: A Phase 1 summary containing reproducible clean-environment solver evidence for the exact D-02 exception; no repository dependency or lockfile change.
</objective>

<execution_context>
@/Users/meigire/.codex/gsd-core/workflows/execute-plan.md
@/Users/meigire/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-constraint-resolution-and-security-control/01-CONTEXT.md
@.planning/phases/01-constraint-resolution-and-security-control/01-RESEARCH.md
@.planning/phases/01-constraint-resolution-and-security-control/01-PATTERNS.md
@.planning/phases/01-constraint-resolution-and-security-control/01-VALIDATION.md
@composer.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace the exact D-02 policy through an isolated PHP 8.4 solver</name>
  <read_first>composer.json; .planning/phases/01-constraint-resolution-and-security-control/01-CONTEXT.md decisions D-01 through D-03; .planning/phases/01-constraint-resolution-and-security-control/01-RESEARCH.md sections “Summary”, “Open Questions”, “Pattern 2”, and “Environment Availability”; .planning/phases/01-constraint-resolution-and-security-control/01-PATTERNS.md section “composer.json”.</read_first>
  <precondition>PHP reports an 8.4.x runtime, Composer reports a 2.10.x-or-newer 2.x runtime, and Packagist is reachable for an isolated read-only solver probe.</precondition>
  <files>None — use only an operating-system temporary directory; leave repository composer.json, composer.lock, vendor/, and application files unchanged.</files>
  <action>On real PHP 8.4, inspect the repository manifest with a PHP JSON assertion before copying it. It must omit `config.platform`, legacy `config.audit.advisories`, legacy `config.audit.ignore`, and all package-wide or severity-wide advisory-ignore keys. Create `mktemp -d`, create a new empty `$tmp/composer-home`, and copy the repository into `$tmp/app` excluding `.git`, `vendor`, and `composer.lock`. Invoke every Composer command with `COMPOSER`, `COMPOSER_HOME`, `COMPOSER_POLICY`, `COMPOSER_NO_BLOCKING`, `COMPOSER_NO_SECURITY_BLOCKING`, `COMPOSER_IGNORE_PLATFORM_REQ`, and `COMPOSER_IGNORE_PLATFORM_REQS` explicitly unset, then set only `COMPOSER_HOME="$tmp/composer-home"`; do not read the normal global Composer home. In the copied manifest only, set PHP to `^8.2`, remove Roave, retain Laravel `^11.0` and Core `^3.0`, use stable minimum stability, and add `config.policy.advisories` with `block: true`, `audit: fail`, and an `ignore-id` object containing only the three D-02 IDs. Each mapping value must say the application is internal-only, the project owner accepted the residual risk, and the exception expires at Phase 2 lockfile review or when compatible stable SendPortal Core permits a Laravel upgrade, whichever occurs first. Assert this copied manifest has exactly those three keys, no extra `ignore-id` key, no package/severity ignore, no legacy audit settings, and no platform emulation. With the scrubbed invocation, validate it, discover tagged Laravel 11 releases, run `composer update --dry-run --prefer-dist --no-interaction --no-scripts --no-progress`, and retain the solver/prohibits output. A candidate is valid only if it remains tagged Laravel 11; reject `11.x-dev`. Per D-01 use real PHP rather than `config.platform`; per D-02 use only the resolved exact exception while retaining native blocking/audit policy; per D-03 leave all repository Laravel 11/Core bounds and unrelated files unchanged.</action>
  <verify>
    <automated>zsh -ceu 'repo="$PWD"; test ! -e "$repo/composer.lock"; php -r "\$m=json_decode(file_get_contents(\"composer.json\"),true,512,JSON_THROW_ON_ERROR); \$c=\$m[\"config\"]??[]; exit(isset(\$c[\"platform\"])||isset(\$c[\"audit\"][\"advisories\"])||isset(\$c[\"audit\"][\"ignore\"])||isset(\$c[\"policy\"][\"advisories\"][\"ignore\"])||isset(\$c[\"policy\"][\"advisories\"][\"ignore-package\"])||isset(\$c[\"policy\"][\"advisories\"][\"ignore-severity\"])?1:0);"; php -r "exit(version_compare(PHP_VERSION, \"8.4.0\", \">=\") && version_compare(PHP_VERSION, \"8.5.0\", \"<\") ? 0 : 1);"; composer --version | grep -E "Composer version 2\\.(10|[1-9][0-9])\\."; tmp="$(mktemp -d)"; trap "rm -rf -- $tmp" EXIT; mkdir "$tmp/composer-home"; rsync -a --exclude=.git --exclude=vendor --exclude=composer.lock "$repo/" "$tmp/app/"; cd "$tmp/app"; php -r "\$m=json_decode(file_get_contents(\"composer.json\"),true,512,JSON_THROW_ON_ERROR); \$m[\"require\"][\"php\"]=\"^8.2\"; unset(\$m[\"require-dev\"][\"roave/security-advisories\"]); \$ids=[\"PKSA-m5cs-t1y6-qpcs\",\"PKSA-3r5d-mb8f-1qw9\",\"PKSA-mdq4-51ck-6kdq\"]; \$reason=\"Internal-only application; residual risk accepted by the project owner on 2026-07-22. Expires at Phase 2 lockfile review or when a compatible stable SendPortal Core permits a Laravel upgrade, whichever occurs first.\"; \$m[\"config\"][\"policy\"][\"advisories\"]=[\"block\"=>true,\"audit\"=>\"fail\",\"ignore-id\"=>array_fill_keys(\$ids,\$reason)]; \$m[\"minimum-stability\"]=\"stable\"; \$m[\"prefer-stable\"]=true; file_put_contents(\"composer.json\",json_encode(\$m,JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES).PHP_EOL);"; php -r "\$m=json_decode(file_get_contents(\"composer.json\"),true,512,JSON_THROW_ON_ERROR); \$a=\$m[\"config\"][\"policy\"][\"advisories\"]??[]; \$ids=[\"PKSA-m5cs-t1y6-qpcs\",\"PKSA-3r5d-mb8f-1qw9\",\"PKSA-mdq4-51ck-6kdq\"]; \$actual=array_keys(\$a[\"ignore-id\"]??[]); sort(\$actual); sort(\$ids); exit(\$a===[\"block\"=>true,\"audit\"=>\"fail\",\"ignore-id\"=>array_combine(\$actual,array_map(fn(\$id)=>\$a[\"ignore-id\"][\$id],\$actual))] && \$actual===\$ids && !isset(\$m[\"config\"][\"platform\"],\$m[\"config\"][\"audit\"])?0:1);"; env -u COMPOSER -u COMPOSER_HOME -u COMPOSER_POLICY -u COMPOSER_NO_BLOCKING -u COMPOSER_NO_SECURITY_BLOCKING -u COMPOSER_IGNORE_PLATFORM_REQ -u COMPOSER_IGNORE_PLATFORM_REQS COMPOSER_HOME="$tmp/composer-home" composer validate --strict --no-check-publish; env -u COMPOSER -u COMPOSER_HOME -u COMPOSER_POLICY -u COMPOSER_NO_BLOCKING -u COMPOSER_NO_SECURITY_BLOCKING -u COMPOSER_IGNORE_PLATFORM_REQ -u COMPOSER_IGNORE_PLATFORM_REQS COMPOSER_HOME="$tmp/composer-home" composer update --dry-run --prefer-dist --no-interaction --no-scripts --no-progress > solver.log 2>&1; ! grep -Eq "laravel/framework.*11\\.x-dev" solver.log; test ! -e "$repo/composer.lock"'</automated>
  </verify>
  <acceptance_criteria>The summary names the PHP and Composer versions, fresh-COMPOSER_HOME procedure, the exact three IDs and per-ID owner/risk/expiry reason, stable Laravel tag evidence, solver exit status, and repository non-mutation result.</acceptance_criteria>
  <done>A reproducible end-to-end probe reaches Composer’s resolver on actual PHP 8.4 under the exact resolved policy without accepting or committing a dependency graph.</done>
</task>

</tasks>

## Artifacts this phase produces

- `01-01-SUMMARY.md` with real PHP 8.4 / Composer 2.10+ solver evidence for the exact D-02 exception and fresh-COMPOSER_HOME isolation.
- No `composer.lock`, CI, runtime, application, or documentation artifact in this plan.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository manifest → Composer resolver | Repository-controlled policy and constraints are interpreted against untrusted package metadata and advisory data. |
| operator environment → Composer policy | Environment variables and global configuration can silently change the committed security posture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | Composer solver invocation | high | mitigate | Use a real PHP 8.4 isolated probe with a new COMPOSER_HOME and explicitly unset COMPOSER/policy/platform override variables; record the command and exit status. |
| T-01-02 | Tampering | Laravel candidate selection | high | mitigate | Require tagged Laravel 11 solver evidence and reject a development-branch selection. |
| T-01-03 | Elevation of privilege | advisory safeguard replacement | high | mitigate | Permit only the three D-02 IDs with per-ID internal-only/project-owner/expiry reasons and reject broad, package, severity, legacy, or platform suppression surfaces. |
</threat_model>

<verification>
Run the Task 1 runtime/version/manifest command with its new COMPOSER_HOME and scrubbed invocation. Confirm the repository remains without a generated lockfile before Plan 01-02 applies the already-resolved policy.
</verification>

<success_criteria>
- The real PHP 8.4 probe makes the stable-Laravel/native-policy condition observable without changing repository dependency files.
- The exact approved D-02 exception and no other suppression surface are represented in the temporary candidate.
- No bypass, development branch, broad exception, package-wide ignore, or severity-wide ignore is misrepresented as a compatible graph.
</success_criteria>

<output>
Create `.planning/phases/01-constraint-resolution-and-security-control/01-01-SUMMARY.md` when done.
</output>

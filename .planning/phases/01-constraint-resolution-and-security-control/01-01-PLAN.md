---
phase: 01-constraint-resolution-and-security-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - COMP-01
  - COMP-03
must_haves:
  truths:
    - "Flagged assumption A-01: a human-approved, stable Laravel 11 remediation or an explicitly authorized security/scope outcome exists before any repository manifest accepts a graph."
    - "A real PHP 8.4 / Composer 2.10+ solver result is captured without changing repository dependency files or using bypass controls."
    - "The current Roave-versus-Laravel blocker and the inadmissibility of the development-branch outcome are decision evidence, not a reason to loosen policy."
  artifacts:
    - path: ".planning/phases/01-constraint-resolution-and-security-control/01-01-SUMMARY.md"
      provides: "Recorded PHP 8.4 solver evidence and the blocking checkpoint outcome for the dependent manifest plan."
  key_links:
    - from: "composer.json"
      to: "isolated real-PHP-8.4 Composer solver"
      via: "temporary copy and dry-run resolver command"
      pattern: "Composer 2.10+ native-policy candidate is evaluated without repository lockfile writes"
  prohibitions:
    - status: flagged-unverified
      rule: "No policy-disabling, platform-ignore, development-branch, or broad advisory-exception route may be treated as a valid resolution."
---

<objective>
Establish decision-ready evidence for the PHP 8.4 dependency blocker before any accepted manifest candidate exists.

Purpose: Preserve D-01 through D-03 while proving the actual Composer/Packagist condition on the target runtime.
Output: A Phase 1 summary containing reproducible solver evidence and a recorded blocking human security/scope decision; no repository dependency or lockfile change.
</objective>

<execution_context>
@/Users/meigire/.codex/gsd-core/workflows/execute-plan.md
@/Users/meigire/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-constraint-resolution-and-security-control/01-CONTEXT.md
@.planning/phases/01-constraint-resolution-and-security-control/01-RESEARCH.md
@.planning/phases/01-constraint-resolution-and-security-control/01-PATTERNS.md
@.planning/phases/01-constraint-resolution-and-security-control/01-VALIDATION.md
@composer.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one real PHP 8.4 manifest-to-solver path without accepting a graph</name>
  <read_first>composer.json; .planning/phases/01-constraint-resolution-and-security-control/01-CONTEXT.md; .planning/phases/01-constraint-resolution-and-security-control/01-RESEARCH.md sections “Summary”, “Open Questions”, and “Environment Availability”; .planning/phases/01-constraint-resolution-and-security-control/01-PATTERNS.md section “composer.json”.</read_first>
  <precondition>PHP reports an 8.4.x runtime, Composer reports a 2.10.x-or-newer 2.x runtime, and Packagist is reachable for an isolated read-only solver probe.</precondition>
  <files>None — use only an operating-system temporary directory; leave repository composer.json, composer.lock, vendor/, and application files unchanged.</files>
  <action>On real PHP 8.4, first run `composer validate --strict --no-check-publish` in the repository. Copy the repository into a `mktemp -d` directory excluding `.git`, `vendor`, and any `composer.lock`; only in that copy, prepare the researched candidate: root PHP constraint `^8.2`, remove the Roave development requirement, add `config.policy.advisories.block: true` and `config.policy.advisories.audit: fail`, and force the probe to consider stable releases. Run `composer update --dry-run --prefer-dist --no-interaction --no-scripts --no-progress` there and preserve the exit status plus `composer prohibits laravel/framework <actual-tested-stable-tag> --tree` output in the plan summary. Per D-01, use the real PHP executable rather than `config.platform`; per D-02, do not pass policy or platform overrides; per D-03, do not alter Laravel 11, SendPortal Core, or unrelated application constraints in the repository. Treat an all-stable-Laravel rejection or a development-branch-only candidate as evidence for Task 2, never as an acceptable graph.</action>
  <verify>
    <automated>composer validate --strict --no-check-publish && php -r 'exit(version_compare(PHP_VERSION, "8.4.0", ">=") &amp;&amp; version_compare(PHP_VERSION, "8.5.0", "&lt;") ? 0 : 1);' && composer --version | grep -E 'Composer version 2\.(10|[1-9][0-9])\.' && test ! -e composer.lock</automated>
  </verify>
  <acceptance_criteria>The summary names the PHP and Composer versions, the exact stable Laravel tag tested, the native-policy candidate shape, solver exit status, and whether a stable Laravel 11 graph exists; the repository still has no composer.lock or vendor/ created by this task.</acceptance_criteria>
  <done>A reproducible end-to-end probe reaches Composer’s resolver on actual PHP 8.4 and produces decision evidence without accepting or committing a dependency graph.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Choose the security and scope outcome before a manifest can accept a graph</name>
  <read_first>.planning/phases/01-constraint-resolution-and-security-control/01-RESEARCH.md sections “Summary”, “Open Questions”, and “Known Threat Patterns”; .planning/phases/01-constraint-resolution-and-security-control/01-VALIDATION.md section “Manual-Only Verifications”; the Task 1 solver evidence recorded in 01-01-SUMMARY.md.</read_first>
  <action>Present the Task 1 evidence and obtain a written choice before any repository manifest change. The decision must preserve D-01’s PHP 8.2–8.4 contract, D-02’s native Composer policy plus a blocking `composer audit --locked` gate, and D-03’s Laravel 11/SendPortal Core boundary unless the user explicitly authorizes a scope change. Do not auto-select the development branch, a policy/platform override, or a broad advisory exception.</action>
  <what-built>A real-PHP-8.4 native-policy solver probe showing whether a stable Laravel 11 graph can be accepted today.</what-built>
  <how-to-verify>
    Review the saved solver output and select one outcome: (A) approve a documented stable upstream remediation that passes the native policy; (B) authorize a specific, bounded security/scope decision with its owner, expiry, and follow-up milestone; or (C) pause this phase until a stable upstream remediation exists. A response that selects Laravel `11.x-dev`, a platform or policy bypass, or a broad advisory ignore does not satisfy the checkpoint.
  </how-to-verify>
  <resume-signal>Record the chosen outcome and its rationale in the execution summary, then type “approved” only if it gives Plan 01-02 a compliant, reviewable path.</resume-signal>
  <verify>
    <automated>test -f .planning/phases/01-constraint-resolution-and-security-control/01-01-SUMMARY.md &amp;&amp; composer validate --strict --no-check-publish</automated>
  </verify>
  <acceptance_criteria>The checkpoint outcome is unambiguous: it either authorizes a concrete path that still meets the Phase 1 safety invariants or explicitly pauses/escalates scope; no manifest file is modified before that record exists.</acceptance_criteria>
  <done>The executor is stopped at the genuine security/scope decision rather than silently accepting an insecure or out-of-scope Composer solution.</done>
</task>

</tasks>

## Artifacts this phase produces

- `01-01-SUMMARY.md` with real PHP 8.4 / Composer 2.10+ solver evidence and the human decision outcome.
- No `composer.lock`, CI, runtime, application, or documentation artifact in this plan.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository manifest → Composer resolver | Repository-controlled policy and constraints are interpreted against untrusted package metadata and advisory data. |
| operator command → Composer policy | Command flags and environment variables can override the committed security posture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | Composer solver invocation | high | mitigate | Use a real PHP 8.4 isolated probe and reject platform-ignore or policy-disabling invocation paths; record the exact command and exit status. |
| T-01-02 | Tampering | `minimum-stability` candidate selection | high | mitigate | Require stable Laravel version evidence and route a development-branch-only result to the blocking checkpoint. |
| T-01-03 | Elevation of privilege | advisory safeguard replacement | high | mitigate | Do not remove Roave in the repository until native advisory policy, later locked audit gate, and checkpoint outcome are all present. |
</threat_model>

<verification>
Run the Task 1 runtime/version/manifest command before the checkpoint. Confirm the repository remains without a generated lockfile, then require the recorded checkpoint result before Plan 01-02 may start.
</verification>

<success_criteria>
- The real PHP 8.4 probe makes the stable-Laravel/native-policy condition observable without changing dependency files.
- A human has explicitly accepted a safe path or deliberately paused/escalated the contradiction.
- No bypass, development branch, or broad exception is misrepresented as a compatible graph.
</success_criteria>

<output>
Create `.planning/phases/01-constraint-resolution-and-security-control/01-01-SUMMARY.md` when done.
</output>

---
phase: 01-constraint-resolution-and-security-control
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - bin/composer-policy
  - tests/Composer/ComposerPolicyGuardTest.php
  - .github/workflows/ci.yml
  - README.md
autonomous: true
gap_closure: true
requirements:
  - COMP-03
must_haves:
  truths:
    - "Every repository-supported dependency-resolution path rejects the unsupported COMPOSER_BIN override and proves the PATH-resolved Composer >=2.10.0 policy command under PHP_BINARY before it can delegate to a resolver, so a version-spoofing shim or installed Composer 2.9.5 cannot resolve dependencies without the native security-policy engine."
    - "The compliant repository path invokes Herd Composer 2.10.2 through the verified PHP 8.4 interpreter, and the Composer process reports PHP 8.4.x while resolving the existing PHP ^8.2, Laravel ^11.0, and SendPortal Core ^3.0 graph without a platform, global-policy, or advisory-policy bypass."
    - "A tracked-file audit classifies every dependency-mutating Composer route, including multiline, prefixed, and wrapper forms; each supported route reaches the guard and every non-production occurrence is recorded as explicitly unsupported."
  artifacts:
    - path: "bin/composer-policy"
      provides: "A fail-closed Composer selection, policy-capability, and security-override preflight that executes only the PATH-resolved Composer through the wrapper process's PHP_BINARY before supported dependency commands."
    - path: "tests/Composer/ComposerPolicyGuardTest.php"
      provides: "Dependency-free regression coverage proving interpreter-bound policy-capability checks and delegation, old-version and version-spoofing override rejection, compliant execution, and the tracked-route classification."
    - path: ".github/workflows/ci.yml"
      provides: "CI installation routed through the guarded Composer command."
    - path: "README.md"
      provides: "Documented operator dependency-management route and Composer 2.10.0 floor."
  key_links:
    - from: "bin/composer-policy"
      to: "selected Composer executable"
      via: "COMPOSER_BIN is rejected before execution; an argument-array beginning with PHP_BINARY runs the PATH-resolved Composer's version and policy-capability probes before delegation"
      pattern: "[PHP_BINARY, resolved composer path, 'policy', '--help']"
    - from: "all tracked repository files"
      to: "bin/composer-policy"
      via: "a repository-wide classifier records every Composer mutation form and accepts supported routes only when their command chain terminates in the guarded wrapper"
      pattern: "ComposerPolicyGuardTest.php --route-audit"
---

<objective>
Close the remaining COMP-03 gap by enforcing the Composer 2.10.0 native-policy floor before every repository-supported dependency-resolution command can start.

Purpose: Native `config.policy` is a security control only when the executing Composer implements it; this plan makes the existing policy effective on operator and CI paths without trusting an environment-selected Composer program.
Output: A tested guarded Composer entry point, CI and operator wiring, a complete tracked-route audit, and PHP 8.4 evidence for both rejected Composer 2.9.5/version-spoofing overrides and Herd Composer 2.10.2 invoked by the verified PHP 8.4 interpreter.
Decision trace: D-01 retains PHP 8.2–8.4 with PHP 8.4 as the live proof target; D-02 retains the blocking native policy, `ignore-unreachable: false`, and exactly its three approved advisory exceptions; D-03 adds only the entry-point enforcement needed for that policy and preserves Laravel 11 and SendPortal Core.
</objective>

<execution_context>
@/Users/meigire/.codex/gsd-core/workflows/execute-plan.md
@/Users/meigire/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-constraint-resolution-and-security-control/01-CONTEXT.md
@.planning/phases/01-constraint-resolution-and-security-control/01-RESEARCH.md
@.planning/phases/01-constraint-resolution-and-security-control/01-PATTERNS.md
@.planning/phases/01-constraint-resolution-and-security-control/01-VALIDATION.md
@.planning/phases/01-constraint-resolution-and-security-control/01-VERIFICATION.md
@.planning/phases/01-constraint-resolution-and-security-control/01-REVIEW.md
@.planning/phases/01-constraint-resolution-and-security-control/01-03-SUMMARY.md
@composer.json
@.github/workflows/ci.yml
@README.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Gate one real dependency-resolution path before Composer can run</name>
  <precondition>`/usr/local/bin/composer --version` reports 2.9.5, and `/Users/meigire/Library/Application Support/Herd/bin/php84 /Users/meigire/Library/Application Support/Herd/bin/composer --version -vvv` reports Composer 2.10.2 running with PHP 8.4.x; halt if either independently checkable fact has changed.</precondition>
  <files>bin/composer-policy, tests/Composer/ComposerPolicyGuardTest.php</files>
  <read_first>composer.json policy and script sections; .planning/phases/01-constraint-resolution-and-security-control/01-VERIFICATION.md “Gaps Summary”, “Key Link Verification”, and the Composer-2.9.5 behavioral spot-check; 01-REVIEW.md CR-01; 01-03-SUMMARY.md clean-home environment pattern; .planning/phases/01-constraint-resolution-and-security-control/01-CONTEXT.md decisions D-01 through D-03.</read_first>
  <behavior>
    - Test 1: With a disposable PATH-resolved fake Composer reporting 2.9.5, `bin/composer-policy` exits nonzero with an actionable 2.10.0 requirement message, and the fake operation marker is absent.
    - Test 2: A disposable `COMPOSER_BIN` version-spoofing shim advertises 2.10.2 and contains a delegated operation to the fake 2.9.5 Composer; the wrapper rejects the override before executing either program, so neither shim nor old-operation marker is present.
    - Test 3: With a disposable PATH-resolved fake Composer reporting 2.10.2 and implementing `policy --help`, the wrapper invokes that capability probe and delegates through the test process's PHP_BINARY exactly once, preserves arguments, and the fake operation marker records that interpreter.
    - Test 4: A requested platform or policy override, or the matching override environment variable, exits before the resolved Composer executable is invoked.
  </behavior>
  <action>Create executable `bin/composer-policy` as a strict-types PHP CLI wrapper. It must accept the requested Composer subcommand and arguments, reject any nonempty `COMPOSER_BIN` value as unsupported before probing or executing that value, and resolve the ordinary `composer` command from `PATH` using a non-shell command lookup. Canonicalize the resolved readable file and run it as a PHP script or PHAR through `PHP_BINARY`, never through its shebang. Build every Composer subprocess as an argument array beginning with `PHP_BINARY`, then that resolved Composer path. Before any resolver command exists, run a non-interactive version probe, parse the semantic version defensively, fail unless it is at least 2.10.0, then run `policy --help` through the identical interpreter/path array and fail if the capability probe fails. This capability probe is required in addition to the version floor: do not treat a version-only self-report as proof that the policy engine exists. Before delegation, reject command-line platform/security override options and the matching Composer override environment variables, including alternate-manifest selection; preserve normal `COMPOSER_HOME` support but never read policy requirements from global configuration. Return the delegated exit code. Make diagnostics name the required floor or rejected override and explain that the repository's native policy needs it, without echoing arbitrary executable output into a shell command. Per D-01, do not alter the PHP support declaration. Per D-02, do not mutate, suppress, or broaden the native advisory policy, its three ID map, or `ignore-unreachable: false`. Per D-03, keep Laravel and Core dependency bounds unchanged.

Create `tests/Composer/ComposerPolicyGuardTest.php` as a dependency-free strict-types CLI test that creates temporary fake PHP Composer programs and tests the wrapper before any vendor tree exists. Write the failing old-version/operation-marker case first, then implement the wrapper until it passes. In an isolated `PATH`, cover the compliant fake path and its `policy --help` capability response; create a separate `COMPOSER_BIN` shim that reports 2.10.2 for its version request but forwards a dependency operation to the fake 2.9.5 program, then assert the wrapper rejects the override without invoking the shim or creating either marker. Cover the policy/platform override rejection cases and a marker emitted by the compliant fake program that proves its `PHP_BINARY`/PHP runtime matches the interpreter that started the wrapper. The test must clean up its temporary directory and return nonzero on any failed assertion. Do not depend on PHPUnit or Composer scripts because this guard must remain testable before dependency installation.</action>
  <verify>
    <automated>zsh -ceu 'repo="$PWD"; herd_php="/Users/meigire/Library/Application Support/Herd/bin/php84"; herd_composer="/Users/meigire/Library/Application Support/Herd/bin/composer"; old_composer="/usr/local/bin/composer"; test -x bin/composer-policy; test -x "$herd_php"; test -r "$herd_composer"; test -x "$old_composer"; "$old_composer" --version | grep -F "Composer version 2.9.5"; herd_probe="$("$herd_php" "$herd_composer" --version -vvv 2&gt;&amp;1)"; grep -F "Composer version 2.10.2" &lt;&lt;&lt; "$herd_probe"; grep -E "Running .*PHP 8\.4\." &lt;&lt;&lt; "$herd_probe"; "$herd_php" -r "exit(version_compare(PHP_VERSION, \"8.4.0\", \">=\") &amp;&amp; version_compare(PHP_VERSION, \"8.5.0\", \"&lt;\") ? 0 : 1);"; "$herd_php" -r "\$m=json_decode(file_get_contents(\"composer.json\"),true,512,JSON_THROW_ON_ERROR); \$a=\$m[\"config\"][\"policy\"][\"advisories\"]??[]; \$expected=[\"PKSA-3r5d-mb8f-1qw9\",\"PKSA-m5cs-t1y6-qpcs\",\"PKSA-mdq4-51ck-6kdq\"]; \$actual=array_keys(\$a[\"ignore-id\"]??[]); sort(\$expected); sort(\$actual); \$reasons=true; foreach(\$actual as \$id){\$reason=\$a[\"ignore-id\"][\$id]??\"\"; \$reasons=\$reasons &amp;&amp; is_string(\$reason) &amp;&amp; str_contains(\$reason,\"internal-only\") &amp;&amp; str_contains(\$reason,\"project owner\") &amp;&amp; str_contains(\$reason,\"Phase 2 lockfile review\") &amp;&amp; str_contains(\$reason,\"compatible stable SendPortal Core\");} exit((\$m[\"require\"][\"php\"]??null)===\"^8.2\" &amp;&amp; (\$m[\"require\"][\"laravel/framework\"]??null)===\"^11.0\" &amp;&amp; (\$m[\"require\"][\"mettle/sendportal-core\"]??null)===\"^3.0\" &amp;&amp; !isset(\$m[\"require-dev\"][\"roave/security-advisories\"]) &amp;&amp; (\$m[\"config\"][\"policy\"][\"ignore-unreachable\"]??null)===false &amp;&amp; (\$a[\"block\"]??null)===true &amp;&amp; (\$a[\"audit\"]??null)===\"fail\" &amp;&amp; \$actual===\$expected &amp;&amp; \$reasons ? 0 : 1);"; "$herd_php" tests/Composer/ComposerPolicyGuardTest.php; tmp="$(mktemp -d)"; trap "rm -rf -- \"$tmp\"" EXIT; rsync -a --exclude=.git --exclude=vendor --exclude=composer.lock "$repo/" "$tmp/app/"; cd "$tmp/app"; set +e; env -u COMPOSER_BIN PATH="$(dirname "$old_composer"):$PATH" "$herd_php" bin/composer-policy install --prefer-dist --no-interaction --no-scripts --no-progress &gt; "$tmp/old.log" 2&gt;&amp;1; old_status=$?; set -e; test "$old_status" -ne 0; grep -F "Composer >= 2.10.0 is required" "$tmp/old.log"; test ! -e composer.lock; test ! -d vendor; mkdir "$tmp/composer-home"; env -u COMPOSER_BIN -u COMPOSER -u COMPOSER_POLICY -u COMPOSER_NO_BLOCKING -u COMPOSER_NO_SECURITY_BLOCKING -u COMPOSER_IGNORE_PLATFORM_REQ -u COMPOSER_IGNORE_PLATFORM_REQS PATH="$(dirname "$herd_composer"):$PATH" COMPOSER_HOME="$tmp/composer-home" "$herd_php" bin/composer-policy update --dry-run --prefer-dist --no-interaction --no-scripts --no-progress -vvv &gt; "$tmp/herd.log" 2&gt;&amp;1; grep -E "Running .*PHP 8\.4\." "$tmp/herd.log"; grep -E "laravel/framework.*v?11\." "$tmp/herd.log"; grep -E "mettle/sendportal-core.*v?3\." "$tmp/herd.log"; test ! -e composer.lock'</automated>
  </verify>
  <acceptance_criteria>The wrapper rejects an environment-selected Composer path before that path executes, then makes both a checked Composer version decision and an interpreter-bound native-policy capability decision before a delegated dependency-resolution process exists. The 2.9.5 and spoofing-shim proofs leave their disposable applications without a lockfile or vendor directory, while Herd Composer 2.10.2 resolved from PATH and invoked by Herd PHP 8.4 completes the real clean-home dry-run, its own verbose output reports PHP 8.4.x, and it reports tagged Laravel 11 and SendPortal Core 3 selections. The committed manifest's PHP declaration, Laravel/Core bounds, exact three D-02 exceptions, blocking/audit values, and unreachable-policy setting remain unchanged.</acceptance_criteria>
  <done>A production-quality operator entry point rejects unsupported executable overrides and proves the native-policy toolchain floor and command capability before dependency resolution, with pre-installation regression coverage for old, version-spoofing, compliant, and bypass-attempt paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make CI and the documented operator route consume the guarded entry point</name>
  <files>.github/workflows/ci.yml, README.md, tests/Composer/ComposerPolicyGuardTest.php</files>
  <read_first>bin/composer-policy and tests/Composer/ComposerPolicyGuardTest.php after Task 1; .github/workflows/ci.yml current dependency step; README.md Installation and Requirements sections; .planning/phases/01-constraint-resolution-and-security-control/01-VERIFICATION.md missing-artifact and supported-route findings; AGENTS.md runtime/deployment notes.</read_first>
  <behavior>
    - Test 1: The CI dependency step invokes `php bin/composer-policy` and retains its existing noninteractive, distribution-preferred, scriptless test-install arguments.
    - Test 2: The README identifies Composer 2.10.0 as a security-policy prerequisite and gives operator install/update examples through the guarded command.
    - Test 3: A tracked-file audit inventories every `install`, `update`, `require`, `remove`, and `create-project` Composer operation, including commands split over lines, prefixed with environment/runner syntax, and repository wrapper invocations; it proves each supported route reaches the guard or emits an explicit unsupported classification with file, line, form, and rationale.
  </behavior>
  <action>Replace the CI dependency step's direct Composer invocation with `php bin/composer-policy` followed by its existing test-install arguments, so the wrapper's preflight executes in the job before resolution. Preserve the existing PHP 8.2/8.3 matrix, MySQL/PostgreSQL services, package-test commands, and intentional script setting; do not change CI images or introduce a dependency install by another route. This uses the job's selected PHP interpreter as PHP_BINARY, so Composer cannot fall back to a shebang-selected interpreter.

Add a concise README dependency-management subsection beneath installation requirements. State that Composer 2.10.0 or newer is mandatory because the committed native security policy is enforced by that version, show the repository command for both operator install and intentional update work using the wrapper, and state that `COMPOSER_BIN` is intentionally unsupported so the wrapper cannot be redirected to an arbitrary PHP program. State that a repository-owned deployment install script was not found during the required route audit, so no deployment automation is invented here; an operator following the external installation guide must use the documented wrapper once inside this checkout. Per D-01 retain the PHP 8.2+ requirement. Per D-02 retain the security safeguard as blocking rather than documenting a bypass. Per D-03 keep the documentation scoped to the existing Laravel 11 and Core integration.

Extend `tests/Composer/ComposerPolicyGuardTest.php` with a `--route-audit` mode and run it after editing. The audit must enumerate `git ls-files -z` and inspect the content of every tracked file, not only CI and README. Normalize escaped-newline continuations and whitespace before detecting Composer `install`, `update`, `require`, `remove`, and `create-project` operations. Detect direct binary calls as well as forms preceded by `php`, `env`, `sudo`, shell/YAML command prefixes, assignment prefixes, and named repository wrapper calls; also inspect wrapper implementations for a Composer subprocess. Emit a deterministic record for every match containing path, original line, normalized command form, operation, classification, and rationale. Classify executable CI, operator documentation, container/build, `Makefile`, `scripts/`, and `bin/` routes as supported; accept a supported route only when its complete command chain invokes `bin/composer-policy`, and statically assert that the guard rejects nonempty `COMPOSER_BIN`, builds its version, `policy --help`, and operation subprocesses from arrays whose first element is `PHP_BINARY`, and cannot delegate until all three preflights finish successfully. Classify planning artifacts, historical summaries, tests/fixtures, and research/source-analysis prose as explicitly unsupported with their rationale; do not silently exclude them from the scan. Fail on an unclassified route or a supported route whose chain lacks the guard, and print the discovered record so an additional gap plan can wire it rather than making an unsupported coverage claim.</action>
  <verify>
    <automated>zsh -ceu 'php -r "$ci=file_get_contents(\".github/workflows/ci.yml\"); $readme=file_get_contents(\"README.md\"); $required=\"php bin/composer-policy\"; exit(str_contains($ci,$required) &amp;&amp; str_contains($readme,$required) &amp;&amp; str_contains($readme,\"Composer 2.10.0\") ? 0 : 1);"; php tests/Composer/ComposerPolicyGuardTest.php; php tests/Composer/ComposerPolicyGuardTest.php --route-audit'</automated>
  </verify>
  <acceptance_criteria>CI cannot reach its dependency operation without first executing the same version, executable-selection, and native-policy capability preflight as operators, and the README makes that guarded route, Composer floor, and rejected override contract explicit. The audit's record covers every tracked Composer mutation form, makes each classification reviewable, and accepts supported routes only when they reach an interpreter-bound guard with no executable override; no repository-owned deployment installer is omitted from coverage because none exists. Existing CI topology and the Phase 1 manifest boundary remain intact.</acceptance_criteria>
  <done>Every currently supported repository route for dependency resolution is demonstrably wired to the enforceable Composer 2.10.0 plus native-policy-capability preflight, every other tracked occurrence is expressly classified, and the verifier's CR-01/COMP-03 blocker is closed.</done>
</task>

</tasks>

## Artifacts this phase produces

- `bin/composer-policy` — pre-resolution Composer 2.10.0, native-policy-capability, executable-selection, and security-override guard used by repository-supported dependency commands.
- `tests/Composer/ComposerPolicyGuardTest.php` — dependency-free proof of interpreter-bound Composer execution, old-version and version-spoofing-override rejection, compliant resolution, and the tracked-route classification.
- `.github/workflows/ci.yml` — existing CI installation step routed through the guard.
- `README.md` — operator-facing Composer floor and guarded install/update contract.
- `01-04-SUMMARY.md` — exact Composer 2.9.5 rejection and Herd Composer 2.10.2 invoked by PHP 8.4 success evidence, including the complete tracked-route audit record.

## Source Coverage Audit

| Source | ID | Required outcome or constraint | Plan | Status | Notes |
|--------|----|--------------------------------|------|--------|-------|
| GOAL | — | Secure PHP 8.2–8.4 dependency resolution on real PHP 8.4 without ineffective safeguards. | 01-04 | COVERED | The final missing link is enforced toolchain capability before resolution. |
| REQ | COMP-01 | Clean PHP 8.4 installation avoids platform emulation and ignore options. | 01-02, 01-04 | COVERED | Existing proof remains authoritative; Task 1 rejects bypass input and re-proves a clean PHP 8.4 resolution. |
| REQ | COMP-02 | Published range accurately covers PHP 8.2, 8.3, and 8.4. | 01-02, 01-04 | COVERED | Existing `^8.2` proof remains authoritative; Task 1 guards against unintended manifest changes. |
| REQ | COMP-03 | Roave is replaced by an effective non-weakened safeguard. | 01-04 | COVERED | Composer 2.10.0 plus a real native-policy capability preflight closes the only remaining blocker. |
| RESEARCH | — | Composer 2.10+ is required for native policy; policy/platform overrides and global state cannot become evidence. | 01-04 | COVERED | Wrapper rejects executable overrides, probes native policy and delegates through PHP_BINARY, rejects bypass surfaces, and Herd proof uses a fresh Composer home. |
| CONTEXT | D-01 | Preserve PHP 8.2–8.4 with PHP 8.4 as proof target. | 01-04 | COVERED | Task 1 makes no PHP-range change and uses real PHP 8.4. |
| CONTEXT | D-02 | Retain native blocking/audit policy, the exact three approved IDs, and no bypass. | 01-04 | COVERED | Guard enforces the only Composer line that implements the policy and rejects override inputs. |
| CONTEXT | D-03 | Make only the smallest compatible change; keep Laravel 11 and Core integration. | 01-04 | COVERED | Adds a wrapper and route wiring; leaves dependency bounds and host code untouched. |

## Unresolved Assumptions

- The spec-less edge probe classified COMP-01, COMP-02, and COMP-03 as unclassified/unresolved. This gap plan treats those classifications as explicit assumptions rather than silently converting them into new requirement status: prior verification already supplied COMP-01/COMP-02 evidence, while this plan closes the documented COMP-03 capability gap and rechecks the relevant invariants.
- `/usr/local/bin/composer` remains the installed 2.9.5 control binary, and Herd's Composer remains 2.10.2 when resolved first on PATH and passed explicitly to Herd PHP 8.4.23. The Task 1 precondition verifies both facts before any edit; changed local tool paths or versions require an evidence update, not a substitute binary or bypass.
- The current route inventory contains the CI command and planning/historical occurrences, while the README will become the supported operator route. Task 2 scans every tracked file, classifies all discovered forms, and fails when a supported form cannot be proved to reach the wrapper.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator or CI command → `bin/composer-policy` | An untrusted invocation selects arguments and possibly environment values before dependency resolution starts. |
| `bin/composer-policy` → PATH-resolved Composer executable | The executable must be a supported default route, run with PHP_BINARY, and demonstrate the committed `config.policy` capability before resolution. |
| Composer policy engine → Packagist and policy sources | Dependency and advisory data remains mutable external input; D-02's committed policy must govern its use. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Tampering | Composer 2.9.x, version-spoofing `COMPOSER_BIN` shim, or shebang-selected execution path | high | mitigate | `bin/composer-policy` rejects nonempty `COMPOSER_BIN` before it executes, runs only PATH-resolved Composer through PHP_BINARY, requires both >=2.10.0 and a successful `policy --help` probe before delegation, and tests that a 2.10.2-spoofing shim which forwards to 2.9.5 never runs. |
| T-01-12 | Elevation of privilege / tampering | Wrapper arguments and Composer override environment | high | mitigate | Reject policy/platform bypass options and matching override variables before executing Composer; use an argument-array subprocess rather than a shell-composed command. |
| T-01-13 | Denial of service | required Composer capability unavailable in CI or an operator host | medium | mitigate | Emit an actionable floor diagnostic, document the required Composer line, and wire CI to the same checked route so a missing capability stops securely before resolution. |
| T-01-14 | Repudiation | CI, operator, and wrapper install contract | low | mitigate | The tracked-route audit emits a deterministic record for every Composer mutation occurrence, with support classification and rationale, while the README and Phase summary retain the canonical command and runtime evidence. |
</threat_model>

<verification>
Run the dependency-free guard test first, including the `COMPOSER_BIN` version-spoofing shim case. In a disposable checkout, resolve `/usr/local/bin/composer` 2.9.5 through PATH and prove it exits from the wrapper before any lockfile or vendor directory can appear. Then resolve Herd Composer 2.10.2 through PATH, invoke it through Herd PHP 8.4 with a new `COMPOSER_HOME` and a scrubbed override environment, and run the guarded real dependency dry-run; require Composer's verbose output to identify PHP 8.4.x. Finally run the tracked-file route audit and retain its classifications, confirming every supported command chain reaches the interpreter-bound wrapper and that it rejects executable overrides.
</verification>

<success_criteria>
- COMP-03: A Composer release that lacks the native policy engine, or an environment-selected executable that claims a compliant version, cannot enter any repository-supported dependency-resolution route.
- Herd Composer 2.10.2 explicitly invoked by real PHP 8.4 completes the guarded clean-home resolution and its own process reports PHP 8.4.x, while the existing PHP, Laravel, Core, advisory, and fail-closed-source policy boundaries remain unchanged.
- Every tracked Composer mutation occurrence is classified; CI and documented operators use an interpreter-bound, capability-proven command with no executable override, and no repository-owned deployment installer is omitted from coverage because none exists in the tracked repository.
</success_criteria>

<output>
Create `.planning/phases/01-constraint-resolution-and-security-control/01-04-SUMMARY.md` when done.
</output>

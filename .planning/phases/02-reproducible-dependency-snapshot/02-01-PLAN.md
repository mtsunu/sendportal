---
phase: 02-reproducible-dependency-snapshot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - composer.lock
autonomous: true
requirements:
  - DEPS-01
must_haves:
  truths:
    - "`git check-ignore composer.lock` exits non-zero (the lockfile is no longer git-ignored) — D-04."
    - "`git ls-files composer.lock` prints `composer.lock` after staging (the reviewed lockfile is tracked) — DEPS-01."
    - "`php bin/composer-policy validate --strict --no-interaction` exits 0 and prints `./composer.json is valid` (composer.json ↔ composer.lock synchronized) — DEPS-01."
    - "The regenerated lock's package name→version set is identical to the pre-regeneration Phase-1 candidate (no version drift; normalization only) — D-03."
    - "The regenerated lock's content-hash equals 41abd56c5581800607cc9d3c28862a76 (composer.json is untouched in this plan) — D-03."
    - "`php bin/composer-policy install --prefer-dist --no-interaction` exits 0 consuming the committed lock without a fresh resolution — DEPS-04 mechanism proof."
  artifacts:
    - "composer.lock (regenerated through the guarded path, drift-proven, git-tracked)"
    - ".gitignore (the composer.lock ignore entry removed)"
  key_links:
    - "composer.json ↔ composer.lock synchronization, proven end-to-end by `validate --strict` and a lock-consuming guarded `install`."
  prohibitions:
    - "Do not re-resolve or upgrade any package; the regenerated lock's name→version set must be byte-identical to the Phase-1-approved candidate (freeze only)."
    - "Do not edit composer.json in this plan (the advisory refresh is Plan 02); the guarded update must run with composer.json and the guard constant still in their existing Phase-1 lockstep."
    - "Do not add a PHP 8.4 CI job, permanent validate/platform/audit CI gating, a Laravel boot check, or a PHPUnit matrix (Phase 3 scope)."
    - "Do not use any bypass flag (`--ignore-platform-reqs`, `--no-audit`, `-d`, `--working-dir`); the guard already rejects these."
---

<objective>
Tracer slice for Phase 2: freeze the Phase-1-approved PHP 8.4 dependency graph into a tracked, synchronized `composer.lock` and prove — end to end — that the committed lockfile is the install contract. This is the thinnest path that touches every layer the phase modifies (VCS ignore rules → lock regeneration → strict validation → lock-consuming install), verified on real PHP 8.4 before the advisory refresh and documentation expansion in Plan 02.

Purpose: Establish the reproducible snapshot mechanism and prove no version drift from the Phase-1 resolution, so downstream local/CI/deploy installs consume one validated graph (DEPS-01, enabling DEPS-04).
Output: A regenerated, drift-proven, git-tracked `composer.lock` and a `.gitignore` with the `composer.lock` entry removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reproducible-dependency-snapshot/02-CONTEXT.md
@.planning/phases/02-reproducible-dependency-snapshot/02-RESEARCH.md
@.gitignore
@composer.json
</context>

<artifacts_this_phase_produces>
This plan produces / changes:
- `composer.lock` — regenerated through the guarded path, drift-proven against the Phase-1 candidate, and git-tracked for the first time.
- `.gitignore` — the `composer.lock` ignore entry (formerly line 19) removed.

Downstream (Plan 02, listed here for the source-grounding pass to treat as newly-edited, not drift): edited `composer.json` `config.policy.advisories.ignore-id` reasons + forward expiry; the `$rationale` constant in `bin/composer-policy`; README "Dependency management" / "Lockfile review" additions; the captured verification evidence.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Freeze, track, and validate the lockfile end-to-end</name>
  <files>.gitignore, composer.lock</files>
  <read_first>
    - `.gitignore` (the `composer.lock` ignore entry to remove — D-04)
    - `composer.json` (unchanged in this plan; confirm the existing `config.policy` block and the content-hash anchor)
    - `.planning/phases/02-reproducible-dependency-snapshot/02-RESEARCH.md` § "Pattern 2: Prove lockfile provenance / zero drift" and § "Runtime State Inventory"
    - `.planning/phases/02-reproducible-dependency-snapshot/02-CONTEXT.md` decisions D-03, D-04
  </read_first>
  <action>
    Wire the one production path from VCS ignore rules through lock regeneration, strict validation, and a lock-consuming install, on real PHP 8.4.23 with Composer 2.10.2.

    1. Preserve the current on-disk `composer.lock` as a drift baseline: copy it to a scratch path outside the repo (the session scratchpad) before regenerating — this is the Phase-1 candidate to diff against.
    2. Remove the `composer.lock` ignore entry from `.gitignore` (D-04). Leave every other ignore line untouched.
    3. Regenerate/normalize the lock through the guarded path only: `php bin/composer-policy update --prefer-dist --no-interaction` (D-03). Do NOT edit `composer.json` — the guarded update runs the guard's manifest-policy check, which passes only because `composer.json` and the guard's `$rationale` constant remain in their existing Phase-1 lockstep.
    4. Prove zero drift (Pattern 2): the name→version set of the regenerated lock must equal the scratch baseline (diff of the extracted `"name"`/`"version"` lines is empty), and the lock `content-hash` must remain `41abd56c5581800607cc9d3c28862a76` because `composer.json` is untouched. A changed name→version pair is re-resolution drift — stop and investigate against Phase-1 evidence before proceeding; do not commit a drifted snapshot.
    5. Stage the validated lock with `git add composer.lock`.

    Do not re-resolve or upgrade any package (regeneration is normalization only). Do not use any bypass flag. Do not touch `.github/workflows/ci.yml` or add any CI job (Phase 3).
  </action>
  <reversibility rating="costly">The committed lock is the reproducibility contract every downstream install consumes (D-03); a drifted snapshot propagates to CI and deployment, so the zero-drift proof gates the commit.</reversibility>
  <verify>
    <automated>php bin/composer-policy validate --strict --no-interaction && php bin/composer-policy install --prefer-dist --no-interaction && git ls-files composer.lock | grep -qx composer.lock</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore composer.lock` exits non-zero (the lockfile is no longer ignored).
    - `php bin/composer-policy validate --strict --no-interaction` exits 0 and prints `./composer.json is valid`.
    - The name→version diff between the scratch baseline and the regenerated `composer.lock` is empty (zero version drift).
    - `grep '"content-hash"' composer.lock` shows `41abd56c5581800607cc9d3c28862a76`.
    - `php bin/composer-policy install --prefer-dist --no-interaction` exits 0 and consumes the lock (no "Updating dependencies", no lock rewrite).
    - `git ls-files composer.lock` prints `composer.lock`.
  </acceptance_criteria>
  <done>The lockfile is regenerated through the guard, proven drift-free and json↔lock-synchronized, no longer git-ignored, consumed by a guarded `install`, and staged for commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → VCS commit | An untrusted/unvalidated lock could be committed and then propagate to every downstream install. |
| caller → Composer phar | The guard mediates all mutation/verification and verifies the tracked phar by SHA-256 before use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | committed `composer.lock` | high | mitigate | D-03 zero-drift proof (name→version diff empty) + `validate --strict` EXIT 0 gate the commit; a drifted lock halts the task. |
| T-02-02 | Elevation | guarded `update`/`install` | medium | mitigate | Route only through `bin/composer-policy`; no bypass flags; guard rejects `--ignore-platform-reqs`/`--no-audit`/`-d`/`--working-dir`. |
| T-02-SC | Tampering | tracked Composer phar | low | accept | Guard verifies `tools/composer/composer-2.10.2.phar` by SHA-256 (`verifiedComposerPath`); no new packages introduced this phase. |
</threat_model>

<verification>
Run on real PHP 8.4.23 + Composer 2.10.2:
- `php bin/composer-policy validate --strict --no-interaction` → `./composer.json is valid`, EXIT 0.
- `php bin/composer-policy install --prefer-dist --no-interaction` → EXIT 0, installs from lock (no re-resolution).
- `git check-ignore composer.lock` → EXIT non-zero; `git ls-files composer.lock` → `composer.lock`.
- name→version diff (scratch baseline vs regenerated lock) → empty.
</verification>

<success_criteria>
The regenerated `composer.lock` is drift-free versus the Phase-1 candidate, synchronized with `composer.json` under `validate --strict`, no longer git-ignored, consumed by a guarded `install`, and tracked in git.
</success_criteria>

<output>
Create `.planning/phases/02-reproducible-dependency-snapshot/02-01-SUMMARY.md` when done, recording the drift-diff result, the content-hash, and the `validate --strict` / `install` EXIT codes.
</output>

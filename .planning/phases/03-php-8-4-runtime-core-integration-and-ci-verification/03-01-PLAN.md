---
phase: 03-php-8-4-runtime-core-integration-and-ci-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - RUNTIME-01
  - RUNTIME-02
  - RUNTIME-03
  - RUNTIME-04
  - CI-01
  - CI-02
must_haves:
  truths:
    - "Pushing a commit runs a `kirschbaumdevelopment/laravel-test-runner:8.4` matrix job in `.github/workflows/ci.yml` alongside the retained `:8.2` and `:8.3` jobs, with every step running identically per matrix entry — CI-01, D-01, D-02."
    - "On every matrix version, the install step runs with Composer scripts enabled, completing `artisan package:discover` via `composer.json`'s `post-autoload-dump` hook — RUNTIME-01, D-04."
    - "On every matrix version, `php artisan about` exits 0, proving a safe Laravel boot — RUNTIME-01, D-05."
    - "On every matrix version, `php artisan route:list` lists `sendportal.dashboard`, proving SendPortal Core's route-registration integration boots unchanged, without asserting any request-time behavior — RUNTIME-04, D-05."
    - "On every matrix version, the existing MySQL PHPUnit step (`DB_CONNECTION=mysql`) and PostgreSQL PHPUnit step (`DB_CONNECTION=pgsql`) remain present, unmodified, and still gate the job — RUNTIME-02, RUNTIME-03, D-06."
    - "Each of the five CI-02 failure surfaces (Composer metadata, platform requirements, dependency audit, Laravel/Core boot, and either database-engine PHPUnit run) is its own independent CI step whose nonzero exit fails the job via GitHub Actions' default fail-closed step/pipefail semantics — CI-02, D-03."
    - "`check-platform-reqs` runs directly against the repository's vendored, checksum-verified Composer distribution (`tools/composer/composer-2.10.2.phar`), not through `bin/composer-policy` and not the container's unpinned system Composer, and the guard's four-command canonical set (`validate`/`audit`/`install`/`update`) is left unchanged — D-03, D-04 discretion."
    - "CI trigger remains `on: push` and the MySQL/PostgreSQL service image versions are unchanged — D-06, D-07."
  artifacts:
    - ".github/workflows/ci.yml — extended in place: `:8.4` matrix entry, script-enabled install, four new gate steps, unchanged MySQL/Postgres PHPUnit steps and `on: push` trigger."
  key_links:
    - "`strategy.matrix.container` list ↔ the new `:8.4` image tag ↔ every step in the job body running once per matrix entry (D-02)."
    - "Script-enabled `install` step ↔ `composer.json` `scripts.post-autoload-dump` ↔ `artisan package:discover` (RUNTIME-01 discovery proof)."
    - "`artisan route:list` output ↔ `Sendportal::webRoutes()` registration in `mettle/sendportal-core` ↔ the `sendportal.dashboard` route name (RUNTIME-04 proof target)."
    - "`check-platform-reqs --lock` / `audit --locked` / `validate --strict` ↔ the already-committed `composer.lock` / `composer.json` (no re-resolution; Phase 2's install-consumes-lock contract preserved)."
  prohibitions:
    - "Must not add error-suppression syntax to any new or existing CI step — no step may swallow a nonzero exit."
    - "Must not add a platform-check or audit bypass flag anywhere in `.github/workflows/ci.yml`, and must not widen `bin/composer-policy`'s four-command canonical set."
    - "Must not pull forward the deferred HARD-01 evidence-summary artifact or the HARD-02 SendPortal Core behavior smoke test."
---

<flagged_assumptions>
## Spec-less Edge Probe — Flagged Assumptions

No phase SPEC.md exists for this phase, so the deterministic edge-probe fallback ran instead of a SPEC `## Edge Coverage` section. It classified all six phase requirement IDs as `unclassified` → `unresolved`, because RUNTIME-01..04 / CI-01 / CI-02 describe CI-verification outcomes, not input-boundary edges the probe's heuristics are built to score. Per the fallback protocol, none of the six are silently dropped; each is recorded here as an explicit flagged assumption and is concretely resolved — not by an input-validation edge case, but by a specific `must_haves.truths` entry and CI step verified in this plan:

- **RUNTIME-01** (unclassified/unresolved) → resolved by: script-enabled install + `php artisan about` boot check (Task 1; `must_haves.truths` row 2-3).
- **RUNTIME-02** (unclassified/unresolved) → resolved by: existing MySQL PHPUnit step, unchanged, now running under the `:8.4` matrix entry (Task 1; `must_haves.truths` row 5).
- **RUNTIME-03** (unclassified/unresolved) → resolved by: existing PostgreSQL PHPUnit step, unchanged, now running under the `:8.4` matrix entry (Task 1; `must_haves.truths` row 5).
- **RUNTIME-04** (unclassified/unresolved) → resolved by: `php artisan route:list` grepped for `sendportal.dashboard` (Task 1; `must_haves.truths` row 4).
- **CI-01** (unclassified/unresolved) → resolved by: `:8.4` matrix entry added and retained alongside `:8.2`/`:8.3` (Task 1; `must_haves.truths` row 1).
- **CI-02** (unclassified/unresolved) → resolved by: five independent gate steps (Tasks 1-2), each failing the job on nonzero exit via GitHub Actions' default fail-closed semantics, with no error-suppression syntax anywhere in the file (Task 2 comprehensive verify).

The in-prompt prohibition-recall pass (no SPEC `## Prohibitions` section either) over-produced candidates per requirement and kept only the three genuinely values/safety items above under `must_haves.prohibitions`; routine-engineering candidates (e.g., "must format the YAML correctly") and canon-security candidates (e.g., generic "must not introduce a supply-chain vulnerability" — already owned by the existing `config.policy` advisory block) were dropped.
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

This phase creates **no new files** and **no new PHP symbols** — no classes, functions, CLI flags, migrations, or config keys are authored. The entire deliverable is an in-place edit to one existing file:

- `.github/workflows/ci.yml` — modified in place:
  - Matrix `container` array: one new string-literal entry, `"kirschbaumdevelopment/laravel-test-runner:8.4"`.
  - One new step: `Verify Composer manifest`.
  - One modified step: `Install composer dependencies` (a flag removed from its existing `run:` value — no new step).
  - One new step: `Check platform requirements`.
  - One new step: `Audit dependencies`.
  - One new step: `Verify Laravel boot and SendPortal Core route registration`.
  - Unchanged: `Verify Composer policy routes` (guard self-test), `Run Testsuite against MySQL`, `Run Testsuite against Postgres`, the `mysql`/`postgres` service blocks, and the `on: push` trigger.

No other file in the repository is modified by this plan (verified in Task 2's comprehensive check that `bin/composer-policy`, `tools/composer/ComposerPolicyCommandContract.php`, and `composer.json`/`composer.lock` are byte-unchanged).
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: --no-scripts -->

<objective>
Turn the one-time Phase 1/2 Composer review checks into permanent PHP 8.4 CI gates, and prove the locked application — including its unchanged SendPortal Core route integration — boots on PHP 8.4 with a real, script-enabled install.

Purpose: Close out the milestone's runtime-validation requirements (RUNTIME-01..04, CI-01, CI-02) by extending the existing container-matrix GitHub Actions workflow: add a `:8.4` job, make the install step script-enabled so Laravel package discovery actually runs, and wire five independently-attributable failure surfaces (Composer metadata, platform requirements, dependency audit, Laravel/Core boot, and the two existing per-engine PHPUnit runs) so the job fails closed on any of them.
Output: `.github/workflows/ci.yml` extended in place with the `:8.4` matrix entry, four new/modified CI steps, and the existing MySQL/PostgreSQL PHPUnit coverage and `on: push` trigger fully preserved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-php-8-4-runtime-core-integration-and-ci-verification/03-CONTEXT.md
@.planning/phases/03-php-8-4-runtime-core-integration-and-ci-verification/03-RESEARCH.md
@.planning/phases/03-php-8-4-runtime-core-integration-and-ci-verification/03-PATTERNS.md
@.github/workflows/ci.yml
@composer.json
@bin/composer-policy
@README.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: PHP 8.4 matrix + script-enabled install + boot/Core-route proof — one path end-to-end</name>
  <precondition>composer.lock exists at the repo root and is git-tracked (Phase 2 DEPS-01 artifact this job installs against) — verified read-only via `git ls-files composer.lock` returning a non-empty path.</precondition>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml` (current file, all 58 lines — matrix at lines 7-11, install step at lines 46-47, step ordering)
    - `.planning/phases/03-php-8-4-runtime-core-integration-and-ci-verification/03-CONTEXT.md` decisions D-01 (matrix entry), D-04 (script-enabled install), D-05 (boot + Core-route proof, Claude's Discretion on combined-vs-separate steps and env provisioning)
    - `.planning/phases/03-php-8-4-runtime-core-integration-and-ci-verification/03-RESEARCH.md` Pitfall 1 (no env provisioning needed — verified locally, no `.env`/`APP_KEY` required for `package:discover`/`about`/`route:list`), Pitfall 3 (route visibility is boot-time, not auth-gated), Pitfall 5 (GitHub Actions' default `bash -eo pipefail` already fails the job correctly on a plain piped command), "Code Examples" section (exact verified command sequence), and "The route target" (RUNTIME-04 proof: `sendportal.dashboard`)
    - `composer.json` `scripts.post-autoload-dump` (the `artisan package:discover --ansi` hook this task's install-step edit exercises)
  </read_first>
  <action>
    Append a third string-literal entry, `"kirschbaumdevelopment/laravel-test-runner:8.4"`, to the `strategy.matrix.container` array (currently the two `:8.2`/`:8.3` entries), matching the existing indentation and trailing-comma style, per D-01.

    In the `Install composer dependencies` step, whose `run:` value is currently `php bin/composer-policy install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist`, remove only the script-suppression flag (the one sitting between `--no-interaction` and `--no-progress`) so Composer's `post-autoload-dump` hook runs `artisan package:discover --ansi` during install, per D-04. Keep every other flag and the guard-wrapper invocation (`php bin/composer-policy install ...`) exactly as-is — do not touch the guard contract itself.

    Immediately after the (now script-enabled) install step and before the `Run Testsuite against MySQL` step, add one new step named `Verify Laravel boot and SendPortal Core route registration` whose `run:` block runs exactly two commands in sequence: first `php artisan about` (RUNTIME-01 safe boot check — exercises the full service-provider boot chain, read-only, no side effects), then `php artisan route:list --no-ansi | grep -q sendportal.dashboard` (RUNTIME-04 Core route-registration proof — `sendportal.dashboard` is the top-level dashboard route SendPortal Core registers via `Sendportal::webRoutes()`, confirmed present in `route:list` output regardless of the `auth`/`RequireWorkspace` middleware wrapping it, per RESEARCH.md Pitfall 3). Do not set an explicit `shell:` key and do not add any override that would let this step's failure be treated as non-fatal to the job — GitHub Actions' default step/pipefail behavior must be the only failure-propagation mechanism, per D-05 and RESEARCH.md Pitfall 5. Do not add an `env:` block to this step: no `.env` copy and no `key:generate` call are required (RESEARCH.md Pitfall 1 verified this live against the real committed lock and real PHP 8.4 — `config('app.key')` resolving to `NULL` does not affect any of these three commands, since none of them resolve the encrypter).
  </action>
  <reversibility rating="reversible">D-01/D-04/D-05 are all rated reversible in 03-CONTEXT.md — a matrix-list addition, a flag removal, and new read-only CI steps are all one-line-scale edits with no data migration or published-contract impact.</reversibility>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` `strategy.matrix.container` array contains all three literals: `kirschbaumdevelopment/laravel-test-runner:8.2`, `:8.3`, and `:8.4`.
    - The `Install composer dependencies` step's `run:` value no longer contains the script-suppression flag, and still contains `--no-ansi`, `--no-interaction`, `--no-progress`, and `--prefer-dist`.
    - A new step named `Verify Laravel boot and SendPortal Core route registration` exists between the install step and the `Run Testsuite against MySQL` step; its `run:` block contains both `php artisan about` and a `php artisan route:list` command piped to `grep -q sendportal.dashboard`.
    - That new step has no `env:` key and no `continue-on-error:` key.
    - Running the edited install command (`php bin/composer-policy install -q --no-ansi --no-interaction --no-progress --prefer-dist`) locally against the real committed lock exits 0.
    - Running `php artisan about` locally exits 0, and running `php artisan route:list --no-ansi | grep -q sendportal.dashboard` locally exits 0.
  </acceptance_criteria>
  <verify>
    <automated>git ls-files composer.lock | grep -q . && grep -n 'laravel-test-runner:8.4' .github/workflows/ci.yml && ! grep -q -- '--no-scripts' .github/workflows/ci.yml && php bin/composer-policy install -q --no-ansi --no-interaction --no-progress --prefer-dist && php artisan about >/dev/null && php artisan route:list --no-ansi | grep -q sendportal.dashboard && echo TRACER_OK</automated>
  </verify>
  <done>The 8.4 matrix entry, the script-enabled install step, and the combined boot/Core-route step are present in `.github/workflows/ci.yml` in the correct relative order, and the exact command sequence has been run locally end-to-end against the real committed lockfile and real PHP 8.4, exiting 0 at every stage.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Composer metadata/platform/audit gates + final whole-workflow assembly check</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml` (as edited by Task 1 — re-read before this task's edits to confirm current step order)
    - `.planning/phases/03-php-8-4-runtime-core-integration-and-ci-verification/03-CONTEXT.md` decision D-03 (five independent failure surfaces, exact guard-vs-direct routing per sub-item), D-06 (database matrix unchanged), D-07 (trigger unchanged)
    - `.planning/phases/03-php-8-4-runtime-core-integration-and-ci-verification/03-RESEARCH.md` "Pattern: Non-mutating diagnostic run directly, mutating/audited commands run through the guard", Pitfall 2 (container's unpinned system Composer — mitigated here by calling the vendored phar directly instead), Pitfall 4 (`--lock`/`--locked` flags must be present, not dropped), "Recommended Step Ordering" section
    - `bin/composer-policy` (confirms `tools/composer/composer-2.10.2.phar` as the vendored, checksum-verified distribution it also uses internally — lines defining `COMPOSER_PHAR_NAME` and the `tools/composer/` path)
    - `README.md` § "Lockfile review" (the exact three-command invocation convention this task's gate steps mirror, including the vendored-phar call for platform requirements)
  </read_first>
  <action>
    Insert one new step named `Verify Composer manifest` immediately after the existing `Verify Composer policy routes` step and before the `Install composer dependencies` step, whose `run:` value is exactly `php bin/composer-policy validate --strict --no-interaction` (guard-routed; `validate` is in the guard's four-command canonical set).

    Insert one new step named `Check platform requirements` immediately after the (script-enabled) install step and before the `Verify Laravel boot and SendPortal Core route registration` step added in Task 1, whose `run:` value is exactly `php tools/composer/composer-2.10.2.phar check-platform-reqs --lock --no-interaction`. Call the repository's own vendored, checksum-verified Composer distribution directly — the same one `bin/composer-policy` itself uses internally and the same invocation `README.md`'s "Lockfile review" section already documents — rather than the container's unpinned system `composer` binary, closing the version-drift risk RESEARCH.md's Pitfall 2 flags. Do not route this through `php bin/composer-policy`: `check-platform-reqs` is confirmed outside the guard's canonical command set and would be rejected.

    Insert one new step named `Audit dependencies` immediately after `Check platform requirements` and before the boot/Core-route step, whose `run:` value is exactly `php bin/composer-policy audit --locked --no-interaction` (guard-routed; `audit` is in the canonical set; this enforces the existing native `config.policy` block already committed in `composer.json` — `block: true`, `audit: "fail"`, the three time-bounded `ignore-id` exceptions — as a permanent CI gate rather than a one-time review).

    The complete step order after this task must read: checkout → `Verify Composer policy routes` → `Verify Composer manifest` → `Install composer dependencies` → `Check platform requirements` → `Audit dependencies` → `Verify Laravel boot and SendPortal Core route registration` → `Run Testsuite against MySQL` → `Run Testsuite against Postgres`. Do not reorder, remove, or modify the MySQL/Postgres service blocks, the two PHPUnit steps, or the `on: push` trigger at the top of the file. Do not add any flag or step, anywhere in this file, that would suppress or bypass a platform, audit, or metadata failure, and do not add any construct that would let a step's nonzero exit be swallowed instead of failing the job.
  </action>
  <reversibility rating="reversible">D-03/D-06/D-07 are all rated reversible in 03-CONTEXT.md — three additional read-only/guarded CI steps inserted at fixed points, with no change to the database services or trigger.</reversibility>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains three new step names, in this relative order: `Verify Composer manifest` (before `Install composer dependencies`), then later `Check platform requirements` and `Audit dependencies` (both after `Install composer dependencies` and before `Verify Laravel boot and SendPortal Core route registration`).
    - `Verify Composer manifest`'s `run:` value is exactly `php bin/composer-policy validate --strict --no-interaction`.
    - `Check platform requirements`'s `run:` value is exactly `php tools/composer/composer-2.10.2.phar check-platform-reqs --lock --no-interaction`.
    - `Audit dependencies`'s `run:` value is exactly `php bin/composer-policy audit --locked --no-interaction`.
    - The `mysql:5.7` and `postgres` service image tags, both PHPUnit steps' `env:` blocks, and the top-level `on: push` trigger are byte-identical to the pre-Task-1 file.
    - No line in `.github/workflows/ci.yml` contains a platform/audit bypass flag or any error-suppression construct.
    - `bin/composer-policy`, `tools/composer/ComposerPolicyCommandContract.php`, `composer.json`, and `composer.lock` are untouched by this plan (empty `git diff` against each).
    - Running the full assembled command sequence locally, in the file's exact order, exits 0 at every step.
  </acceptance_criteria>
  <verify>
    <automated>php bin/composer-policy validate --strict --no-interaction && php bin/composer-policy install -q --no-ansi --no-interaction --no-progress --prefer-dist && php tools/composer/composer-2.10.2.phar check-platform-reqs --lock --no-interaction && php bin/composer-policy audit --locked --no-interaction && php artisan about >/dev/null && php artisan route:list --no-ansi | grep -q sendportal.dashboard && echo FULL_DRY_RUN_OK</automated>
    <automated>awk '/name: Verify Composer policy routes/{g=NR} /name: Verify Composer manifest/{a=NR} /name: Install composer dependencies/{b=NR} /name: Check platform requirements/{c=NR} /name: Audit dependencies/{d=NR} /name: Verify Laravel boot and SendPortal Core route registration/{e=NR} /name: Run Testsuite against MySQL/{f=NR} END{print (g<a && a<b && b<c && c<d && d<e && e<f) ? "ORDER_OK" : "ORDER_BAD"}' .github/workflows/ci.yml</automated>
    <automated>! grep -q -- '--ignore-platform-req' .github/workflows/ci.yml && ! grep -q -- '--no-audit' .github/workflows/ci.yml && ! grep -q -- 'continue-on-error' .github/workflows/ci.yml && ! grep -q -- '|| true' .github/workflows/ci.yml && ! grep -q -- 'set +e' .github/workflows/ci.yml && echo PROHIBITIONS_OK</automated>
    <automated>git diff --quiet HEAD -- bin/composer-policy tools/composer/ComposerPolicyCommandContract.php composer.json composer.lock && echo GUARD_UNTOUCHED</automated>
  </verify>
  <done>All five CI-02 failure surfaces are wired as independent, correctly-ordered steps in `.github/workflows/ci.yml`; the existing MySQL/Postgres coverage and `on: push` trigger are byte-unchanged; the guard contract and lockfile are untouched; and the entire assembled command sequence has been run locally, end-to-end, against real PHP 8.4 and the real committed lock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner ↔ committed repo state | GitHub Actions executes commands against the committed `composer.lock`/`composer.json`; a weakened gate step could let an incompatible or vulnerable graph reach `main` undetected. |
| Guarded Composer route ↔ direct Composer route | `validate`/`install`/`audit` go through `bin/composer-policy`'s checksum-verified, fail-closed wrapper; `check-platform-reqs` is a deliberate, narrow exception — a read-only diagnostic invoked directly against the same vendored, checksum-verified phar (not the container's unpinned system Composer). |
| Push trigger ↔ contributor-authored commits | `on: push` is the existing, unchanged exposure surface; this phase adds no new trigger. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | New/modified CI gate steps (validate, install, platform-reqs, audit, boot/route) | high | mitigate | Every new step relies exclusively on GitHub Actions' default fail-closed step/pipefail semantics; Task 2's acceptance criteria and verify block explicitly assert no error-suppression construct exists anywhere in the file. |
| T-03-02 | Tampering | Dependency graph consumed by the 8.4 job | high | mitigate | Install step continues to consume the committed lock only (`install`, never `update`); Task 2 asserts no platform/audit bypass flag is present anywhere in the edited file, and that `composer.json`/`composer.lock` are untouched by this plan. |
| T-03-03 | Elevation of Privilege | `bin/composer-policy` canonical command set | medium | accept | `check-platform-reqs` stays outside the guard's four-command set per the guard's own test-authoritative contract; it is read-only/non-mutating, so no elevation risk, and Task 2 verifies the guard contract file is byte-unchanged. |
| T-03-04 | Tampering | Composer binary used for the platform-requirements check | low | mitigate | Invoke the already checksum-verified `tools/composer/composer-2.10.2.phar` directly (the same distribution the guard itself uses) instead of an unpinned container system `composer` binary, closing the version-drift risk identified in RESEARCH.md Pitfall 2. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package-manager installs occur in this phase (Package Legitimacy Audit: N/A per 03-RESEARCH.md) — no packages to disposition. |
</threat_model>

<verification>
Run on real PHP 8.4.23 + Composer 2.10.2 (all EXIT 0):
- `php bin/composer-policy validate --strict --no-interaction`
- `php bin/composer-policy install -q --no-ansi --no-interaction --no-progress --prefer-dist`
- `php tools/composer/composer-2.10.2.phar check-platform-reqs --lock --no-interaction`
- `php bin/composer-policy audit --locked --no-interaction`
- `php artisan about`
- `php artisan route:list --no-ansi | grep -q sendportal.dashboard`

Plus a structural check of `.github/workflows/ci.yml`: the `:8.4` matrix entry, the five new/modified gate steps in the exact order above, and the existing MySQL/Postgres PHPUnit steps and `on: push` trigger unchanged. The actual `:8.4` CI job (real Docker container, real MySQL/Postgres services) must also be observed green on GitHub Actions before `/gsd-verify-work` — local reproduction covers every command's correctness, but the container's own Composer/extension baseline (RESEARCH.md Pitfall 2/Assumption A1) is only fully proven by the first live run.
</verification>

<success_criteria>
`.github/workflows/ci.yml` runs a `:8.4` matrix job alongside the retained `:8.2`/`:8.3` jobs; on every matrix version the install step is script-enabled and package discovery completes; a boot check and a SendPortal Core route-registration proof both pass; the existing MySQL and PostgreSQL PHPUnit steps are unchanged and still gate the job; and each of the five CI-02 failure surfaces (metadata, platform requirements, audit, boot/Core, per-engine PHPUnit) is an independently-attributable step that fails the job on nonzero exit, with no bypass flag or error-suppression construct anywhere in the file.
</success_criteria>

<output>
Create `.planning/phases/03-php-8-4-runtime-core-integration-and-ci-verification/03-01-SUMMARY.md` when done, capturing: the full local dry-run output (all six command exit codes), the structural order check result, the prohibition/guard-untouched check results, and a note that the actual GitHub Actions `:8.4` job run is the outstanding real-CI proof.
</output>

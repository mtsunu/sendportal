---
phase: 02-reproducible-dependency-snapshot
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - composer.json
  - bin/composer-policy
  - composer.lock
  - README.md
autonomous: true
requirements:
  - DEPS-01
  - DEPS-02
  - DEPS-03
  - DEPS-04
must_haves:
  truths:
    - "The three advisory reasons in composer.json (`config.policy.advisories.ignore-id`) and the `$rationale` constant in bin/composer-policy are byte-identical and name `laravel/framework v11.55.0` with the forward expiry — D-01."
    - "composer.json contains exactly the three owner-approved IDs (`grep -c 'PKSA-' composer.json` returns 3): PKSA-3r5d-mb8f-1qw9, PKSA-m5cs-t1y6-qpcs, PKSA-mdq4-51ck-6kdq — D-02."
    - "The consumed self-referential Phase-2-lockfile-review expiry clause no longer appears in composer.json (`grep -c 'Phase 2 lockfile review' composer.json` returns 0) — D-01."
    - "`php bin/composer-policy install --prefer-dist --no-interaction` exits 0 after the edit (guard `$rationale` constant in lockstep with composer.json; manifest policy passes) — D-01 / Pitfall 2."
    - "`php bin/composer-policy audit --locked --no-interaction` exits 0 and reports 3 ignored advisories affecting 1 package, no non-ignored advisory — DEPS-03."
    - "`php tools/composer/composer-2.10.2.phar check-platform-reqs --lock --no-interaction` exits 0 with every row reporting `success` on PHP 8.4 — DEPS-02."
    - "`php bin/composer-policy validate --strict --no-interaction` exits 0 after the advisory edit and lock regeneration (json↔lock re-synced) — DEPS-01."
    - "The regenerated lock's package name→version set is unchanged from Plan 01 (only content-hash changes from the manifest edit) — D-03 / Pitfall 3."
    - "README documents guarded `install` for operators, guarded `install --no-dev --optimize-autoloader` for deployment, and reserves `update` for intentional upgrades; and adds a repeatable 'Lockfile review' three-command procedure — DEPS-04 / D-05 / D-06 / D-07."
    - ".github/workflows/ci.yml still installs via the guarded `install ... --no-scripts` (dev deps kept) with no PHP 8.4 job and no new gate added — DEPS-04 / D-06."
  artifacts:
    - "composer.json (three advisory reasons + forward expiry naming v11.55.0)"
    - "bin/composer-policy (`$rationale` constant edited in lockstep)"
    - "composer.lock (regenerated: content-hash refreshed, name→version set unchanged)"
    - "README.md (install-vs-update contract, --no-dev deploy note, Lockfile review procedure)"
    - "Captured verification evidence (three-command review, inline in this plan's SUMMARY)"
  key_links:
    - "composer.json `config.policy.advisories.ignore-id` ↔ bin/composer-policy `$rationale`/ID map (strict equality; must stay byte-identical or guarded install/update fail closed)."
    - "composer.json ↔ composer.lock re-synchronization after the advisory edit (`validate --strict`)."
  prohibitions:
    - "No package-wide ignore in config.policy.advisories."
    - "No severity-wide ignore."
    - "No non-blocking audit mode (`block` stays true, `audit` stays `fail`)."
    - "No platform/ignore bypass flags (`--ignore-platform-reqs`, `--no-check-all`, `--no-check-platform-reqs`, `--no-audit`, `-d`, `--working-dir`)."
    - "Do not broaden the owner residual-risk acceptance beyond exactly the three IDs; add no fourth ID."
    - "Do not invent new deployment automation or deploy scripts (D-05); documentation only."
    - "Do not re-resolve or upgrade the approved graph (freeze only; name→version set must match Plan 01)."
    - "Do not add a dedicated PHP 8.4 CI job, permanent validate/platform/audit CI gating, a Laravel boot check, or a PHPUnit matrix (Phase 3 / CI-01, CI-02, RUNTIME-01…04)."
---

<objective>
Expand from the proven tracer: resolve the expiring Phase-1 advisory exception against the locked `laravel/framework v11.55.0` (RETAIN branch), keeping `composer.json` and the hardcoded guard constant in lockstep; capture the three-command lockfile-review evidence on real PHP 8.4; and document the `install`-vs-`update` install contract for local, CI, and deployment paths.

Purpose: Finalize the security-policy contract (DEPS-03 + D-01), prove the locked graph is PHP-8.4 platform-clean (DEPS-02), keep json↔lock synchronized after the manifest edit (DEPS-01), and make the committed lock the documented standard install (DEPS-04) without inventing automation or crossing into Phase 3.
Output: Edited `composer.json` + `bin/composer-policy` (lockstep), regenerated `composer.lock`, README install/review documentation, and captured review evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reproducible-dependency-snapshot/02-CONTEXT.md
@.planning/phases/02-reproducible-dependency-snapshot/02-RESEARCH.md
@composer.json
@bin/composer-policy
@README.md
@.github/workflows/ci.yml
</context>

<artifacts_this_phase_produces>
This plan produces / changes:
- `composer.json` — `config.policy.advisories.ignore-id`: three reasons rewritten to name `laravel/framework v11.55.0` with the forward expiry; the consumed self-referential clause removed. `block: true`, `audit: fail`, exactly three IDs preserved.
- `bin/composer-policy` — the `$rationale` constant (single occurrence, ~line 365) edited byte-identically to the new composer.json reason.
- `composer.lock` — regenerated once so its content-hash reflects the edited manifest; name→version set unchanged from Plan 01.
- `README.md` — "Dependency management" clarified (`install` vs `update`, `--no-dev --optimize-autoloader` deploy note) plus a "Lockfile review" three-command procedure.
- Verification evidence — the three review-command outputs (EXIT codes + audit table) captured inline in this plan's SUMMARY (D-07).

Not modified (verified only): `.github/workflows/ci.yml` (already guarded, kept as-is; Phase 3 owns the PHP 8.4 job).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Lockstep advisory refresh + lock re-sync + audit</name>
  <files>composer.json, bin/composer-policy, composer.lock</files>
  <read_first>
    - `composer.json` (the `config.policy.advisories.ignore-id` map, lines ~35-39)
    - `bin/composer-policy` (the `hasExactManifestPolicy` function and the `$rationale` constant at line ~365, plus the `install`/`update` policy checks at lines ~403/~422)
    - `.planning/phases/02-reproducible-dependency-snapshot/02-RESEARCH.md` § "Pattern 1" (RETAIN branch, v11.55.0), § "Pitfall 2" (hardcoded rationale), § "Pitfall 3" (content-hash drift)
    - `.planning/phases/02-reproducible-dependency-snapshot/02-CONTEXT.md` decisions D-01, D-02, D-03
  </read_first>
  <action>
    The research verified the RETAIN branch: locked `laravel/framework` is `v11.55.0` and `audit --locked` confirms all three advisory IDs still affect it — so keep exactly the three IDs; do NOT drop them and do NOT add any other, package-wide, or severity-wide ignore. Keep `"block": true` and `"audit": "fail"`.

    1. In `composer.json` `config.policy.advisories.ignore-id`, rewrite each of the three reasons (for PKSA-3r5d-mb8f-1qw9, PKSA-m5cs-t1y6-qpcs, PKSA-mdq4-51ck-6kdq) to one shared string that (a) names `laravel/framework v11.55.0`, (b) states the residual risk is accepted by the project owner, and (c) sets the forward expiry to: the next dependency-upgrade review, or when a stable `mettle/sendportal-core` release permits an upgraded Laravel line, whichever comes first. The consumed self-referential clause that pinned the expiry to this phase's lockfile review must not survive into the committed manifest.
    2. In `bin/composer-policy`, edit the single `$rationale` constant (~line 365) so it is BYTE-IDENTICAL to the new composer.json reason string. The guard compares `config.policy.advisories.ignore-id` to its expected map with strict equality; any mismatch makes guarded `install`/`update` fail closed. This lockstep edit is mandatory and non-negotiable.
    3. Regenerate the lock once through the guarded path: `php bin/composer-policy update --prefer-dist --no-interaction`. This refreshes the lock content-hash to match the edited manifest (the hash legitimately changes — Pitfall 3 — this is not drift). Prove the name→version set is unchanged from the Plan 01 lock (extracted `"name"`/`"version"` diff empty); a changed version is drift and must be stopped and investigated.
    4. Re-establish sync and prove the guard constant is in lockstep: a guarded `install` (which runs the manifest-policy check) must exit 0.

    Do not use any bypass flag. Do not touch `.github/workflows/ci.yml`.
  </action>
  <reversibility rating="costly">The ignore-id set and its expiry are the security-policy contract (D-01); changing them later means re-auditing and re-obtaining owner residual-risk acceptance, and the manifest↔guard coupling must stay byte-identical.</reversibility>
  <verify>
    <automated>php bin/composer-policy install --prefer-dist --no-interaction && php bin/composer-policy validate --strict --no-interaction && php bin/composer-policy audit --locked --no-interaction && test "$(grep -c 'PKSA-' composer.json)" = "3" && test "$(grep -c 'Phase 2 lockfile review' composer.json)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PKSA-' composer.json` returns `3` (exactly the three owner-approved IDs).
    - `grep -c 'Phase 2 lockfile review' composer.json` returns `0` (consumed self-referential clause removed).
    - `grep -c 'v11.55.0' composer.json` returns `3` (each reason names the locked version) and the identical string appears in `bin/composer-policy`.
    - `php bin/composer-policy install --prefer-dist --no-interaction` exits 0 (guard manifest policy passes → constant is in lockstep; Pitfall 2 detector).
    - `php bin/composer-policy validate --strict --no-interaction` exits 0 (`./composer.json is valid`) after regeneration.
    - `php bin/composer-policy audit --locked --no-interaction` exits 0 and reports 3 ignored advisories affecting 1 package.
    - name→version diff between the Plan 01 lock and the regenerated lock is empty (only content-hash changed).
  </acceptance_criteria>
  <done>The three advisory reasons and the guard constant are byte-identical and name v11.55.0 with a real forward expiry; guarded `install`/`validate`/`audit` all exit 0; the lock is re-synced with the package set unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Platform-check evidence + install-contract documentation</name>
  <files>README.md</files>
  <read_first>
    - `README.md` § "Dependency management" (lines ~33-46)
    - `.github/workflows/ci.yml` (the "Install composer dependencies" step, line ~47 — verify only, do not edit)
    - `.planning/phases/02-reproducible-dependency-snapshot/02-RESEARCH.md` § "Pattern 3" (no guarded route for `check-platform-reqs` → tracked phar), § "Pattern 4" (install contract), § "Validation Architecture" (three-command procedure)
    - `.planning/phases/02-reproducible-dependency-snapshot/02-CONTEXT.md` decisions D-05, D-06, D-07
  </read_first>
  <action>
    1. DEPS-02 evidence: run the platform check on the locked graph via the tracked, integrity-verified distribution directly — `php tools/composer/composer-2.10.2.phar check-platform-reqs --lock --no-interaction`. There is no guarded route (the guard's allowed commands are validate/audit/install/update only — Pattern 3/Pitfall 1); this run is read-only and cannot weaken any safeguard, and D-07 permits it ("where a guarded route exists" — here none does). Capture the output (every row `success`, EXIT 0) for the evidence record.
    2. DEPS-04 documentation in `README.md` "Dependency management": clarify that ordinary operator installs use the guarded `install --prefer-dist --no-interaction` (which consumes the committed lock, no re-resolution); add the production deployment install as guarded `install --no-dev --optimize-autoloader --no-interaction`; and state that `update` is reserved for intentional dependency-upgrade work only. Do not invent any deploy script (D-05) — document the existing guarded command.
    3. D-07 procedure: add a short README "Lockfile review" subsection listing the repeatable three-command review — guarded `validate --strict`, tracked-phar `check-platform-reqs --lock`, guarded `audit --locked` — as the standard lockfile-review evidence steps.
    4. Confirm `.github/workflows/ci.yml` still installs via the guarded `install ... --no-scripts ...` with dev deps kept, and that NO PHP 8.4 job or new validate/platform/audit gate has been added (that is Phase 3 / CI-01, CI-02). Verify-only; do not modify the workflow.
    5. Capture the three review-command outputs (EXIT codes + the audit table) inline in this plan's SUMMARY as the phase verification evidence.
  </action>
  <verify>
    <automated>php tools/composer/composer-2.10.2.phar check-platform-reqs --lock --no-interaction && grep -q 'no-dev' README.md && grep -q 'Lockfile review' README.md && grep -q 'no-scripts' .github/workflows/ci.yml && git diff --quiet HEAD -- .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `php tools/composer/composer-2.10.2.phar check-platform-reqs --lock --no-interaction` exits 0 with every requirement row reporting `success` (no `failed`/`missing`) on PHP 8.4.
    - `grep -q 'no-dev' README.md` succeeds (deployment `--no-dev --optimize-autoloader` guidance present).
    - `grep -q 'Lockfile review' README.md` succeeds (the three-command review procedure documented).
    - README distinguishes operator `install` from intentional `update` (both under the guarded command).
    - `grep -q 'no-scripts' .github/workflows/ci.yml` succeeds and `git diff --quiet HEAD -- .github/workflows/ci.yml` exits 0 (workflow byte-unchanged; no PHP 8.4 job or new gate added).
    - The three review-command outputs are captured in the plan SUMMARY.
  </acceptance_criteria>
  <done>DEPS-02 platform evidence is captured on real PHP 8.4; README documents the install-vs-update contract, the `--no-dev` deploy install, and the repeatable Lockfile-review procedure; ci.yml is confirmed unchanged with no Phase-3 CI work added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer.json policy ↔ guard constant | Two coupled encodings of the ignore-id map; drift between them fails the guard open or closed. |
| caller → Composer phar | The guard verifies the tracked phar by SHA-256; the direct `check-platform-reqs` run is read-only. |
| locked graph → security audit | `audit --locked` + `config.policy.advisories` is the non-bypassed gate for the frozen graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Elevation | config.policy.advisories | high | mitigate | D-02: keep exactly the three IDs; no package-/severity-wide ignore, no non-blocking mode; guard strict-equality on the ignore-id map enforces the scope. |
| T-02-04 | Tampering | composer.json ↔ bin/composer-policy | high | mitigate | Lockstep byte-identical edit (Pitfall 2), verified by a guarded `install` exiting 0; asymmetric pass (validate/audit ok but install rejects) signals drift. |
| T-02-05 | Tampering | regenerated composer.lock | medium | mitigate | Prove name→version set unchanged from Plan 01 (Pitfall 3: only content-hash changes) + `validate --strict` re-sync before commit. |
| T-02-06 | Elevation | guarded install/update, platform check | medium | mitigate | No bypass flags; the guard rejects `--ignore-platform-reqs`/`--no-audit`/`-d`/`--working-dir`; the direct phar `check-platform-reqs` is read-only. |
| T-02-SC | Tampering | tracked Composer phar | low | accept | Guard verifies `tools/composer/composer-2.10.2.phar` by SHA-256; no new packages introduced this phase. |
</threat_model>

<verification>
Run on real PHP 8.4.23 + Composer 2.10.2 (all EXIT 0):
- `php bin/composer-policy validate --strict --no-interaction` → `./composer.json is valid`.
- `php tools/composer/composer-2.10.2.phar check-platform-reqs --lock --no-interaction` → every row `success`.
- `php bin/composer-policy audit --locked --no-interaction` → 3 ignored advisories affecting 1 package.
- `php bin/composer-policy install --prefer-dist --no-interaction` → EXIT 0 (manifest policy passes → guard in lockstep).
- `grep -c 'PKSA-' composer.json` → 3; `grep -c 'Phase 2 lockfile review' composer.json` → 0; `git diff --quiet HEAD -- .github/workflows/ci.yml` → EXIT 0 (workflow unchanged).
</verification>

<success_criteria>
The advisory exception is resolved on the RETAIN branch (three IDs, reasons naming v11.55.0 with a forward expiry, guard constant in lockstep); the locked graph passes the non-bypassed `audit --locked` and the PHP 8.4 `check-platform-reqs --lock`; json↔lock stays synchronized under `validate --strict`; and README documents the committed-lock install contract without inventing automation or adding Phase-3 CI work.
</success_criteria>

<output>
Create `.planning/phases/02-reproducible-dependency-snapshot/02-02-SUMMARY.md` when done, with the three review-command outputs (EXIT codes + audit table), the guarded-install lockstep confirmation, and the name→version diff result.
</output>

---
phase: 01-constraint-resolution-and-security-control
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - composer.json
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
must_haves:
  truths:
    - "The published root PHP constraint is ^8.2, accurately declaring PHP 8.2, 8.3, and 8.4 support under D-01."
    - "Roave is absent only when Composer 2.10+ native advisory policy keeps block: true and audit: fail and limits ignore-id to the three D-02 advisory IDs with their internal-only, owner-accepted-risk, and expiry reasons."
    - "A fresh temporary COMPOSER_HOME clean PHP 8.4 install completes without inherited Composer configuration, platform bypass, policy bypass, config.platform, package-wide ignore, severity-wide ignore, or a development-branch Laravel."
    - "The configured locked audit passes, while a separate audit with the three ignores removed reports exactly the approved three IDs and no unexpected suppressed advisory."
  artifacts:
    - path: "composer.json"
      provides: "Accurate PHP support declaration and Composer-native advisory policy without the incompatible Roave metapackage."
    - path: ".planning/phases/01-constraint-resolution-and-security-control/01-02-SUMMARY.md"
      provides: "Exact candidate versions, clean PHP 8.4 install/audit evidence, residual-risk evidence audit, and a Phase 2 lockfile handoff."
  key_links:
    - from: "composer.json config.policy.advisories"
      to: "Composer 2.10+ resolver and composer audit --locked"
      via: "block=true, audit=fail, and only D-02's three per-ID exceptions"
      pattern: "The configured audit passes only for the explicit exception, and an ignore-free evidence audit reports exactly the same three IDs."
    - from: "composer.json require.php"
      to: "real PHP 8.4 clean install"
      via: "root constraint ^8.2 and a fresh COMPOSER_HOME"
      pattern: "The declared compatibility contract and target runtime agree without config.platform emulation."
---

<objective>
Apply the resolved D-02 policy and minimal PHP constraint change, then prove that it resolves and audits on isolated real PHP 8.4 without suppressing unexpected advisories.

Purpose: Deliver the accurate, reviewable Phase 1 Composer contract that Phase 2 can freeze into a reviewed lockfile.
Output: Updated `composer.json` plus clean-environment PHP 8.4 install, configured-audit, and ignore-free evidence-audit records; repository `composer.lock` remains absent.
</objective>

<execution_context>
@/Users/meigire/.codex/gsd-core/workflows/execute-plan.md
@/Users/meigire/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-constraint-resolution-and-security-control/01-CONTEXT.md
@.planning/phases/01-constraint-resolution-and-security-control/01-RESEARCH.md
@.planning/phases/01-constraint-resolution-and-security-control/01-PATTERNS.md
@.planning/phases/01-constraint-resolution-and-security-control/01-VALIDATION.md
@.planning/phases/01-constraint-resolution-and-security-control/01-01-SUMMARY.md
@composer.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the exact approved Composer support and advisory-policy contract</name>
  <reversibility rating="costly">D-01’s PHP 8.2–8.4 constraint is published Composer metadata used by operators and later PHP CI coverage, so narrowing or reopening it requires coordinated compatibility changes.</reversibility>
  <read_first>composer.json; .planning/phases/01-constraint-resolution-and-security-control/01-01-SUMMARY.md; 01-CONTEXT.md decisions D-01 through D-03; 01-PATTERNS.md section “composer.json”; 01-RESEARCH.md sections “Pattern 1”, “Pattern 2”, and “Open Questions”.</read_first>
  <files>composer.json</files>
  <action>Implement only the resolved manifest change. Per D-01, replace the redundant root `require.php` expression with `^8.2`. Per D-02, remove `roave/security-advisories` from `require-dev` and add `config.policy.advisories` under the existing `config`: `block: true`, `audit: fail`, and an `ignore-id` object containing exactly `PKSA-m5cs-t1y6-qpcs`, `PKSA-3r5d-mb8f-1qw9`, and `PKSA-mdq4-51ck-6kdq`. For each ID, use a reason that says the application is internal-only, the project owner accepted the residual risk, and it expires at Phase 2 lockfile review or when a compatible stable SendPortal Core release permits a Laravel upgrade, whichever comes first. Preserve the existing config entries, all unrelated bounds, scripts, minimum stability, and host source files. Per D-03, retain `laravel/framework: ^11.0` and `mettle/sendportal-core: ^3.0`. Do not add `config.platform`, legacy `config.audit.advisories` or `config.audit.ignore`, `policy.advisories.ignore`, package-wide ignore, severity-wide ignore, a Composer policy/platform override, or a development-branch framework constraint.</action>
  <verify>
    <automated>composer validate --strict --no-check-publish && php -r '$m=json_decode(file_get_contents("composer.json"),true,512,JSON_THROW_ON_ERROR); $c=$m["config"]??[]; $a=$c["policy"]["advisories"]??[]; $ids=["PKSA-m5cs-t1y6-qpcs","PKSA-3r5d-mb8f-1qw9","PKSA-mdq4-51ck-6kdq"]; $actual=array_keys($a["ignore-id"]??[]); sort($ids); sort($actual); foreach ($actual as $id) { $reason=$a["ignore-id"][$id]??""; if (!str_contains($reason,"internal-only") || !str_contains($reason,"project owner") || !str_contains($reason,"Phase 2 lockfile review") || !str_contains($reason,"compatible stable SendPortal Core")) exit(1); } exit(($m["require"]["php"]??null)==="^8.2" && !isset($m["require-dev"]["roave/security-advisories"]) && ($a["block"]??null)===true && ($a["audit"]??null)==="fail" && $actual===$ids && !isset($c["platform"],$c["audit"]["advisories"],$c["audit"]["ignore"],$a["ignore"],$a["ignore-package"],$a["ignore-severity"]) ? 0 : 1);' && test ! -e composer.lock</automated>
  </verify>
  <acceptance_criteria>`composer.json` is valid; it declares `php: ^8.2`, keeps Laravel 11 and SendPortal Core constraints unchanged, uses native blocking/audit-fail policy with exactly the three approved D-02 IDs and per-ID reasons, contains no Roave or broad suppression route, and repository lockfile creation remains deferred.</acceptance_criteria>
  <done>The repository manifest honestly declares the PHP support contract and has the exact, time-bounded Composer-native security-control replacement.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the isolated PHP 8.4 graph and its exact advisory boundary</name>
  <read_first>composer.json after Task 1; 01-01-SUMMARY.md; 01-CONTEXT.md decision D-02; 01-RESEARCH.md sections “Pattern 2”, “Validation Architecture”, and “Security Domain”; 01-VALIDATION.md sections “Per-Task Verification Map” and “Wave 0 Requirements”.</read_first>
  <precondition>PHP is 8.4.x, Composer is 2.10.x-or-newer 2.x, Packagist is reachable, and 01-01-SUMMARY.md records a tagged stable Laravel 11 candidate under the same exact D-02 policy.</precondition>
  <files>composer.json</files>
  <action>Create a disposable `mktemp -d` copy excluding `.git`, `vendor`, and `composer.lock`, with a newly created empty `$tmp/composer-home`. Assert both the repository manifest and copied manifest omit `config.platform`, legacy `config.audit.advisories`, legacy `config.audit.ignore`, `policy.advisories.ignore`, package-wide ignore, and severity-wide ignore; assert their only ignore surface is the exact three-key D-02 `ignore-id` object with the required reason terms. For every Composer command in the copy, explicitly unset `COMPOSER`, `COMPOSER_HOME`, `COMPOSER_POLICY`, `COMPOSER_NO_BLOCKING`, `COMPOSER_NO_SECURITY_BLOCKING`, `COMPOSER_IGNORE_PLATFORM_REQ`, and `COMPOSER_IGNORE_PLATFORM_REQS`, then set only `COMPOSER_HOME="$tmp/composer-home"`. Run `composer validate --strict --no-check-publish`, then `composer install --prefer-dist --no-interaction` on actual PHP 8.4. Parse the temporary lock to require a tagged stable Laravel 11 release, reject `11.x-dev`, and retain exact Laravel/Core versions. Run the configured `composer audit --locked` with `audit: fail`; it must succeed. Separately copy the installed temporary manifest to an evidence directory, remove only its three `ignore-id` entries, set `config.policy.advisories.audit` to `report`, keep blocking enabled, and run `composer audit --locked --format=json` with the same fresh-home invocation. Parse that JSON report by flattening every advisory entry and compare the sorted advisory IDs to the three approved IDs; fail on an empty report, an unexpected ID, or a missing approved ID. Record both audit commands, results, manifest assertions, PHP/Composer versions, and resolved Laravel/Core versions in the summary. Per D-01/D-02 do not use config.platform, ignore-platform flags, policy bypasses, package-wide or severity-wide ignores; per D-03 stop with evidence rather than changing the Laravel 11/Core boundary if the candidate cannot resolve.</action>
  <verify>
    <automated>zsh -ceu 'repo="$PWD"; test ! -e "$repo/composer.lock"; env | grep -Eq "^(COMPOSER|COMPOSER_HOME|COMPOSER_POLICY|COMPOSER_NO_BLOCKING|COMPOSER_NO_SECURITY_BLOCKING|COMPOSER_IGNORE_PLATFORM_REQ|COMPOSER_IGNORE_PLATFORM_REQS)=" && exit 1 || true; php -r "exit(version_compare(PHP_VERSION, \"8.4.0\", \">=\") && version_compare(PHP_VERSION, \"8.5.0\", \"<\") ? 0 : 1);"; composer --version | grep -E "Composer version 2\\.(10|[1-9][0-9])\\."; tmp="$(mktemp -d)"; trap "rm -rf -- $tmp" EXIT; mkdir "$tmp/composer-home"; rsync -a --exclude=.git --exclude=vendor --exclude=composer.lock "$repo/" "$tmp/app/"; cd "$tmp/app"; php -r "\$m=json_decode(file_get_contents(\"composer.json\"),true,512,JSON_THROW_ON_ERROR); \$c=\$m[\"config\"]??[]; \$a=\$c[\"policy\"][\"advisories\"]??[]; \$ids=[\"PKSA-m5cs-t1y6-qpcs\",\"PKSA-3r5d-mb8f-1qw9\",\"PKSA-mdq4-51ck-6kdq\"]; \$actual=array_keys(\$a[\"ignore-id\"]??[]); sort(\$ids); sort(\$actual); foreach(\$actual as \$id){\$r=\$a[\"ignore-id\"][\$id]??\"\"; if(!str_contains(\$r,\"internal-only\")||!str_contains(\$r,\"project owner\")||!str_contains(\$r,\"Phase 2 lockfile review\")||!str_contains(\$r,\"compatible stable SendPortal Core\"))exit(1);} exit(\$actual===\$ids && !isset(\$c[\"platform\"],\$c[\"audit\"][\"advisories\"],\$c[\"audit\"][\"ignore\"],\$a[\"ignore\"],\$a[\"ignore-package\"],\$a[\"ignore-severity\"])?0:1);"; env -u COMPOSER -u COMPOSER_HOME -u COMPOSER_POLICY -u COMPOSER_NO_BLOCKING -u COMPOSER_NO_SECURITY_BLOCKING -u COMPOSER_IGNORE_PLATFORM_REQ -u COMPOSER_IGNORE_PLATFORM_REQS COMPOSER_HOME="$tmp/composer-home" composer validate --strict --no-check-publish; env -u COMPOSER -u COMPOSER_HOME -u COMPOSER_POLICY -u COMPOSER_NO_BLOCKING -u COMPOSER_NO_SECURITY_BLOCKING -u COMPOSER_IGNORE_PLATFORM_REQ -u COMPOSER_IGNORE_PLATFORM_REQS COMPOSER_HOME="$tmp/composer-home" composer install --prefer-dist --no-interaction > install.log 2>&1; env -u COMPOSER -u COMPOSER_HOME -u COMPOSER_POLICY -u COMPOSER_NO_BLOCKING -u COMPOSER_NO_SECURITY_BLOCKING -u COMPOSER_IGNORE_PLATFORM_REQ -u COMPOSER_IGNORE_PLATFORM_REQS COMPOSER_HOME="$tmp/composer-home" composer audit --locked; php -r "\$l=json_decode(file_get_contents(\"composer.lock\"),true,512,JSON_THROW_ON_ERROR); foreach(array_merge(\$l[\"packages\"]??[],\$l[\"packages-dev\"]??[]) as \$p){if((\$p[\"name\"]??null)===\"laravel/framework\"){exit(preg_match(\"/^v?11\\.[0-9]+\\.[0-9]+$/\",\$p[\"version\"]??\"\")===1?0:1);}} exit(1);"; mkdir "$tmp/evidence"; cp composer.json "$tmp/evidence/composer.json"; cp composer.lock "$tmp/evidence/composer.lock"; cd "$tmp/evidence"; php -r "\$m=json_decode(file_get_contents(\"composer.json\"),true,512,JSON_THROW_ON_ERROR); unset(\$m[\"config\"][\"policy\"][\"advisories\"][\"ignore-id\"]); \$m[\"config\"][\"policy\"][\"advisories\"][\"audit\"]=\"report\"; file_put_contents(\"composer.json\",json_encode(\$m,JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES).PHP_EOL);"; env -u COMPOSER -u COMPOSER_HOME -u COMPOSER_POLICY -u COMPOSER_NO_BLOCKING -u COMPOSER_NO_SECURITY_BLOCKING -u COMPOSER_IGNORE_PLATFORM_REQ -u COMPOSER_IGNORE_PLATFORM_REQS COMPOSER_HOME="$tmp/composer-home" composer audit --locked --format=json > evidence-audit.json; php -r "\$d=json_decode(file_get_contents(\"evidence-audit.json\"),true,512,JSON_THROW_ON_ERROR); \$found=[]; foreach((\$d[\"advisories\"]??[]) as \$entries){foreach(\$entries as \$entry){\$id=\$entry[\"advisoryId\"]??\$entry[\"id\"]??null; if(!is_string(\$id))exit(1); \$found[]=\$id;}} \$expected=[\"PKSA-m5cs-t1y6-qpcs\",\"PKSA-3r5d-mb8f-1qw9\",\"PKSA-mdq4-51ck-6kdq\"]; sort(\$found); \$found=array_values(array_unique(\$found)); sort(\$expected); exit(\$found===\$expected?0:1);"; test ! -e "$repo/composer.lock"'</automated>
  </verify>
  <acceptance_criteria>The isolated standard install completes on PHP 8.4, the configured `composer audit --locked` passes under `block: true` / `audit: fail`, its Laravel is tagged stable 11.x, and the separate ignore-free report contains exactly the three approved IDs. The summary proves no global configuration, broad ignore, platform bypass, policy bypass, or unexpected advisory suppression was involved; the repository still lacks `composer.lock`.</acceptance_criteria>
  <done>A secure, real-PHP-8.4 candidate is proven ready for Phase 2’s intentional lockfile creation and review, with the residual-risk exception bounded to precisely the approved advisories.</done>
</task>

</tasks>

## Artifacts this phase produces

- `composer.json` with the approved PHP declaration and exact Composer-native advisory policy.
- `01-02-SUMMARY.md` with isolated PHP 8.4 install/audit and ignore-free audit evidence plus exact-version handoff to Phase 2.
- No committed `composer.lock`; CI, runtime validation, and application integration changes remain Phase 3 work.

## Source Coverage Audit

| Source | ID | Required outcome or constraint | Plan | Status | Notes |
|--------|----|--------------------------------|------|--------|-------|
| GOAL | — | Secure, accurately declared PHP 8.2–8.4 graph resolves on real PHP 8.4 without Composer bypasses. | 01-01, 01-02 | COVERED | Tracer proves the exact policy in a clean home; manifest/install/audits prove it without inherited configuration. |
| REQ | COMP-01 | Standard clean PHP 8.4 install has no platform-ignore or emulation route. | 01-02 | COVERED | Disposable real-runtime install uses a fresh COMPOSER_HOME and explicitly scrubbed override variables. |
| REQ | COMP-02 | Root manifest accurately declares PHP 8.2, 8.3, and 8.4 support. | 01-02 | COVERED | Task 1 requires and verifies `require.php: ^8.2`. |
| REQ | COMP-03 | Roave conflict is replaced without weakening platform checks or the security safeguard. | 01-01, 01-02 | COVERED | Exact D-02 policy, configured audit, and ignore-free evidence audit retain the safeguard. |
| RESEARCH | — | Composer 2.10 native policy, stable-only candidate, clean isolation, no dev branch, and Phase 2 lockfile boundary. | 01-01, 01-02 | COVERED | Every constraint is an action, precondition, verify, acceptance criterion, or handoff. |
| CONTEXT | D-01 | Preserve PHP 8.2–8.4, prioritizing PHP 8.4. | 01-01, 01-02 | COVERED | Cited in tracer, manifest action, and real-runtime verification. |
| CONTEXT | D-02 | Replace Roave with blocking native policy and only the exact three internal-only residual-risk IDs, each with expiry. | 01-01, 01-02 | COVERED | Cited in both task actions, manifest assertion, configured audit, and ignore-free evidence audit. |
| CONTEXT | D-03 | Preserve Laravel 11 and existing SendPortal Core composition. | 01-01, 01-02 | COVERED | Both plans require stable Laravel 11 and unchanged Core constraint. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository `composer.json` → Composer 2.10+ policy engine | The manifest determines package candidacy and audit behavior for an external dependency ecosystem. |
| isolated installation → Packagist packages/advisories | A real target-runtime resolution consumes mutable package/advisory metadata and may create an ephemeral lockfile. |
| operator environment/global Composer home → policy engine | Environment or global configuration can add overrides outside the committed manifest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | Composer command/config policy | high | mitigate | Commit `block=true`, `audit=fail`, and only the exact D-02 ignore-id mapping; use fresh COMPOSER_HOME and reject policy/platform override variables. |
| T-01-05 | Tampering | PHP compatibility declaration | high | mitigate | Preserve D-01’s `^8.2` contract and test the candidate on actual PHP 8.4 without `config.platform` emulation. |
| T-01-06 | Tampering | Laravel candidate selection | high | mitigate | Require tagged stable Laravel 11 and stop on a development branch, Core fork, major upgrade, or unrelated bound change. |
| T-01-07 | Elevation of privilege | removal of Roave safeguard | high | mitigate | Run a passing configured locked audit, then an ignore-free reporting audit whose parsed IDs must equal the approved three; reject any broad suppression surface. |
</threat_model>

<verification>
After the manifest edit, run its exact-policy assertion and `composer validate`. For the approved result, reproduce the disposable PHP 8.4 install under a fresh COMPOSER_HOME, run the configured locked audit, then the ignore-free reporting audit and exact-ID parser. Confirm the repository contains no generated lockfile.
</verification>

<success_criteria>
- COMP-01: The approved candidate completes a clean standard install on actual PHP 8.4 without platform emulation, global Composer configuration, or ignore flags.
- COMP-02: `composer.json` declares `^8.2`, which includes PHP 8.2, 8.3, and 8.4.
- COMP-03: Roave no longer blocks the graph, native policy is explicit, only the three owner-approved time-bounded IDs are ignored, and the two audits prove nothing else was silently suppressed.
</success_criteria>

<output>
Create `.planning/phases/01-constraint-resolution-and-security-control/01-02-SUMMARY.md` when done.
</output>

---
phase: 01-constraint-resolution-and-security-control
plan: 09
type: execute
wave: 9
depends_on:
  - 01-08
files_modified:
  - tests/Composer/ComposerPolicyGuardTest.php
  - .github/workflows/ci.yml
autonomous: true
gap_closure: true
requirements:
  - COMP-03
must_haves:
  truths:
    - "A staged supported workflow cannot hide a literal direct Composer mutation inside a shell brace group or supported function body: the audit reaches the nested command or emits a nonempty unclassified failure record."
    - "A staged supported workflow cannot hide direct Composer execution in literal `php -r` process-launch code: the audit recursively applies the existing shell/guard contract or emits a nonempty unclassified failure record."
    - "Literal guarded commands in those compound and inline-PHP forms remain classified through ComposerPolicyCommandContract, while dynamic, malformed, and over-bound forms fail closed with source evidence."
    - "Every existing CI matrix job runs the dependency-free policy-route regression suite and the production route audit before its Composer install, so COMP-03 regressions block CI."
  artifacts:
    - path: tests/Composer/ComposerPolicyGuardTest.php
      provides: "Bounded compound-shell and inline-PHP `-r` route-audit classification with disposable staged regression fixtures and deterministic unclassified evidence."
    - path: .github/workflows/ci.yml
      provides: "A mandatory pre-install policy-route regression gate for every existing CI matrix entry."
  key_links:
    - from: "workflow run scalar containing a brace group or function definition"
      to: "classifyShellRouteSegment()"
      via: "quote-aware bounded compound-body extraction followed by the existing recursive evaluator and Composer contract path"
      pattern: "Nested direct Composer is unsupported and nested guarded Composer is supported; unsupported compound syntax with Composer/evaluator text is unclassified."
    - from: "workflow `php -r` code"
      to: "phpProcessLaunches() and classifyShellRouteSegment()"
      via: "bounded literal process-launch extraction with `php -r` provenance and existing recursive shell classification"
      pattern: "Direct, guarded, nested evaluator, and dynamic inline-PHP paths all leave a deterministic audit record."
    - from: ".github/workflows/ci.yml"
      to: "tests/Composer/ComposerPolicyGuardTest.php"
      via: "dependency-free full suite and explicit production `--route-audit` commands before guarded dependency installation"
      pattern: "The standalone route parser is continuously enforced even though it is outside PHPUnit discovery."
---

<objective>
Close the remaining verified COMP-03 supported-route bypasses in compound shell and inline PHP, then make their dependency-free regression suite a mandatory CI gate.

Purpose: Direct Composer mutations must never disappear from a supported workflow audit merely because they sit inside a brace group, function body, or literal PHP `-r` program.
Output: A narrow bounded parser/test extension in `ComposerPolicyGuardTest.php`, plus a pre-install CI route-audit gate. This plan deliberately does not alter the manifest, Composer guard/runtime contract, PHAR/provenance, lockfile, live Packagist proof, Laravel/Core integration, README, or PHP-version matrix.
Decision trace: D-01 remains the PHP 8.2–8.4 compatibility contract; D-02's blocking native policy and its exact three approved exceptions remain untouched; D-03 confines this work to the existing audit test and CI enforcement rather than expanding dependency or application scope. WR-01 is necessary: the standalone parser suite is outside PHPUnit, so it must execute in CI to make the corrected COMP-03 control persistent.
</objective>

<execution_context>
@/Users/meigire/.codex/gsd-core/workflows/execute-plan.md
@/Users/meigire/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-constraint-resolution-and-security-control/01-CONTEXT.md
@.planning/phases/01-constraint-resolution-and-security-control/01-REVIEW.md
@.planning/phases/01-constraint-resolution-and-security-control/01-VERIFICATION.md
@.planning/phases/01-constraint-resolution-and-security-control/01-07-SUMMARY.md
@.planning/phases/01-constraint-resolution-and-security-control/01-08-SUMMARY.md
@tests/Composer/ComposerPolicyGuardTest.php
@tools/composer/ComposerPolicyCommandContract.php
@bin/composer-policy
@.github/workflows/ci.yml
@README.md
@composer.json

<interfaces>
- `commandChainSegments(string $logicalLine, bool $strict = true): array` is the existing 16,384-byte, 64-segment, quote/escape-aware command-list seam; extend it only enough to preserve a recognized compound body for recursive classification.
- `commandTokenDetails()` retains raw token provenance and shell-dynamic state; `parseInvocation()` normalizes the reviewed wrapper set and routes direct Composer commands through `ComposerPolicyCommandContract::decide()`.
- `classifyShellRouteSegment()` creates source-provenance records and already owns bounded bash/sh/zsh/eval recursion (`MAX_ROUTE_EVALUATOR_DEPTH` and `MAX_ROUTE_EVALUATOR_PAYLOADS`).
- `phpProcessLaunches()` is the existing token-based extractor for literal `proc_open`, `exec`, `system`, `passthru`, and `shell_exec` calls. Reuse it for inline source rather than inventing a second PHP parser.
- `initializeFixtureRepositoryFiles()` creates disposable staged Git roots; adversarial commands belong there so `git ls-files` production evidence cannot be satisfied by test literals.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace a compound workflow command through recursive route classification and CI</name>
  <files>tests/Composer/ComposerPolicyGuardTest.php, .github/workflows/ci.yml</files>
  <read_first>01-VERIFICATION.md `gaps` entries for brace groups and function bodies; 01-REVIEW.md CR-01 and WR-01; 01-08-SUMMARY.md decisions/bounds; tests/Composer/ComposerPolicyGuardTest.php `commandChainSegments`, `commandTokenDetails`, `parseInvocation`, `classifyShellRouteSegment`, `routeAuditFailures`, `auditRoutes`, and fixture helpers; .github/workflows/ci.yml in full; tools/composer/ComposerPolicyCommandContract.php in full.</read_first>
  <behavior>
    - RED 1: A disposable staged workflow containing `{ bash -c 'composer install'; }` produces a nonempty direct unsupported subset and a nonempty `routeAuditFailures()` result; `{ bash -c 'php bin/composer-policy install'; }` produces guarded evidence through the shared command contract.
    - RED 2: A disposable staged workflow containing `runner() { bash -c 'composer install'; }` has nonempty failure evidence, while the corresponding guarded body has a supported guarded record; the fixture may include an invocation of `runner` but the declaration body itself is the audit source.
    - RED 3: Composer/evaluator-bearing malformed, unmatched, over-depth, or unrecognized brace/function shapes produce exactly one source-provenance `unclassified` record and a nonempty failure result rather than an empty record set or an exception.
    - GREEN: The full dependency-free guard suite and its explicit production `--route-audit` both pass after the staged fixtures have demonstrated the direct-versus-guarded behavior.
  </behavior>
  <action>Write the disposable staged Git workflow fixtures and their RED assertions before changing parser code. Assemble fixture command strings from fragments or encoded values so source literals in the test file do not influence production route evidence. Each negative fixture must assert both the expected nonempty record subset and a nonempty `routeAuditFailures()` result; each guarded fixture must assert the record reaches the existing guard/contract classification rather than merely avoiding a failure.

Implement a deliberately bounded compound-shell seam before the current absent-invocation return. Preserve only two exact shell compound forms: a brace group with one matching outer `{ ...; }` body and a named shell-function definition with an identifier, empty `()`, and one matching `{ ...; }` body. Scan quotes and escapes while locating the matching body boundary; keep command-list separators inside the recognized body available to the existing segmentation logic. Apply a named compound nesting bound no greater than the established evaluator-depth bound, the existing logical-line length bound, and the existing per-scalar segment bound. Recursively feed each literal body segment into `classifyShellRouteSegment()` with the same source path, line, scalar, logical command, and shared recursion state, adding a deterministic compound trail entry. This must allow an inner literal `bash -c` payload to use the already-established evaluator recursion and direct-versus-guarded Composer contract.

For a supported production scalar that contains Composer or evaluator text but cannot satisfy this exact compound grammar—unmatched braces, malformed function shape, dynamic text, an exhausted bound, or an unsupported shell compound form—emit one `unclassified` record at the outer source with a nonempty raw segment and a precise bounded-grammar rationale. Do not emulate function invocation, variable expansion, redirection, here-documents, arbitrary shell ASTs, or other shell forms; the required behavior outside the two literal forms is explicit failure evidence.

Resolve WR-01 as required persistent COMP-03 enforcement: add a named dependency-free step after checkout and before `php bin/composer-policy install` in the existing CI job. The step runs `php tests/Composer/ComposerPolicyGuardTest.php` and `php tests/Composer/ComposerPolicyGuardTest.php --route-audit` as separate fail-fast commands for every unchanged PHP 8.2/8.3 matrix entry. Do not change the matrix, container images, install arguments, database jobs, or add PHP 8.4 CI work; those remain Phase 3 scope. Per D-01 through D-03, keep the manifest, guard, command contract, PHAR/provenance, live Packagist test, README, lockfile, vendor tree, and application code out of this task.</action>
  <verify>
    <automated>php -l tests/Composer/ComposerPolicyGuardTest.php && php tests/Composer/ComposerPolicyGuardTest.php --group=route-audit-fail-closed && php tests/Composer/ComposerPolicyGuardTest.php && php tests/Composer/ComposerPolicyGuardTest.php --route-audit && php -r '$ci=file_get_contents(".github/workflows/ci.yml"); exit($ci !== false && preg_match("~name: Verify Composer policy routes[\\s\\S]*php tests/Composer/ComposerPolicyGuardTest\\.php[\\s\\S]*php tests/Composer/ComposerPolicyGuardTest\\.php --route-audit[\\s\\S]*name: Install composer dependencies~", $ci) === 1 ? 0 : 1);'</automated>
  </verify>
  <done>Brace groups and exact function bodies provide direct or guarded nested audit evidence, malformed Composer/evaluator-bearing compounds fail closed with source detail, and every current CI matrix run enforces the standalone route suite before dependency installation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify literal inline PHP process launches and preserve Phase 01 boundaries</name>
  <files>tests/Composer/ComposerPolicyGuardTest.php</files>
  <read_first>01-VERIFICATION.md `gaps` entry for `php -r`; 01-REVIEW.md CR-02; tests/Composer/ComposerPolicyGuardTest.php after Task 1, especially `phpProcessLaunches`, `literalPhpCommandTokens`, `containsComposerExecutableText`, `classifyShellRouteSegment`, route-record shape, compound recursion state, and staged fixtures; 01-07-SUMMARY.md protected-artifact boundary; composer.json, bin/composer-policy, ComposerPolicyCommandContract.php, CI, and README as verification boundaries only.</read_first>
  <behavior>
    - RED 1: A staged workflow `php -r` literal program whose `system()` or `exec()` launches direct Composer yields a nonempty direct unsupported record and nonempty audit failure; the guarded equivalent yields supported guard-contract evidence.
    - RED 2: A literal inline program that launches a literal evaluator command, including `system("bash -c 'composer install'")`, reaches the existing bounded shell/evaluator recursion with an inline-PHP trail.
    - RED 3: Composer/guard-bearing dynamic program text, dynamic process-launch expressions, malformed PHP tokens, unsupported `php -r` argv, an inline program exceeding the route bound, or more literal launches than the named inline-PHP limit yields explicit nonempty `unclassified` failure evidence without executing the program.
    - RED 4: Existing PHP-file process-launch fixtures, shell/evaluator fixtures, production CI/README route records, and the new compound fixtures retain their classifications.
  </behavior>
  <action>Add the staged workflow fixtures and RED assertions first, covering direct and guarded literal `system()`/`exec()` forms plus a literal inline process launch that requires existing shell-evaluator recursion. Include dynamic and malformed variants whose program text carries Composer/guard information; assert an `unclassified` subset with nonempty failure evidence for each negative case. Do not execute fixture workflow code—the test must inspect only staged tracked text through `auditRoutes()`.

Treat exact `php -r <single literal program token>` as an executable-code boundary in `classifyShellRouteSegment()` rather than returning `null` from the existing generic invocation path. Retain the outer source record fields, raw/decoded token provenance, and a `php -r` trail entry. Subject the decoded program to the existing logical-line length limit and a named small literal-launch limit. Prefix only the tokenization input with a PHP opening tag when needed so `token_get_all()` can reuse `phpProcessLaunches()`; preserve the original program in audit evidence. For each bounded literal process-launch result, route its command tokens through the same invocation/contract decision used for PHP files. If those tokens identify an evaluator, recursively call the existing shell classifier with the shared evaluator/compound state so `php -r` cannot become a recursion bypass.

When inline PHP is Composer/guard-bearing but the program or launch expression is dynamic, malformed, exceeds a bound, has unsupported `-r` argv, has no bounded executable launch, or cannot yield literal command tokens, create one deterministic `unclassified-php` record with outer workflow provenance and a precise reason. Do not interpret arbitrary PHP expressions, variables, includes, callbacks, string concatenation, language evaluation, filesystem reads, or runtime execution; the bounded parser recognizes only the existing five process-launch calls and literal command/argv forms.

Run the focused group, full dependency-free suite, and production audit. Reassert D-01/D-02/D-03 through the exact manifest-policy assertion, the protected-artifact diff gate, and root lockfile/vendor absence checks. The only allowed working-tree changes for the completed plan are this test/audit file and the Task 1 CI file; do not repeat live Packagist verification because no dependency source, guard, manifest, PHAR, or runtime input changes.</action>
  <verify>
    <automated>php -l tests/Composer/ComposerPolicyGuardTest.php && php tests/Composer/ComposerPolicyGuardTest.php --group=route-audit-fail-closed && php tests/Composer/ComposerPolicyGuardTest.php && php tests/Composer/ComposerPolicyGuardTest.php --route-audit && php -r '$m=json_decode(file_get_contents("composer.json"),true,512,JSON_THROW_ON_ERROR); $p=$m["config"]["policy"]??[]; $a=$p["advisories"]??[]; $ids=array_keys($a["ignore-id"]??[]); sort($ids); $expected=["PKSA-3r5d-mb8f-1qw9","PKSA-m5cs-t1y6-qpcs","PKSA-mdq4-51ck-6kdq"]; sort($expected); exit(($m["require"]["php"]??null)==="^8.2" && ($m["require"]["laravel/framework"]??null)==="^11.0" && ($m["require"]["mettle/sendportal-core"]??null)==="^3.0" && !isset($m["require-dev"]["roave/security-advisories"], $m["config"]["platform"]) && ($p["ignore-unreachable"]??null)===false && ($a["block"]??null)===true && ($a["audit"]??null)==="fail" && $ids===$expected ? 0 : 1);' && test ! -e composer.lock && test ! -d vendor && git diff --exit-code -- composer.json bin/composer-policy tools/composer/ComposerPolicyCommandContract.php tools/composer/composer-2.10.2.phar tools/composer/composer-2.10.2.phar.sha256 tests/Composer/ComposerPolicyLivePackagistTest.php README.md</automated>
  </verify>
  <done>Literal Composer-bearing `php -r` programs reach the shared route contract or fail explicitly, dynamic/unsupported inline-PHP forms leave deterministic evidence, all parser regressions execute in CI, and the narrow Phase 01 dependency/runtime boundary remains intact.</done>
</task>

</tasks>

## Source Coverage Audit

| Source | ID | Required outcome or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Operators resolve a secure, accurately declared PHP 8.2–8.4 graph without Composer safeguards being bypassed. | 01-09 | COVERED | The verified bypasses are in the supported-route safeguard; this plan closes them without changing the proven resolver/runtime path. |
| REQ | COMP-03 | Roave's replacement must not weaken platform or dependency-security controls. | 01-09 | COVERED | Compound and inline execution routes now either reach the guard contract or fail closed, and CI continuously enforces that result. |
| RESEARCH | — | Keep native policy, real-PHP evidence, stable Laravel/Core, and the narrow Phase 01 artifact boundary. | 01-09 | COVERED | The parser/CI-only edit reasserts these boundaries and does not introduce a dependency, external integration, or broad language parser. |
| CONTEXT | D-01 | Preserve the PHP 8.2–8.4 contract with PHP 8.4 as proof target. | 01-09 | COVERED | Exact manifest assertion is retained; no runtime or matrix change is planned. |
| CONTEXT | D-02 | Retain blocking native policy, audit failure, and exactly the three time-bounded approved IDs. | 01-09 | COVERED | Parser routes nested guarded commands to the existing contract and verification reasserts the policy boundary. |
| CONTEXT | D-03 | Preserve Laravel 11/Core 3 and avoid unrelated refactors. | 01-09 | COVERED | Only the audit test and the required standalone CI invocation change. |
| VERIFICATION | Gap 1 | Brace groups and function bodies must recursively classify or yield explicit failure evidence. | 01-09 Task 1 | COVERED | Disposable staged direct/guarded/malformed fixtures enforce nonempty records and failures. |
| VERIFICATION | Gap 2 | Inline workflow `php -r` must recursively classify literal process launches or yield explicit failure evidence. | 01-09 Task 2 | COVERED | The existing bounded PHP-launch extractor is reused with exact outer-source provenance. |
| REVIEW | WR-01 | Standalone security-route regressions need CI enforcement. | 01-09 Task 1 | COVERED | Necessary because PHPUnit discovery does not execute this file; the gate runs before installation in the current jobs. |
| CONTEXT | Deferred ideas | None. | — | EXCLUDED | CONTEXT.md contains no deferred implementation idea to carry into this gap plan. |
| VERIFICATION | Deferred advisory expiry | Phase 2 lockfile review must remove or explicitly re-approve temporary exceptions. | — | EXCLUDED | Explicitly assigned to Phase 2; no lockfile or advisory-expiry work is pulled into this plan. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| supported workflow scalar → compound-shell extractor | Untrusted workflow text can use valid brace/function syntax to hide an evaluator from a shallow command parser. |
| inline `php -r` text → PHP launch extractor | An inline program is executable code and may spawn Composer through a literal or dynamic process-launch expression. |
| recursive classifier → audit evidence | Parser limits and unsupported syntax must become failure records, not exceptions or silent no-record results. |
| CI configuration → persistent regression enforcement | A standalone security parser has no PHPUnit discovery path and must be explicitly executed to protect future changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-38 | Tampering / elevation of privilege | compound shell evaluator route | critical | mitigate | Recognize only exact literal brace/function bodies, recursively classify their segments, and emit one source-provenance unclassified record for Composer/evaluator-bearing forms outside the grammar. |
| T-01-39 | Tampering / elevation of privilege | inline `php -r` process launch | critical | mitigate | Reuse the literal PHP process-launch extractor, route nested shell commands through the shared contract, and fail closed for dynamic/unbounded/unparseable Composer-bearing code. |
| T-01-40 | Denial of service | compound and inline recursion | high | mitigate | Apply existing logical-line/segment/evaluator limits plus bounded compound nesting and literal-launch counts before descent; transform limit breaches into deterministic records. |
| T-01-41 | Repudiation | staged security regression fixtures | medium | mitigate | Require nonempty records and failures in disposable staged Git roots while preserving outer path, line, scalar, raw/decoded payload, and recursion trail. |
| T-01-42 | Tampering | CI route-security enforcement | high | mitigate | Add an explicit full-suite and production-audit step before guarded install in every unchanged matrix job, so parser bypasses fail CI. |
| T-01-43 | Tampering | Phase 01 dependency/runtime artifacts | high | mitigate | Enforce exact manifest checks, protected-artifact diff gate, and root lockfile/vendor absence after parser/CI changes. |
</threat_model>

<verification>
Run the focused route-audit group, the complete dependency-free guard suite, and the production `--route-audit`. The staged compound and inline-PHP fixtures must prove both positive guarded evidence and nonempty direct/unclassified failure evidence. Check the workflow ordering for the two pre-install commands, then run the exact D-01/D-02/D-03 manifest assertion, protected-artifact diff gate, and root artifact absence checks.
</verification>

<success_criteria>
- COMP-03 direct Composer execution in a supported brace group, exact function body, or literal inline-PHP process launch cannot yield zero records and zero failures.
- Literal guarded equivalents are identified by the same command contract as top-level routes; dynamic, malformed, unrecognized, or over-bound forms fail explicitly with useful source evidence.
- CI invokes the standalone regression suite and production route audit before each current guarded installation, preventing local-only safety coverage.
- The plan adds no generic shell/PHP interpreter and leaves the phase's dependency, manifest, PHAR, lockfile, live-network, Laravel/Core, README, and matrix scope intact.
</success_criteria>

<output>
Create `.planning/phases/01-constraint-resolution-and-security-control/01-09-SUMMARY.md` when done.
</output>

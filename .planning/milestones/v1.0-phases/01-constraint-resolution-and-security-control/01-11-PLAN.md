---
phase: 01-constraint-resolution-and-security-control
plan: 11
type: execute
wave: 11
depends_on:
  - 01-10
files_modified:
  - tests/Composer/ComposerPolicyGuardTest.php
autonomous: true
gap_closure: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
must_haves:
  truths:
    - "Every marker-bearing Composer script handler in the root composer.json has finite event-and-handler provenance and produces supported, unsupported, or unclassified audit evidence before JSON is otherwise excluded; direct Composer and @composer handlers cannot return zero records."
    - "The documented non-Composer Laravel script handlers in composer.json remain accepted as non-candidates, and the production route audit retains only the existing guarded CI/README evidence."
    - "Every Composer, guard, or evaluator-marker-bearing supported scripts/*.php program produces explicit source-provenanced unclassified PHP evidence when bounded direct-launch extraction produces no record, including callable, variable-function, and unmodeled process-dispatch forms."
    - "Direct Composer script/PHP fixtures make both the audit record set and routeAuditFailures() nonempty, while literal guarded handlers use ComposerPolicyCommandContract and remain supported where the command is canonical."
    - "The closure changes only the dependency-free route-audit test source and performs no Composer script execution, arbitrary Composer-script/data-flow interpretation, arbitrary PHP evaluation, dependency/manifest/lockfile/PHAR change, CI runtime-matrix change, or live test."
  artifacts:
    - path: tests/Composer/ComposerPolicyGuardTest.php
      provides: "A finite root-Composer-script provenance extractor, script-handler rejection path, and unconditional marker-bearing PHP program fallback covered by disposable staged Git fixtures."
  key_links:
    - from: "composer.json top-level scripts event handlers"
      to: "auditRoutes() and routeAuditFailures()"
      via: "bounded event/handler extraction before the generic JSON exclusion, script-prefix normalization, and ComposerPolicyCommandContract classification"
      pattern: "composer-script source provenance"
    - from: "scripts/*.php callable, variable-function, and popen dispatch programs"
      to: "auditRoutes() PHP program finalization"
      via: "one unclassified-php source record whenever a marker-bearing program leaves no bounded child record"
      pattern: "marker-bearing PHP no-record fallback"
    - from: "literal guarded Composer script/PHP fixture"
      to: "ComposerPolicyCommandContract::decide()"
      via: "the existing canonical validate/audit/install/update decision seam"
      pattern: "ComposerPolicyCommandContract"
---

<objective>
Close the two remaining verified Phase 01 route-audit completeness gaps as one bounded provenance batch: Composer script hooks and indirect/unmodeled PHP process dispatch.

Purpose: A script-enabled guarded Composer install and a supported PHP script must not conceal a direct Composer mutation by producing no audit evidence.
Output: One dependency-free test/audit source that extracts root Composer script-handler provenance, rejects direct/@composer script forms, preserves documented Laravel handlers, and finalizes every marker-bearing supported PHP program with evidence.
Decision trace: D-01 preserves the PHP 8.2–8.4 contract; D-02 preserves the native blocking/audit safeguard and its exact advisory boundary; D-03 confines this closure to audit evidence without changing Laravel 11, SendPortal Core, or application behavior.
</objective>

<execution_context>
@/Users/meigire/.codex/gsd-core/workflows/execute-plan.md
@/Users/meigire/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-constraint-resolution-and-security-control/01-CONTEXT.md
@.planning/phases/01-constraint-resolution-and-security-control/01-RESEARCH.md
@.planning/phases/01-constraint-resolution-and-security-control/01-07-SUMMARY.md
@.planning/phases/01-constraint-resolution-and-security-control/01-10-SUMMARY.md
@.planning/phases/01-constraint-resolution-and-security-control/01-10-PLAN.md
@.planning/phases/01-constraint-resolution-and-security-control/01-VERIFICATION.md
@.planning/debug/route-audit-completeness.md
@composer.json
@tests/Composer/ComposerPolicyGuardTest.php
@tools/composer/ComposerPolicyCommandContract.php
@bin/composer-policy
@.github/workflows/ci.yml

<interfaces>
`auditRoutes(string $repositoryRoot): array` is the only production audit entry point. It enumerates staged tracked files, dispatches a provenance kind, and `routeAuditFailures()` consumes its records.

`routeSourceKind()` currently marks all JSON non-source before candidate finalization. The new Composer-script path is limited to the root `composer.json` top-level `scripts` object; every extracted record must retain path, physical line, event key, handler ordinal, raw handler text, and normalized route evidence.

`phpProcessLaunches()` remains a lexical extractor for five literal APIs only. `auditRoutes()` must finalize a marker-bearing supported PHP program even when that extractor returns no launch; no callable/data-flow evaluator is introduced.

`ComposerPolicyCommandContract::decide()` remains the sole canonical-command approval seam for literal guarded forms.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one root Composer post-install script handler into finite, source-provenanced audit evidence</name>
  <reversibility rating="reversible">The extractor and route records are internal dependency-free test-audit behavior; reverting them changes no manifest, runtime dependency, public API, or persisted data.</reversibility>
  <files>tests/Composer/ComposerPolicyGuardTest.php</files>
  <read_first>01-VERIFICATION.md `gaps` and `Key Link Verification`; composer.json `scripts`; tests/Composer/ComposerPolicyGuardTest.php routeSourceKind, routeAuditMarker, auditRoutes, workflow/Docker scalar extraction, initializeFixtureRepositoryFiles, and routeAuditFailures; ComposerPolicyCommandContract.php; 01-10-SUMMARY.md decisions and validation evidence.</read_first>
  <behavior>
    - RED 1: A disposable staged root composer.json whose post-install-cmd handler is a direct Composer install produces a nonempty record set and nonempty routeAuditFailures() result; its record names composer.json, the post-install-cmd event, the handler ordinal, physical source line, raw handler, operation, and unsupported classification.
    - RED 2: A disposable staged post-install-cmd @composer update handler produces the same nonempty evidence/failure outcome and is classified as a direct Composer mutation according to Composer script-event semantics.
    - RED 3: A literal @php bin/composer-policy install script handler produces supported evidence only through ComposerPolicyCommandContract; a direct guarded spelling follows the same bounded normalization.
    - RED 4: A marker-bearing scripts object entry outside the finite scalar-or-list-of-scalars shape produces one unclassified Composer-script record with event/handler provenance and a nonempty failure result.
    - GREEN: The real root composer.json continues to produce no route record for Illuminate ComposerScripts, artisan discovery/key generation, or the literal environment-copy handler, and the production audit retains exactly the existing guarded CI/README records.
  </behavior>
  <action>Write the disposable staged Git regressions before changing the audit and demonstrate the present zero-record/zero-failure result for the direct and @composer post-install handlers. Never run Composer, invoke a script hook, create vendor, or rely on a live dependency graph: each fixture is text committed only into a temporary Git root and is inspected solely by auditRoutes(). Assert negative scenarios with explicit nonempty record and failure checks rather than a static source-shape assertion.

Add one finite composer.json script-provenance extractor ahead of the generic JSON exclusion in auditRoutes(). Limit it to the root path `composer.json`; parse JSON only to locate the top-level scripts map and accept only a scalar string handler or an ordered list of scalar-string handlers. Preserve the event key and zero-based handler ordinal, derive the physical handler line from the original JSON text with a bounded, deterministic locator, retain the raw handler, and emit source-provenanced unclassified evidence when a marker-bearing scripts entry cannot meet that finite shape or line-locator bound. Do not traverse dependency manifests, execute hooks, expand aliases/environment variables, follow Composer script callbacks, or implement arbitrary Composer-script data flow.

Normalize only the documented literal handler prefixes required by the fixtures: a direct Composer executable, the Composer @composer shortcut, PHP/@php followed by the existing repository guard, and the direct guarded form. Treat @composer as Composer's current executable shortcut for classification, not as an approved guarded route. Feed the resulting literal argv through the existing parser and ComposerPolicyCommandContract decision; direct Composer install/update is unsupported, while a canonical literal guard command is supported. For a marker-bearing handler outside this bounded prefix/argv grammar, append one unclassified Composer-script record rather than skipping it. Existing Laravel handlers are non-candidates because they do not match a bounded Composer/guard/evaluator executable marker; preserve them and preserve the generic JSON exclusion for every other JSON file. Per D-01, D-02, and D-03, leave the manifest contents, policy, PHP/Composer runtime behavior, guard, contract, CI, PHAR/provenance, Laravel/Core integration, and live tests untouched.
  </action>
  <verify>
    <automated>php -l tests/Composer/ComposerPolicyGuardTest.php &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php --group=route-audit-fail-closed &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php --route-audit &amp;&amp; php -r '$m=json_decode(file_get_contents("composer.json"),true,512,JSON_THROW_ON_ERROR); $scripts=$m["scripts"]??null; exit(is_array($scripts) &amp;&amp; isset($scripts["post-autoload-dump"], $scripts["post-root-package-install"], $scripts["post-create-project-cmd"]) ? 0 : 1);' &amp;&amp; test ! -e composer.lock &amp;&amp; test ! -d vendor</automated>
  </verify>
  <done>Root Composer script hooks are audited by finite event-and-handler provenance before JSON exclusion: direct and @composer mutations fail with nonempty evidence, canonical guarded literals use the shared contract, malformed marker-bearing entries fail closed, and the existing Laravel script handlers remain non-candidates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finalize every marker-bearing supported PHP program when indirect dispatch has no bounded launch record</name>
  <reversibility rating="reversible">The fallback is local to the dependency-free audit record finalization and can be removed without changing Composer configuration, execution behavior, or any external interface.</reversibility>
  <files>tests/Composer/ComposerPolicyGuardTest.php</files>
  <read_first>01-VERIFICATION.md `gaps`, `Behavioral Spot-Checks`, and `Anti-Patterns Found`; .planning/debug/route-audit-completeness.md `Required invariant`, `PHP programs`, and `Precise exclusions`; tests/Composer/ComposerPolicyGuardTest.php phpProcessLaunches, routeAuditMarker, classifyInlinePhpRouteSegment, auditRoutes PHP branch, and the current scripts/dependencies.php fixture; 01-10-PLAN.md Task 2 PHP exclusions; ComposerPolicyCommandContract.php.</read_first>
  <behavior>
    - RED 1: Each disposable staged scripts/*.php program using call_user_func with a system callable and a direct Composer literal, a variable function carrying that callable/command, or popen with a direct Composer literal returns a nonempty record set containing source path/line/raw program evidence and a nonempty routeAuditFailures() result.
    - RED 2: Each of those fixtures emits one unclassified-php source-level fallback, not a supported/direct record inferred from callable or variable data flow; no fixture PHP is executed.
    - RED 3: The existing five literal process APIs continue to classify direct Composer as unsupported and a literal guarded array form as supported through ComposerPolicyCommandContract.
    - RED 4: A marker-bearing supported PHP source with no direct API match receives the same fallback; an unmarked PHP source remains outside this detector.
    - GREEN: The full staged PHP matrix and production route audit pass with no temporary fixture path in production evidence, no additional production Composer-script/PHP record from the existing tree, and unchanged guarded CI/README records.
  </behavior>
  <action>Extend the existing disposable staged scripts/dependencies.php matrix as a complete regression batch, not as isolated examples. Build source text from fragments or fixture payload data so the production audit over the real test file cannot mistake the fixture definitions for tracked route evidence. For each reproduced indirect/unmodeled form, assert records are nonempty, routeAuditFailures() is nonempty, record classification is unclassified, executable is unclassified-php, and evidence includes the staged source path, physical marker line, and raw source/program context. Add a separate unmarked control to prove the fallback is marker-gated. Do not execute PHP, Composer, a callback, a popen handle, or any fixture command.

Keep phpProcessLaunches() deliberately limited to its present five literal APIs. In auditRoutes()' supported-PHP branch, record the number of records before processing the program and, whenever routeAuditMarker() finds a Composer/guard/evaluator marker yet no child audit record was emitted, append exactly one source-provenanced unclassified-php fallback. Apply it unconditionally with respect to whether phpProcessLaunches() found a direct API; this closes callable dispatch, variable functions, popen, and every other unmodeled marker-bearing process expression without trying to evaluate callbacks, variables, constants, concatenation, includes, autoloading, reflection, arbitrary process APIs, or PHP data flow. Retain current per-launch literal classification and inline PHP behavior so valid direct and guard cases preserve their existing records.

Ensure the PHP source fallback uses the existing physical source line/record fields and an actionable rationale that the marker-bearing program had no bounded process-launch record. Avoid duplicate fallback records when a recognized literal or already-unclassified child record exists. Keep this fallback inside supported PHP provenance; source-kind dispatch still handles unknown marker-bearing sources, and planning/test material remains excluded from production evidence. Preserve Task 1's Composer-script semantics and the existing Laravel script hooks. Per D-01, D-02, and D-03, do not alter composer.json, Composer policy, the guard, command contract, PHAR/provenance, CI configuration or matrix, README, live tests, lockfile, vendor tree, Laravel/Core integration, or application source.
  </action>
  <verify>
    <automated>php -l tests/Composer/ComposerPolicyGuardTest.php &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php --group=route-audit-fail-closed &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php &amp;&amp; php tests/Composer/ComposerPolicyGuardTest.php --route-audit &amp;&amp; php -r '$m=json_decode(file_get_contents("composer.json"),true,512,JSON_THROW_ON_ERROR); $p=$m["config"]["policy"]??[]; $a=$p["advisories"]??[]; $ids=array_keys($a["ignore-id"]??[]); sort($ids); $expected=["PKSA-3r5d-mb8f-1qw9","PKSA-m5cs-t1y6-qpcs","PKSA-mdq4-51ck-6kdq"]; sort($expected); exit(($m["require"]["php"]??null)==="^8.2" &amp;&amp; ($m["require"]["laravel/framework"]??null)==="^11.0" &amp;&amp; ($m["require"]["mettle/sendportal-core"]??null)==="^3.0" &amp;&amp; !isset($m["require-dev"]["roave/security-advisories"], $m["config"]["platform"]) &amp;&amp; ($p["ignore-unreachable"]??null)===false &amp;&amp; ($a["block"]??null)===true &amp;&amp; ($a["audit"]??null)==="fail" &amp;&amp; $ids===$expected ? 0 : 1);' &amp;&amp; php -r '$ci=file_get_contents(".github/workflows/ci.yml"); exit($ci !== false &amp;&amp; preg_match("~php tests/Composer/ComposerPolicyGuardTest\\.php[\\s\\S]*php tests/Composer/ComposerPolicyGuardTest\\.php --route-audit[\\s\\S]*php bin/composer-policy install~", $ci) === 1 ? 0 : 1);' &amp;&amp; test ! -e composer.lock &amp;&amp; test ! -d vendor &amp;&amp; git diff --exit-code -- composer.json bin/composer-policy tools/composer/ComposerPolicyCommandContract.php tools/composer/composer-2.10.2.phar tools/composer/composer-2.10.2.phar.sha256 .github/workflows/ci.yml README.md tests/Composer/ComposerPolicyLivePackagistTest.php</automated>
  </verify>
  <done>All reproduced indirect/unmodeled marker-bearing PHP dispatch forms have deterministic staged source evidence and failures; the audit makes no arbitrary PHP/data-flow claim, does not execute fixtures, preserves literal/shared-contract behavior, and cannot silently pass a marker-bearing supported PHP program with zero records.</done>
</task>

</tasks>

## Source Coverage Audit

| Source | ID | Required outcome or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Operators resolve on PHP 8.4 without a Composer safeguard bypass. | 01-11 | COVERED | Closes two demonstrated script-enabled/direct-dispatch audit bypasses without changing the resolved graph. |
| REQ | COMP-01 | The clean guarded PHP 8.4 install remains non-bypassed. | 01-11 | COVERED | No install/live test is run; lock/vendor absence and protected dependency boundary are asserted. |
| REQ | COMP-02 | PHP 8.2–8.4 metadata remains accurate. | 01-11 | COVERED | Verification reasserts the unchanged root PHP constraint. |
| REQ | COMP-03 | Roave replacement retains platform and dependency-security controls. | 01-11 | COVERED | Composer-script and PHP dispatch paths cannot hide a direct dependency mutation from the audit. |
| RESEARCH | Architecture responsibility map | Keep manifest policy in Composer, locked audit in Phase 2, and runtime integration in Phase 3. | 01-11 | COVERED | Only the dependency-free audit test changes; no manifest/runtime work is planned. |
| RESEARCH | Do-not-hand-roll constraint | Do not invent dependency policy or resolver behavior. | 01-11 | COVERED | Literal guarded forms delegate solely to the existing command contract; no Composer execution or script interpreter is added. |
| RESEARCH | Phase boundary | No lockfile, platform emulation, Laravel/Core major change, or unrelated refactor. | 01-11 | COVERED | Protected-artifact/no-lock/vendor checks and one-file ownership enforce the boundary. |
| CONTEXT | D-01 | Preserve PHP 8.2–8.4 with PHP 8.4 as target. | 01-11 | COVERED | Both tasks retain and verify the root `^8.2` contract. |
| CONTEXT | D-02 | Preserve native blocking/audit policy and exact time-bounded exceptions. | 01-11 | COVERED | Both tasks retain the current manifest policy and direct only literal guarded commands to the existing contract. |
| CONTEXT | D-03 | Preserve Laravel 11/Core 3 and avoid unrelated application work. | 01-11 | COVERED | The sole modified file is the dependency-free audit test source. |
| VERIFICATION | Gap 1 | Composer scripts need finite provenance and direct/@composer rejection. | 01-11 Task 1 | COVERED | Root scripts extraction precedes JSON exclusion; staged direct, shortcut, guarded, malformed, and Laravel-preservation cases prove outcomes. |
| VERIFICATION | Gap 2 | Callable, variable, and popen PHP dispatch need no-record failure evidence. | 01-11 Task 2 | COVERED | An unconditional marker-bearing PHP finalizer and three staged scripts/*.php classes prove nonempty failures. |
| DEBUG | Required invariant | Marker-bearing candidates must not silently emit zero records. | 01-11 Task 1–2 | COVERED | Event-handler and PHP-program fallbacks each emit deterministic source evidence. |
| CONTEXT | Deferred ideas | None. | — | EXCLUDED | CONTEXT.md has no deferred implementation item. |
| VERIFICATION | Temporary advisory expiry | Phase 2 lockfile review. | — | EXCLUDED | Explicitly assigned to Phase 2; no lockfile/policy change occurs here. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| composer.json script event → Composer execution | Script handlers can invoke the Composer executable during a documented script-enabled guarded install. |
| supported scripts/*.php source → route-audit record | Callable, variable-function, and unmodeled process expressions can conceal a direct Composer mutation from a direct-API-only extractor. |
| literal route candidate → ComposerPolicyCommandContract | Only canonical literal guarded commands may be supported; direct/shortcut forms must remain rejected. |
| staged fixtures → production audit inventory | Test payloads must never become tracked production route evidence or be executed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-52 | Tampering / elevation of privilege | Composer script handler provenance | critical | mitigate | Extract only finite root scripts event handlers before JSON exclusion, preserve event/line/ordinal evidence, and reject direct/@composer or malformed marker-bearing entries. |
| T-01-53 | Tampering / elevation of privilege | indirect PHP process dispatch | critical | mitigate | Finalize every marker-bearing supported PHP program with one unclassified-php record when bounded direct-launch extraction leaves no child record. |
| T-01-54 | Tampering | guarded script normalization | high | mitigate | Route literal guard handlers through ComposerPolicyCommandContract; never infer approval from a shortcut, callback, or variable value. |
| T-01-55 | Repudiation | source audit evidence | medium | mitigate | Retain root path, physical line, script event, handler ordinal, raw handler/program, and explicit rationale in staged records. |
| T-01-56 | Denial of service / tampering | parser scope | high | mitigate | Keep JSON/script and PHP extraction finite and marker-gated; reject unsupported forms rather than executing or interpreting arbitrary scripts/PHP. |
| T-01-57 | Tampering | Phase 01 dependency/runtime boundary | high | mitigate | Run dependency-free tests only and assert unchanged manifest/policy, guard/PHAR/CI artifacts, and absent root lockfile/vendor. |
</threat_model>

<verification>
Run the focused route-audit group, complete dependency-free suite, and production audit. In disposable staged repositories, require nonempty records and failures for direct Composer, @composer, malformed marker-bearing script entries, call_user_func, variable-function, and popen PHP forms; require shared-contract support only for canonical literal guard forms. Confirm the current Laravel Composer scripts remain non-candidates, the real production record set stays guarded CI/README evidence, no fixture path enters production output, the CI audit remains before install, and protected dependency/runtime artifacts stay unchanged.
</verification>

<success_criteria>
- Every marker-bearing root Composer script handler has finite source provenance and a supported, unsupported, or unclassified record before the general JSON skip applies.
- Every marker-bearing supported PHP program ends with at least one record, even if its process dispatch is callable, indirect, or unmodeled; unsupported PHP remains source-evidence only.
- Negative staged forms prove both a nonempty audit record set and a nonempty route failure set; guarded literals use the existing command contract.
- Existing Laravel script handlers, guarded CI/README production records, Composer policy, lockfile boundary, PHAR provenance, CI runtime matrix, and host/Core behavior remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/01-constraint-resolution-and-security-control/01-11-SUMMARY.md` when done.
</output>

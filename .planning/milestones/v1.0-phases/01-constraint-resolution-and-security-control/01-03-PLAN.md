---
phase: 01-constraint-resolution-and-security-control
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - composer.json
autonomous: true
gap_closure: true
requirements:
  - COMP-03
must_haves:
  truths:
    - "D-01: Composer fails closed when a policy source is unreachable during an isolated real-PHP-8.4 update or install while retaining the PHP 8.2–8.4 declaration."
    - "D-02: The manifest retains blocking advisory policy and its exact three documented advisory exceptions while adding the fail-closed policy control."
    - "D-03: A clean script-enabled standard Composer install and configured audit continue to complete on real PHP 8.4 without platform or policy bypasses or a Laravel/Core change."
  artifacts:
    - path: "composer.json"
      provides: "A native Composer policy with `ignore-unreachable: false`, blocking advisory handling, audit failure, and only D-02's approved per-ID exceptions."
    - path: ".planning/phases/01-constraint-resolution-and-security-control/01-03-SUMMARY.md"
      provides: "Reproducible real-PHP-8.4 manifest/install/audit and intentional unreachable-policy-source failure evidence."
  key_links:
    - from: "composer.json config.policy.ignore-unreachable"
      to: "Composer update and install policy-source handling"
      via: "boolean false evaluated from an isolated disposable manifest with a deliberately unreachable HTTPS custom-policy source"
      pattern: "Both operations return a nonzero status instead of accepting incomplete policy data."
    - from: "composer.json config.policy.advisories"
      to: "Composer resolver and composer audit --locked"
      via: "block=true, audit=fail, and only the three D-02 ignore-id entries"
      pattern: "The configured audit remains successful only for the documented, time-bounded exceptions."
---

<objective>
Close the verified COMP-03 fail-open path by making Composer policy-source outages fail closed, without changing Laravel/Core constraints, widening advisory exceptions, or creating a repository lockfile.

Purpose: Preserve D-02's native dependency-security replacement when Packagist policy or repository data is unavailable during the operations operators use to resolve and install dependencies.
Output: `composer.json` gains the fail-closed native Composer setting, and the plan summary records an isolated real-PHP-8.4 standard install/audit plus deliberate update/install outage failures.
Decision trace: D-01: retain the PHP 8.2–8.4 declaration and prove on real PHP 8.4; D-02: preserve the blocking native policy and exactly its three approved residual-risk IDs; D-03: add only the policy control while preserving Laravel 11 and SendPortal Core.
</objective>

<execution_context>
@/Users/meigire/.codex/gsd-core/workflows/execute-plan.md
@/Users/meigire/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-constraint-resolution-and-security-control/01-CONTEXT.md
@.planning/phases/01-constraint-resolution-and-security-control/01-RESEARCH.md
@.planning/phases/01-constraint-resolution-and-security-control/01-PATTERNS.md
@.planning/phases/01-constraint-resolution-and-security-control/01-02-SUMMARY.md
@.planning/phases/01-constraint-resolution-and-security-control/01-VERIFICATION.md
@composer.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end fail-closed Composer policy on the normal PHP 8.4 path</name>
  <reversibility rating="reversible">The local manifest boolean can be restored in one configuration file, with no schema, wire format, or published package interface change.</reversibility>
  <read_first>composer.json; .planning/phases/01-constraint-resolution-and-security-control/01-VERIFICATION.md sections “Gaps Summary” and “Behavioral Spot-Checks”; 01-CONTEXT.md decisions D-01 through D-03; 01-02-SUMMARY.md section “Isolated PHP 8.4 Evidence”; 01-RESEARCH.md sections “Standard Stack” and “Security Domain”.</read_first>
  <precondition>The recorded Herd Composer executable `/Users/meigire/Library/Application Support/Herd/bin/composer` exists, runs under PHP 8.4.x, reports Composer 2.10.x-or-newer 2.x, and Packagist is reachable for the clean standard-install proof.</precondition>
  <files>composer.json</files>
  <action>In the existing `config.policy` object, add the exact native Composer key `ignore-unreachable` with boolean value `false` beside the existing `advisories` policy. Per D-02, preserve `advisories.block: true`, `advisories.audit: fail`, and exactly the existing three `ignore-id` mappings; each value must retain its internal-only, project-owner accepted-risk, Phase-2-lockfile-review, and compatible-stable-SendPortal-Core expiry rationale. Per D-01, preserve `require.php: ^8.2`; per D-03, preserve `laravel/framework: ^11.0`, `mettle/sendportal-core: ^3.0`, every unrelated manifest key, and all host source. Run the quick exact-policy assertion and strict validation before starting the networked disposable install. Then create a `mktemp -d` copy excluding `.git`, `vendor`, and `composer.lock`, with a newly created empty `composer-home`. For every Composer command, use `php '/Users/meigire/Library/Application Support/Herd/bin/composer'` with `COMPOSER`, `COMPOSER_HOME`, `COMPOSER_POLICY`, `COMPOSER_NO_BLOCKING`, `COMPOSER_NO_SECURITY_BLOCKING`, `COMPOSER_IGNORE_PLATFORM_REQ`, and `COMPOSER_IGNORE_PLATFORM_REQS` removed before setting only the temporary `COMPOSER_HOME`. In the copied application run strict validation, the script-enabled standard `composer install --prefer-dist --no-interaction --no-progress`, and configured `composer audit --locked`; record PHP/Composer versions and selected tagged Laravel 11/Core versions in the summary. Keep all generated artifacts in the disposable directory and confirm the repository has no `composer.lock`.</action>
  <verify>
    <automated>zsh -ceu 'repo="$PWD"; composer_bin="/Users/meigire/Library/Application Support/Herd/bin/composer"; test -x "$composer_bin"; php -r "exit(version_compare(PHP_VERSION, \"8.4.0\", \">=\") &amp;&amp; version_compare(PHP_VERSION, \"8.5.0\", \"&lt;\") ? 0 : 1);"; php "$composer_bin" --version | grep -E "Composer version 2\\.(10|[1-9][0-9])\\."; php -r "\$m=json_decode(file_get_contents(\"composer.json\"),true,512,JSON_THROW_ON_ERROR); \$p=\$m[\"config\"][\"policy\"]??[]; \$a=\$p[\"advisories\"]??[]; \$expected=[\"PKSA-3r5d-mb8f-1qw9\",\"PKSA-m5cs-t1y6-qpcs\",\"PKSA-mdq4-51ck-6kdq\"]; \$reasons=\$a[\"ignore-id\"]??[]; \$actual=array_keys(\$reasons); sort(\$expected); sort(\$actual); \$reasonsAreDocumented=true; foreach(\$expected as \$id){\$reason=\$reasons[\$id]??null; \$reasonsAreDocumented=\$reasonsAreDocumented &amp;&amp; is_string(\$reason) &amp;&amp; str_contains(\$reason,\"internal-only\") &amp;&amp; str_contains(\$reason,\"project owner\") &amp;&amp; str_contains(\$reason,\"Phase 2 lockfile review\") &amp;&amp; str_contains(\$reason,\"compatible stable SendPortal Core\");} exit((\$p[\"ignore-unreachable\"]??null)===false &amp;&amp; (\$a[\"block\"]??null)===true &amp;&amp; (\$a[\"audit\"]??null)===\"fail\" &amp;&amp; \$actual===\$expected &amp;&amp; \$reasonsAreDocumented &amp;&amp; !isset(\$m[\"config\"][\"platform\"],\$m[\"config\"][\"audit\"],\$a[\"ignore\"],\$a[\"ignore-severity\"])?0:1);"; tmp="$(mktemp -d)"; trap "rm -rf -- $tmp" EXIT; mkdir "$tmp/composer-home"; rsync -a --exclude=.git --exclude=vendor --exclude=composer.lock "$repo/" "$tmp/app/"; cd "$tmp/app"; env -u COMPOSER -u COMPOSER_HOME -u COMPOSER_POLICY -u COMPOSER_NO_BLOCKING -u COMPOSER_NO_SECURITY_BLOCKING -u COMPOSER_IGNORE_PLATFORM_REQ -u COMPOSER_IGNORE_PLATFORM_REQS COMPOSER_HOME="$tmp/composer-home" php "$composer_bin" validate --strict --no-check-publish; env -u COMPOSER -u COMPOSER_HOME -u COMPOSER_POLICY -u COMPOSER_NO_BLOCKING -u COMPOSER_NO_SECURITY_BLOCKING -u COMPOSER_IGNORE_PLATFORM_REQ -u COMPOSER_IGNORE_PLATFORM_REQS COMPOSER_HOME="$tmp/composer-home" php "$composer_bin" install --prefer-dist --no-interaction --no-progress; env -u COMPOSER -u COMPOSER_HOME -u COMPOSER_POLICY -u COMPOSER_NO_BLOCKING -u COMPOSER_NO_SECURITY_BLOCKING -u COMPOSER_IGNORE_PLATFORM_REQ -u COMPOSER_IGNORE_PLATFORM_REQS COMPOSER_HOME="$tmp/composer-home" php "$composer_bin" audit --locked; php -r "\$l=json_decode(file_get_contents(\"composer.lock\"),true,512,JSON_THROW_ON_ERROR); foreach(array_merge(\$l[\"packages\"]??[],\$l[\"packages-dev\"]??[]) as \$p){if((\$p[\"name\"]??null)===\"laravel/framework\"){exit(preg_match(\"/^v?11\\.[0-9]+\\.[0-9]+$/\",\$p[\"version\"]??\"\")===1?0:1);}} exit(1);"; test ! -e "$repo/composer.lock"'</automated>
  </verify>
  <acceptance_criteria>The committed manifest validates strictly and exposes `config.policy.ignore-unreachable` as boolean `false`. The exact D-02 advisory map retains its per-ID internal-only, project-owner, Phase-2-lockfile-review, and compatible-stable-Core rationale; PHP 8.2–8.4, Laravel 11, and SendPortal Core constraints remain intact. An isolated real-PHP-8.4 script-enabled install and configured locked audit succeed without platform emulation, inherited Composer policy, or ignore flags, and no lockfile is created in the repository.</acceptance_criteria>
  <done>The normal operator path has a valid, fail-closed Composer policy and current PHP 8.4 install/audit evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Prove update and install reject an unreachable isolated policy source</name>
  <read_first>composer.json after Task 1; .planning/phases/01-constraint-resolution-and-security-control/01-VERIFICATION.md gap reason and “Advisory-source outage” spot-check; 01-02-SUMMARY.md environment-scrubbing pattern; 01-RESEARCH.md “Security Domain”; Composer configuration reference section `policy.ignore-unreachable`.</read_first>
  <precondition>Task 1's strict manifest assertion and clean PHP 8.4 install/audit passed, and the repository still lacks `composer.lock`.</precondition>
  <files>composer.json</files>
  <action>Create two independent disposable copies of the Task 1 manifest, each with a separate new empty `COMPOSER_HOME`. In each copy only, add a custom policy named `gap-closure-outage-probe` containing `block: true`, `audit: fail`, and `sources: [{"type":"url","url":"https://127.0.0.1:9/sendportal-policy-outage"}]`; retain the copied root `ignore-unreachable: false` and all advisory-policy entries. Strictly validate each newly written probe manifest before starting its networked Composer operation. This loopback HTTPS endpoint intentionally has no listener. For the probe validation and operation commands, unset upper- and lower-case HTTP(S)/ALL proxy variables and set both `NO_PROXY` and `no_proxy` to `127.0.0.1,localhost`, so the custom-policy request bypasses proxies and cannot disclose package metadata to an external proxy. Run `composer update --dry-run --prefer-dist --no-interaction --no-scripts --no-progress` in the first copy and `composer install --prefer-dist --no-interaction --no-scripts --no-progress` in the second with the same Herd PHP 8.4 Composer executable and the complete scrubbed environment from Task 1. Capture each command's status and output; each must be nonzero, and one blank-line-delimited Composer error stanza in that operation's own log must contain both the exact configured loopback HTTPS URL and a recognized connection/transport cause. Treat any other failure classification or a zero status as a security-control regression. Remove both temporary copies on exit, assert the repository manifest contains neither the probe policy nor a custom `sources` policy, and reassert the exact D-02 three-ID map and each reason's internal-only, project-owner, Phase-2-lockfile-review, and compatible-stable-Core rationale. Record both failed-operation outputs/statuses in the summary. Per D-02 and D-03, the intentional test configuration is disposable evidence only; it must not alter the approved three-ID exception, Laravel/Core bounds, repository sources, or lockfile boundary.</action>
  <verify>
    <automated>zsh -ceu 'repo="$PWD"; composer_bin="/Users/meigire/Library/Application Support/Herd/bin/composer"; tmp="$(mktemp -d)"; trap "rm -rf -- $tmp" EXIT; for operation in update install; do mkdir -p "$tmp/$operation/home"; rsync -a --exclude=.git --exclude=vendor --exclude=composer.lock "$repo/" "$tmp/$operation/app/"; cd "$tmp/$operation/app"; php -r "\$m=json_decode(file_get_contents(\"composer.json\"),true,512,JSON_THROW_ON_ERROR); \$m[\"config\"][\"policy\"][\"gap-closure-outage-probe\"]=[\"block\"=&gt;true,\"audit\"=&gt;\"fail\",\"sources\"=&gt;[[\"type\"=&gt;\"url\",\"url\"=&gt;\"https://127.0.0.1:9/sendportal-policy-outage\"]]]; file_put_contents(\"composer.json\",json_encode(\$m,JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES).PHP_EOL);"; env -u COMPOSER -u COMPOSER_HOME -u COMPOSER_POLICY -u COMPOSER_NO_BLOCKING -u COMPOSER_NO_SECURITY_BLOCKING -u COMPOSER_IGNORE_PLATFORM_REQ -u COMPOSER_IGNORE_PLATFORM_REQS -u HTTP_PROXY -u HTTPS_PROXY -u ALL_PROXY -u http_proxy -u https_proxy -u all_proxy COMPOSER_HOME="$tmp/$operation/home" NO_PROXY="127.0.0.1,localhost" no_proxy="127.0.0.1,localhost" php "$composer_bin" validate --strict --no-check-publish; set +e; if [ "$operation" = update ]; then env -u COMPOSER -u COMPOSER_HOME -u COMPOSER_POLICY -u COMPOSER_NO_BLOCKING -u COMPOSER_NO_SECURITY_BLOCKING -u COMPOSER_IGNORE_PLATFORM_REQ -u COMPOSER_IGNORE_PLATFORM_REQS -u HTTP_PROXY -u HTTPS_PROXY -u ALL_PROXY -u http_proxy -u https_proxy -u all_proxy COMPOSER_HOME="$tmp/$operation/home" NO_PROXY="127.0.0.1,localhost" no_proxy="127.0.0.1,localhost" php "$composer_bin" update --dry-run --prefer-dist --no-interaction --no-scripts --no-progress &gt; "$tmp/$operation.log" 2&gt;&amp;1; else env -u COMPOSER -u COMPOSER_HOME -u COMPOSER_POLICY -u COMPOSER_NO_BLOCKING -u COMPOSER_NO_SECURITY_BLOCKING -u COMPOSER_IGNORE_PLATFORM_REQ -u COMPOSER_IGNORE_PLATFORM_REQS -u HTTP_PROXY -u HTTPS_PROXY -u ALL_PROXY -u http_proxy -u https_proxy -u all_proxy COMPOSER_HOME="$tmp/$operation/home" NO_PROXY="127.0.0.1,localhost" no_proxy="127.0.0.1,localhost" php "$composer_bin" install --prefer-dist --no-interaction --no-scripts --no-progress &gt; "$tmp/$operation.log" 2&gt;&amp;1; fi; status=$?; set -e; test "$status" -ne 0; php -r "\$url=\"https://127.0.0.1:9/sendportal-policy-outage\"; \$transport=\"/(Connection refused|curl error|Failed to open stream|HTTP [0-9]{3})/i\"; \$stanza=[]; \$matched=false; \$check=static function(array \$lines) use (\$url,\$transport): bool { \$text=implode(PHP_EOL,\$lines); return str_contains(\$text,\$url) &amp;&amp; preg_match(\$transport,\$text)===1; }; foreach(array_merge(file(\"$tmp/$operation.log\", FILE_IGNORE_NEW_LINES), [\"\"]) as \$line){if(trim(\$line)===\"\"){if(\$check(\$stanza)){\$matched=true; break;} \$stanza=[]; continue;} \$stanza[]=\$line;} exit(\$matched?0:1);"; done; php -r "\$m=json_decode(file_get_contents(\"$repo/composer.json\"),true,512,JSON_THROW_ON_ERROR); \$p=\$m[\"config\"][\"policy\"]??[]; \$a=\$p[\"advisories\"]??[]; \$expected=[\"PKSA-3r5d-mb8f-1qw9\",\"PKSA-m5cs-t1y6-qpcs\",\"PKSA-mdq4-51ck-6kdq\"]; \$reasons=\$a[\"ignore-id\"]??[]; \$actual=array_keys(\$reasons); sort(\$expected); sort(\$actual); \$reasonsAreDocumented=true; foreach(\$expected as \$id){\$reason=\$reasons[\$id]??null; \$reasonsAreDocumented=\$reasonsAreDocumented &amp;&amp; is_string(\$reason) &amp;&amp; str_contains(\$reason,\"internal-only\") &amp;&amp; str_contains(\$reason,\"project owner\") &amp;&amp; str_contains(\$reason,\"Phase 2 lockfile review\") &amp;&amp; str_contains(\$reason,\"compatible stable SendPortal Core\");} exit((\$p[\"ignore-unreachable\"]??null)===false &amp;&amp; (\$a[\"block\"]??null)===true &amp;&amp; (\$a[\"audit\"]??null)===\"fail\" &amp;&amp; \$actual===\$expected &amp;&amp; \$reasonsAreDocumented &amp;&amp; !isset(\$p[\"gap-closure-outage-probe\"],\$p[\"sources\"]) &amp;&amp; !file_exists(\"$repo/composer.lock\")?0:1);"'</automated>
  </verify>
  <acceptance_criteria>After each temporary probe manifest passes strict Composer validation, both isolated operations fail with a nonzero status. For each operation, a single Composer error stanza correlates the exact configured loopback HTTPS URL with a recognized connection or HTTP transport failure caused by its sole added custom policy source. The probe commands bypass proxies; the repository remains free of the probe policy and a lockfile, and it retains the complete documented D-02 three-ID rationale. The evidence demonstrates that `ignore-unreachable: false` is enforced for both update and install rather than silently accepting incomplete policy data.</acceptance_criteria>
  <done>The authoritative COMP-03 outage-path gap is closed by a reproducible fail-closed proof for both Composer operations.</done>
</task>

</tasks>

## Artifacts this phase produces

- `composer.json` with the exact `config.policy.ignore-unreachable: false` completion of the approved native security policy.
- `01-03-SUMMARY.md` with strict validation, isolated real-PHP-8.4 standard install/audit evidence, and separate intentional update/install policy-source-outage failure records.
- No committed `composer.lock`, Laravel/Core change, CI change, or application-source change.

## Source Coverage Audit

| Source | ID | Required outcome or constraint | Plan | Status | Notes |
|--------|----|--------------------------------|------|--------|-------|
| GOAL | — | Secure PHP 8.2–8.4 graph resolves on real PHP 8.4 without Composer safeguards failing open. | 01-03 | COVERED | Tracer re-proves the standard operator path; Task 2 proves the missing outage path fails closed. |
| REQ | COMP-01 | Standard clean PHP 8.4 install has no platform-ignore or emulation route. | 01-02, 01-03 | COVERED | Already verified in 01-02; rerun after the policy completion is necessary to prove no regression. |
| REQ | COMP-02 | Root manifest accurately declares PHP 8.2, 8.3, and 8.4 support. | 01-02, 01-03 | COVERED | 01-02 established `^8.2`; Task 1 asserts it remains unchanged. |
| REQ | COMP-03 | Roave is replaced without weakening platform checks or the dependency-security safeguard. | 01-03 | COVERED | Adds and proves the missing fail-closed setting while retaining D-02's narrow exception. |
| RESEARCH | — | Composer 2.10+ native policy, real PHP 8.4, isolated fresh home, stable Laravel, and no bypasses. | 01-03 | COVERED | Task 1 carries the established evidence protocol; Task 2 uses Composer's documented custom-policy-source mechanism only in disposable copies. |
| CONTEXT | D-01 | Preserve PHP 8.2–8.4 support with PHP 8.4 as the proof target. | 01-03 | COVERED | The manifest assertion retains `^8.2` and every executable proof runs on PHP 8.4.x. |
| CONTEXT | D-02 | Native blocking/audit policy retains exactly the three time-bounded residual-risk IDs. | 01-03 | COVERED | Exact policy assertion and configured audit preserve the approved boundary. |
| CONTEXT | D-03 | Apply only the smallest compatible policy change; preserve Laravel 11 and Core integration. | 01-03 | COVERED | One manifest boolean is added; all temporary outage policies are removed with their disposable copies. |

## Unresolved Assumptions

- The deterministic fallback labeled COMP-01 through COMP-03 unclassified. The authoritative verification resolves COMP-01 and COMP-02 as satisfied and identifies one concrete COMP-03 blocker; this plan does not claim new coverage for unrelated work.
- The recorded Herd Composer path and live Packagist access are execution-time prerequisites for the normal-install proof. Task 1 asserts both before mutation; failure is a halt condition, not a reason to use a platform, policy, or cache workaround.
- The loopback URL is intentionally unreachable by construction. Task 2 still requires observed nonzero exits and transport evidence for both Composer operations, so the assertion is not accepted merely from configuration inspection.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed `composer.json` → Composer policy engine | The repository controls whether dependency-policy source failures are fatal or silently ignored. |
| Composer policy engine → Packagist/custom policy sources | Advisory and policy availability is external, mutable input during dependency resolution and installation. |
| operator environment/global Composer home → Composer invocation | Environment variables or a global home can bypass or replace the committed policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Denial of service | advisory or custom policy source availability | high | mitigate | Commit `config.policy.ignore-unreachable: false` and require isolated update/install commands to fail when a temporary loopback policy source is unavailable. |
| T-01-09 | Tampering / information disclosure | Composer environment, proxy routing, and policy interpretation | high | mitigate | Use an empty temporary `COMPOSER_HOME`, unset the established policy/platform override and HTTP(S)/ALL proxy variables for the loopback probe, set both proxy-bypass variables to the loopback hosts, and assert the exact committed policy before every proof. |
| T-01-10 | Elevation of privilege | advisory exception boundary | high | mitigate | Require `block: true`, `audit: fail`, exactly D-02's three per-ID entries, and each entry's internal-only, owner-accepted-risk, Phase-2-review, and compatible-Core expiry rationale; configured audit must pass only under that explicit mapping. |
</threat_model>

<verification>
Run Task 1's quick strict JSON-policy assertion, isolated PHP 8.4 script-enabled standard install, and configured locked audit. Then strictly validate each Task 2 disposable probe manifest before its operation; with proxy bypass enforced, require both `update` and `install` to return nonzero, name the exact configured loopback URL, and record a connection or HTTP transport failure. Finally confirm the repository contains only the committed fail-closed policy addition, its fully documented D-02 exception map, and no lockfile.
</verification>

<success_criteria>
- COMP-03: The native replacement for Roave fails closed if a policy/repository source is unreachable during Composer update or install.
- The approved D-02 three-ID exception, platform checks, PHP declaration, Laravel 11 constraint, and SendPortal Core constraint are unchanged.
- A real PHP 8.4 standard Composer install and configured audit still succeed with the completed policy; no repository lockfile is created.
</success_criteria>

<output>
Create `.planning/phases/01-constraint-resolution-and-security-control/01-03-SUMMARY.md` when done.
</output>
